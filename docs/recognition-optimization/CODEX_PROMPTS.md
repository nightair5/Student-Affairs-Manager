# 可复制优化、目标与恢复提示词

> 2026-09-05 / RCO-DOCS-002。提示词是操作模板，不是既有授权。
> 当前只完成文档重整；010-E1已交付，B9仍FAIL，RCO-G5质量未证明。
> 权威：当前用户指令 → AGENTS/PRD → 最新追加日志 → CURRENT_CONTEXT → 总计划 → 本模板。
> 不从RCO-0重启；不复用旧API Key访问、付费调用、10元预算、真实材料或部署许可。

## 1. 下一步优化提示词（待用户明确采用并授权）

```text
授权执行 RCO-5-MAINLINE-01：仅建立“完整通知→可核对建议→用户确认→隔离存储”的本机端到端验证链路，0次外部识别模型调用。

仓库：C:\Users\Winner\student-affairs-multimodal-exp
分支：codex/e2-multimodal-recognition-exp；运行前现场核验。
目标：先证明“给定正确人工响应，产品能否完整承接并正确确认”，找出模型以外的主线损失。
本包不测模型准确率，也不声明RCO-G5通过。

允许：
新增最小隔离适配、工程夹具、测试入口、测量设计和报告；
复用已有合法结果做只读诊断，不覆盖既有结果。
确认只能写新建测试数据库/内存repository，不能触及用户正式工作区。

禁止：
外部识别模型请求、verifier、Repair、retry和实验费用均为0；
不读取或写入API Key，不沿用历史模型调用许可。
不修改既有Expected/freeze/dataset/checkpoint/cache、冻结组件、契约、历史runner或结果；
不执行旧一次性runner，不创建B10或新的盲测数据。
不改稳定路径、RC.4/Release/Production，不启动RCO-6，不部署，不做真人或真实材料研究。
确需改变受保护或公共产品组件时，提交最小差距与下一包范围并停止，不扩大本轮授权。

工作步骤：
1. 核对git status、分支HEAD、当前授权和保护哈希。保留所有用户未提交内容。
2. 阅读AGENTS、PRD第14节相关部分、CURRENT_CONTEXT、最新日志、
   总计划第4节；不重读全部历史报告。
3. 追踪真实App/capture/recognize/draft/DomainCommitPlan/repository，
   区分产品实际组件、隔离适配、模拟部分与尚未接入的研究组件。
4. 以明确标注的人工工程响应验证正确结构能够完整通过。
   工程夹具不是模型预测、人工独立语义真值或新盲测。
5. 检查动作、对象、执行人、时间、材料、条件、修订与来源关联；
   不能用空数组、默认日期或未知转false掩盖缺字段。
6. 覆盖单/多任务、无日期/模糊时间、纯信息、问句/命令、
   条件真/假/未知、旧要求作废与新要求、长来源尾部要求、
   单项错误隔离、失败恢复、部分确认、刷新和重复提交。
7. 至少验证一个真实客户端组件与隔离存储联动场景。
   读回最终存储验证结果，不以toast、点击或composer测试代替闭环。
8. 输出不可表达字段、漏项、误勾、人工补录及实际组件覆盖范围；
   未走到的阶段标NOT_RUN。
9. 设计首次建议快照、修改类别、active/wall/wait时间和最终处置记录，
   但不采集真人数据、不上传编辑内容。
10. 定向验证后做无上下文独立审查；只修本轮根因最多两轮。
    通过后运行适用工程门与保护检查、更新短交接和追加日志，
    单独提交并推送后停止。

验收：
正确可表达响应的关键字段端到端保真100%；
错误默认勾选=0、未确认正式写入=0、重复/覆盖/丢失=0；
非法或不完整输入有明确失败/核对状态，不能假装整份成功。
现有冻结契约无法表达则报告缺口及PARTIAL/NO_PROMOTION，
不造一个玩具链路绕过真实问题，也不擅自改冻结契约。
不以全部不勾选或不输出获得虚假安全成功。

上下文：
每轮只处理一个主根因，先rg再读源码片段；
常规工具摘要尽量≤30行，长日志留在获准位置；
CURRENT_CONTEXT约80行、不超过200行且12 KB。
压缩/交接前保存目标、授权、HEAD、证据路径、失败和下一步，
恢复后先核对现场，不凭聊天记忆调用模型或重跑实验。

最终只报告：
改了什么、推进主线哪一环、验证数字、未解决问题、
保护状态、提交推送状态、下一包需要什么授权。
本轮模型识别准确率必须写“未测量”；不要自动进入MAINLINE-02。
```

## 2. 长期目标提示词（目标，不是持续执行或付费许可）

```text
长期目标：把学生事务管家做成在明确支持范围内，
能从文字、图片和文件可靠形成待确认任务的商业化产品。
用户价值是少漏事、少改错、少花时间，不是更多AI按钮或更多规则。

主线：
完整读取→正确理解→任务/时间/材料/条件/修订关联完整
→用户可回看依据并快速核对→确认后可靠保存→失败可恢复。

优先顺序：
先处理造成最多漏项、错事或人工修改的根因；
保留来源保护、统一时间AST、Schema、v8事务和用户确认机制。
AI承担受引用约束的语义理解，本机构造来源位置、有效引用和selected；
程序不能因引用存在就宣称语义正确，模型不能直接创建正式任务。

优化必须同时提高效用并保持安全：
首次建议与用户修改后结果分开；
漏项、误项、未知率、错误默认勾选与重大修改一起报告；
工程回归、合成代理、独立真实材料、真人效用和发布证据分开。
未知不是false，少输出不是高成功率，冻结不等于真值正确。

路线：
完成RCO-5隔离端到端上限与候选职责验证；
新Development同模型配对证明完整事实净收益后，才申请RCO-6模态消融；
视觉没有稳定净收益就保留文字路线；
最终按已批准契约做两批独立真实Holdout、真人计时与浏览器验收；
Commercial Preview与Production分别授权。

商业上同时验证真实省时、重复使用、付费意愿和单位成功处理成本。
开发先聚焦高频通知不等于自动缩减正式验收矩阵。
不提前扩展支付、同步、微信或自动外发，不用问卷意向冒充收入。

每阶段必须有明确授权、唯一主假设、有限预算、可验证交付和停止线；
本提示词不授权任何未批准的模型、Secret、真实数据、真人、部署或下一阶段。
维护短CURRENT_CONTEXT和追加日志；保护所有冻结证据和用户内容。
同根因两轮修补无主指标收益时重审职责或范围，停止堆关键词。
不承诺自然语言绝对零错误，不把一次PASS宣布为商业化完成。
```

## 3. 压缩/更换开发模型后的恢复提示词

```text
先恢复上下文，不立即执行实验：
1. 核对 C:\Users\Winner\student-affairs-multimodal-exp 的git status、分支、HEAD和upstream；
   不切换或覆盖用户工作区。
2. 阅读AGENTS.md、PRD当前相关章节、CURRENT_CONTEXT、
   OPTIMIZATION_LOG最新有编号追加记录和总计划当前工作包。
3. 日志开头旧状态索引不代表最新阶段。010-E1已交付，不从RCO-0重做；
   B9历史FAIL不变，MAINLINE工作包是否获批只看当前明确授权。
4. 只按证据路径读取必要源码/指标摘要；付费/正式验证前再读对应完整契约。
5. 复述目标、授权、当前失败根因、允许改动和禁止动作后继续。
6. 只读任务或状态冲突时仅报告差异，不为了同步文档擅自写入。
7. 不凭摘要读Secret、重跑一次性runner、创建新数据、调用付费或部署。
8. 获批写入阶段结束时更新短交接/追加日志并如实记录提交推送；
   当前文档提交SHA以Git为准，不递归写入自身或冻结清单。
```
