# 可靠基线存量回答转化与核对减负审计

本轮没有调用模型，也没有改候选、原答、评分或账本。目标是把 candidate03 已有的 20 份真实历史回答一次回放清楚：正确事实能否经过核对进入原子保存，哪些是合理阻止，哪些还需要修改模型事实。

## 结论先说

- 20 份来源包含 31 条建议任务。
- 其中 20 条事实正确且在工程回放中正确保存；分布于 16 份来源。
- 3 条仍需先纠正模型事实，不能因技术上能写入就算成功。
- 3 条因条件未知、依赖未满足或日期仍待核对而合理待处理。
- 5 条是已取消、被替代或条件不成立的旧行动，正确结果就是不创建新待办。
- 2 份纯信息通知本来就没有任务。
- 既有共享材料时间修复生效后，20 份中没有新的程序误拦；独立仓储读回、重复回放幂等、原答与首次建议保留均通过。
- 28 份独立材料事实各保存一次核对结果；共享给多个任务时不按任务重复要求同一核对。

这里的“正确保存 20 条”是公开接口上的工程回放，不是真人转化率，也不是模型准确率提升。程序一度允许写入的 23 条中，有 3 条语义仍错，已从成功数中扣除。

## 20 份来源结果

|来源|建议任务|正确保存|其余处置|
|---|---:|---:|---|
|U01 校园影展送审安排|2|2|无|
|U02 温室巡查记录|1|1|模糊日期经明确接受“日期待定”|
|U03 志愿服务影像留存|1|1|无|
|U04 户外水样采集的条件通知|2|1|1 条条件未成立，不创建待办|
|U05 报销补件处理|1|1|无|
|U06 设备借用候补安排|1|0|条件未知，合理待处理|
|U07 校刊稿件交接方式修订|2|1|旧寄送要求已作废|
|U08 实训行程取消与独立提交|2|1|旧参访不执行；纯取消被原答误写成替代，仍需改语义|
|U09 校园观星夜信息|0|0|纯信息|
|U10 社团器材盘点交接|2|2|无|
|U11 材料送样前的准备|3|1|提交样本的条件被误判成立；另多造“准备两个密封袋”任务|
|U12 毕业展布展责任澄清|3|1|1 条错误依赖/材料归属需改；1 条旧要求已取消|
|V01 开放日设备交接|2|2|无|
|V02 展架标识安装|2|1|安装任务的日期与依赖仍待核对|
|V03 实验日志校对|1|1|模糊日期经明确接受“日期待定”|
|V04 个人资料留存|1|1|真正无日期，正常保存|
|V05 备用电源领取条件|1|0|条件未知，合理待处理|
|V06 数据交付要求替代|2|1|旧上传要求已作废|
|V07 两项独立归档|2|2|无|
|V08 校园植物观察说明|0|0|纯信息|

完整逐任务状态、核对操作和保存结果见 `REPLAY.json`。运行时将 `fetch` 改为禁止调用并确认网络调用数为 0。

## 本轮减少的程序负担

既有共享材料时间修复已经消除了 U11/V02 的假性 `MULTIPLE_DEADLINES`。本轮继续修正用户说明：页面不再只显示 `unknown` 或技术错误码，而是明确告诉用户：

- 条件尚未确认，所以任务保留但现在不作为可执行任务保存；
- 条件明确未成立，所以不会将它当作当前行动；
- 旧要求已作废或被替代，所以不会创建新的待办；
- 真正的时间冲突、坏引用和修订目标缺失分别说明原因。

这只是把阻碍解释清楚，没有降低确认规则。新增反例证明条件未知和旧要求作废仍不能被选择或确认，失败事务不改变工作区。

## 真实页面、刷新、独立读库与下载

本轮按约定只尝试一次官方浏览器连接，约 25 秒后返回 `nodeRepl.fetch request failed`，因此没有循环连接、重装插件或重复要求刷新。当前自动浏览器操作记为 `NOT_RUN`。

可复用的同一 6632 隔离库真实证据有两组：

1. 先前已由真实页面完成 Q01/Q07 三项正确任务确认，刷新后任务中心、日历、详情及独立仓储读回一致；这轮没有重新点击，不冒充本轮新操作。
2. 2026-09-13 实际下载的独立仓储工程 JSON 与完整测试库 JSON 已在本轮重新读取比较。两份文件来自两个实际下载入口，canonical workspace 完全相等，稳定 SHA-256 为 `7a3b21a3fcbf02c19c5ae56cf3558cf4ea8aedacc214e6779b4100ecfc2b8c7f`。其中真实保存了任务“填写送样登记单”、材料“送样登记单”（`required=true`、`status=unverified`）及截止 `2026-09-17T14:00`；两份来源、识别运行、草稿、5 条历史和 8 条证据引用逐字段一致。

这组下载只能证明当时 U11 独立任务的刷新/读库/下载闭环，不含第二条替代关系的下载，所以不能把两个当前旅程都写成重新验收通过。详细逐字段哈希见 `DOWNLOAD_COMPARISON.json`。

## 下一次模型优化只做一件事

下一优先问题是**复合通知中的事实归属判断**：模型要分清某个短语是独立动作、附属材料，还是条件/取消关系的对象。证据集中在 U11/U12：错误已经存在于模型原答，当前字段、转换和保存结构能够表达正确结果；继续改本机规则只会掩盖模型事实错误。U08 的纯取消/替代误标是同类关系归属问题，但不影响独立正确任务保存。

此前 candidate07 的提示顺序重排和 candidate09 的结构简化都没有稳定改善，反而产生退化。因此下一轮不应创建 candidate10 继续堆提示词；更有信息量的实验是固定 candidate03、输入和 wire，只改变模型能力，针对 U11/U12 类复合归属通知做一次小规模模型对照。它需要新的模型调用授权，并应先核验届时实际可用模型名和价格。本轮没有实施或付费。

## 工程检查

- app/node TypeScript：通过。
- lint：通过，既有 warning 保留。
- 识别契约、时间 AST、server、Worker、Functions：通过。
- Vitest：1382 通过、0 失败、1 跳过。
- Vite 与本地 6632 preview 构建、HTTP 状态：通过；模型调用关闭。
- 零调用20份回放、独立仓储读回、重复回放、双文件比较：通过。
- 根 `.env` 未读取；模型调用 0。
- 旧 RCO 检查器的历史 `package-lock` SHA 差异继续单列，没有删除或弱化旧断言。

本轮交付的是正确建议到保存的工程承接率和解释性改善，不是首次模型准确率提升。
