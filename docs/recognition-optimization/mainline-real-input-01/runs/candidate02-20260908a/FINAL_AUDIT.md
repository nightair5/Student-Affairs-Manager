# 新候选02：真实验证已完成，浏览器交付未闭合

## 结论与实际改进
8次真实请求全部完成；条件未成立、旧/新要求表达有改善，模糊时间仍漏失。严格整份通知一致仍1/8，不宣布质量达标。
代码工程层通过；实际Edge仅完成原库独立读回和C08原答载入。逐键核对/保存/确认/刷新/新下载未闭合，不能包装为整包成功。

- 一个candidate02复用原语义结构与来源索引，实体先于关系，保持条件三值、修订引用、材料归属；旧候选/Expected/评分器不改。
- 新回答保留独立候选Run/Draft，不标人工、不覆盖旧回答和已确认任务。C05/C08本机公开接口确认读回通过。
- 空白OCR明确报问题；PDF线程失败保留已读内容；选中空白页不能冒充读全后发送。
- 混合页OCR和文本层分别可看，实际OCR可放入校对缓冲后明确保存，不用参考正文替换。
- candidate02读取工具只返回新读取，不覆盖旧发送记录，不写库/发送模型。

## 新旧结果：原评分不改
8份已见匿名工程通知，不是盲测；C使用原A完整文字，不是OCR成绩。完整分母见METRICS.json。

| 新C指标 | 原严格结果 |
|---|---:|
| 请求成功 | 8/8 |
| 任务正确/预测/参考 | 8/9/9；P、R均88.89% |
| requiresAction正确/参考 | 9/10 |
| 时间正确/预测/参考 | 1/2/3 |
| 材料正确/预测/参考 | 0/6/2 |
| 条件正确/参考 | 8/9 |
| 修订正确/参考 | 1/1 |
| Evidence精确正确/参考 | 0/14 |
| 语义标签精确正确/参考 | 0/10 |
| 覆盖声明正确/参考 | 13/25 |
| Complete Case | 1/8，仅纯信息C04 |
| 原评分Major Correction | 7/8 |
| Forbidden | 0；自动选择NOT_ENABLED |

材料/依据的精确零分包含实体粒度、格式文字、scope选择差异，不能解释为完全没有材料或来源依据，也不能改评分掩盖。
旧B：6正确/7预测/9参考，P85.71%、R66.67%，Complete1/8；有读取校对差异，不作纯Prompt因果比较。
原绑定文字A03–A08与C03–C08配对：旧4/5/6，新5/6/6，Recall66.67%→83.33%。这是6份配对诊断，不替换原A八份分母，A01失败永久保留。

逐例业务解释：
- C01两个动作和实际截止保留，事件不再声明空实体；第二时间类型、格式、完成标准仍有差异。
- C02保存活动手册/无截止正确；额外手册材料须核对必需性和准备状态，不是新增任务。
- C03描述有“近期”，时间coverage却有声明无实体，仍需纠正，是未解决主瓶颈。
- C04纯信息正确；C05条件true正例可确认；C06任务保留且条件false，改善旧A06 unknown/旧B06漏项。
- C07 unknown正确，“请提交”与“提交”造成严格任务匹配差异，不是新增实际行动。
- C08旧打印任务取消、新提交任务有效且修订引用存在，改善旧坏引用。旧项材料coverage仍不一致；新项独立确认本机验证成功。

## 真实本机读取与工程测试分开
实际Node Tesseract读取PNG/JPEG/WebP三载体有输出，B01“凭证”读成“赁证”，未自动替换。
实际PDF.js文本提取+页面栅格OCR完成四PDF五页：B07扫描页文本层为空，OCR读出审批结果；B08同页文本层只有旧要求，OCR还读出新要求。这证明仅取文本层会漏内容。
引擎stdout有额外语言加载警告，原日志保留；非空输出不认证完整。Node实测不是Edge读取链路验收。READING_DIAGNOSTIC保存原输出，无参考正文进入识别。
首次独立PDF诊断destroy接口错误，日志保留；改用当前loading task接口后通过，未改产品依赖。

## 工程与保护
- 全量功能1250通过/1原skip/0失败；不是1250份模型样本。
- 最终影响层33通过+1新增启动器断言失败；新断言误把解析访问等同输出污染。现明确保留旧fixture解析路径、检查tree-shaken后输出无内容，最终该项通过。旧测试不弱化，多次数字不相加。
- 新C05/C08本机公开接口2项：核对确认、旧任务全对象保留、时间/提醒不增、重复回放和独立canonical读回通过；首次超5秒日志保留，最终明确60秒测试超时。
- 预算46、网关46、候选5、新发送审核1组通过。原网关未修改。
- App/Node类型、lint、build、Schema/时间契约、安全扫描通过，4旧lint警告保留。最终变化只重验受影响层。
- 框架envFile/configFile=false，新临时env/cache/output，无根.env读取。
- 旧checker默认历史HEAD断言不改、不冒称当前通过；本轮显式candidate02保护单列。
- 945保护、334旧包证据、原日志/账本前缀不变；44源码最终SHA见快照。
- 发送前为执行者针对性安全复核，不冒称独立评审。发送后客户端变动不重写SEND_REVIEW。

## 实际浏览器与缺口
原6631/原隔离库。独立new CanonicalWorkspaceRepository读回6任务、jobs0，全对象SHA：
a3ea9835a6fde772efb356ebf9bce6405b0ff978257e5fdd8cb5d5b55538b883。
C08通过页面工具载入后仍6任务，未自动确认。上述读回来自真实工具按钮与页面证据，不是对下载文件自算。
自动刷新ERR_BLOCKED_BY_CLIENT；用户再次刷新后原标签控制Emulation.setFocusEmulationEnabled超时。未绕过防护，不把连接失败写成产品故障。新候选实际逐键确认/刷新读库、实际读取校对未完成；旧A02/时区/下载证据保持，不混作本轮通过。

## 调用与费用
新增8次，本包累计24次，全部授权已用完；0 verifier/Repair/retry。新等待合计58,770ms。
合法usage按核验最高价格结算上界：新批0.162228元，全部结算0.427704元；加A01永久未知预留3.30元，占用上界3.727704元。账户实际扣款NOT_OBSERVABLE。
原34条账本前缀逐字保留，追加后51条；未重发旧请求，无新数据/依赖/真实用户库/稳定入口/部署。

## 交付与唯一下一动作
基线本机/远端f5e2c54106b1d363eea41246a980a2f7822ff044，初始干净。
本轮审计提交仅包含新证据、追加账本、短交接与追加日志；业务源码保留未提交。工程可用与整包验收分开。
恢复官方Edge控制后，在原6631/原库完成C08核对→逐键编辑→保存→主动确认→刷新独立读库，以及一条混合PDF校对旅程。复用未变工程，不再调用模型、不重做计划，通过后才业务交付。
暂不建议直接开未见测试：先针对C03模糊时间及材料/覆盖残留做下一轮优化决定。真人编辑时间、省时与商业效果NOT_RUN。

