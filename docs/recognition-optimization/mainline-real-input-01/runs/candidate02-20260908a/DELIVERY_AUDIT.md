# 存量PDF校对与C08确认：真实App补验交付
日期：2026-09-08。起点12a8087ff82ca94eacafa749cdc883b576a1e5ed，本机/远端一致。
交付范围：已保存B08文件读取记录的校对恢复，以及C08新任务确认保存。不是新一轮识别质量或商业验收。

## 本轮操作与结果
- 官方Edge原tab763114892、原6631、原隔离库。用户要求再次直接操作后连接恢复；全部以下校对/确认由代理在真实App执行。
- 进入本轮时原库已有B08-revision.pdf读取Source（source:653956bd），没有已保存校对。文件5007537字节、SHA1d23427a358457fb001ebc5f5fd32ca42423458c0355304b3f15601ebd79bdee与旧载体一致。初次上传动作未由本轮观察，不冒称代理新上传。
- 真实InputReview恢复这份读取，观察文本层只有旧要求，OCR有旧要求和新要求。点击“采用本机OCR”进入缓冲；未保存提示与保存按钮状态可见，模型按钮始终关闭。
- 逐键删除尾部换行时连句号一同删除，随后逐键补回句号并再次明确保存；两条历史均保留，不修改或抹掉第一次记录。最终校对包含完整旧新两句。
- 刷新后重新打开InputReview、恢复同一草稿，最终校对仍在，保存按钮禁用；原始文本层/OCR未覆写、sendSnapshot仍null。
- C08已有Run/Draft source:78c7cfc8:draft:1:2，本轮未再次载入。原答明确旧打印任务cancelled/superseded，新提交任务active，修订supersedes有效；旧项仍有材料coverage问题且不能确认。
- 针对新任务保存材料决定required=true/status=missing（工程场景核对，不是新增模型事实），点击本项事实已核对，仅勾选新提交任务，再加入已选1项。
- 刷新后任务中心7项；新任务可在日历无日期区和详情找到，详情保留修订、原文及review_material/review_task/confirm记录。
- 旧6任务、旧材料逐对象不变；仅新增1任务和1材料，旧作废打印任务没有正式绑定。时间仍2个，提醒0，实际jobs函数0。
- 两个“提交电子报名表”分别属于旧A08人工纠错和本轮C08不同Run；本次确认记录只有1条，不冒称跨候选自动去重已验收。
- 原模型raw/first/input/send及RecognitionRun、SourceVersion未变；当前A08来源仅按确认行为从needs_review变成partially_confirmed并更新操作时间。

## 文件与仓储证据
先点击真实工程工具“独立仓储读回全对象”，再下载其结果；另从真实App导出完整测试库。
比较基准来自按钮内部new CanonicalWorkspaceRepository(new IsolatedTestStore(name)).load()，不是下载文件自算两次。
- 操作前全对象SHA：02c4173ed3024d0b6ae941bff2a4f386ac58df481d02ac5a64e48714db7ea624。
- 操作后全对象SHA：e789d9a39a15d6f11b1d449444f7def1493dd23b51ae7802e768311289baf8fe。
- 实际完整导出1002971字节，文件SHA1711a6a7027aac8e17e6b31350b24780a4008ab0641592690b4fa1da325dbff2。
- 下载对象与独立仓储全对象深比较一致；现有v8+real-input-01联合validator通过。
- 原文件留在Downloads，新增DELIVERY_BROWSER_BEFORE/INDEPENDENT/DOWNLOAD是逐字副本；路径、文件SHA和验证结论见DELIVERY_CHECKS.json。

## 未计通过的步骤与现存问题
- 完成存量校对后额外尝试通过官方fileChooser.setFiles重新上传同一B08，接口返回Not allowed。未重试、未绕过、未产生第二份Source；随后关闭面板。
- 因此“本轮重新上传成功次数”为0；上传/首次读取仅沿用已存在的真实文件读取记录，不把重传或首次上传动作列为本轮通过。新文件上传自动化仍有工具能力缺口。
- 材料控件一次精确label定位无匹配，重新观察后按实际combobox可访问名称操作成功；未改产品。
- 文件审计初稿误要求确认来源status/updatedAt也完全不变，报告失败；已改为精确验证needs_review→partially_confirmed及确认时刻，正文/身份/其他字段继续逐项不变。没有修改产品、旧测试或评分。
- C03模糊时间仍待核对，防止假日期不等于已解决其识别错误。复用已有负例与C05/旧B02正常对照，不重跑。
- 收件箱读取草稿的旧兼容摘要仍可能只展示原文本层/“未整理”；完整校对以InputReview为准。正式编辑/执行/真实提醒/ICS、任意新输入模型识别、稳定接入和商业化不在本次交付结论内。
- 自动选择NOT_ENABLED；人工核对后的正确保存不计入模型首次准确率。

## 工程、保护与Git
本轮未修改任何产品/测试/依赖源码。44实现逐字匹配READ_CLOSE最终快照；
复用43项受影响测试、类型/lint/build和未变部分的原1250项日志，不重新跑模型或全量工程。
945保护、382旧证据、51条账本全文及日志旧前缀保持；本轮0新增模型/费用/密钥/剪贴板。
历史浏览器NOT_RUN与失败审计不改，仅追加本补验。按精确实际改动清单交付现有业务实现和本次报告，不把新上传工具拒绝或完整产品质量说成PASS。
业务提交与远端精确号以最终Git核验记录为准；不强推或自动变基。完成后停止。
