# 本机读取校对与新候选确认交付：本机通过，Edge待补
日期：2026-09-08。起点：f3b1ed68604e3f95c25894ee4717b1eed2072ec9；执行前远端一致。
状态：LOCAL_ENGINEERING_PASS_BROWSER_INCOMPLETE。仅审计交付，业务实现仍保留未提交。

## 本轮实际改了什么
- 在现有 InputReview 增加显式 localOnly；仅当前 recordedCandidate02 实验模式启用。
- 允许读取、查看文本层/OCR差异、缓冲校对、明确保存和选择已保存读取草稿。模型按钮明确关闭，runtime发送处理器独立拒绝。
- 新读取使用 local-review 操作身份，恢复列表只展示这些独立读取来源；旧A/B/C输入、原回答与已确认任务不作为该入口的编辑对象。
- 复用原校对与原子仓储，不重构全局契约。普通入口和旧录制模式保持默认行为。
- 新增当前 read-close 检查配置，绑定本次审计HEAD、44源码、945保护、382旧证据和完整51条账本；旧历史HEAD检查不改。
- 相对本轮起点只改变 InputReview.tsx、runtime.ts、acceptance.test.tsx 和两个 checker 文件；其余39实现逐字不变。

## 检查结果（不能相加不同尝试）
- 受影响 acceptance/extraction/inputReceipt：43/43；包含实际旧C03/C05/C08回答的本机公开接口回放。
- C03：模糊时间仍有覆盖声明而没有可确认时间实体，保留 COVERAGE_TIME；确认被拒绝，没有伪装成真正无日期。
- C05/C08：各有有效任务可主动核对确认；本机新仓储实例读回一致，既有任务、原回答、首次建议、时间和提醒保持。
- localOnly正常/反例：本机保存校对可恢复；未保存缓冲未写入；执行器0次；直接发送处理器拒绝且整库不变；旧输入和任务未覆盖。
- 当前编排1组正反检查通过：错误HEAD、账本、保护、候选SHA、路径数量均拒绝。第一次再次执行未传载体manifest而失败，补回原参数后通过；失败日志保留，未改测试救门。
- 本轮类型、受影响lint、稳定构建通过；构建使用新临时envDir/cacheDir/outDir，configFile/envFile=false。保留既有大包警告。
- 原1250通过/1旧skip为前轮结果，日志及未改部分按SHA复用；不称本轮重跑1250，也不把SSR/内存检查称为实际浏览器通过。
- 模型、verifier、Repair、retry本轮均0，账本51条全文未改变；未访问密钥/剪贴板。

## 实际Edge证据与剩余项目
- 使用原6631、原隔离库；未清库、建替代库或fallback。最新本机服务为 recorded_batch + recorded_candidate02，upstreamEnabled=false。
- 官方控制选择原tab763114892成功，AX看到真实App和6项任务。刷新成功；点击“打开文字、图片或文件录入”随后 Runtime.evaluate 超时，截图报告 Cannot take screenshot with 0 width。
- 只据此认定控制受阻，不将其认定为产品读取、保存或数据库故障。没有反复刷新、改系统或关闭防护。
- 已一次请求用户将原测试标签置前、展开窗口、关闭开发者工具（如开启）并手动刷新；本轮未取得恢复后的操作证据。
- 混合PDF上传→采用实际OCR→逐键校对→保存→刷新恢复：NOT_RUN。
- C08在原库的主动确认→任务中心/日历无日期列表/详情→独立new repository读回：NOT_RUN。前轮仅载入回答，不是已确认。
- 本轮未执行上述写库操作，不能报告当前库已经增加第7个任务。原6项保护的最新实际读库证据仍来自前轮。
- 原A02、下载、时区未受此次改动影响，不重跑；本轮没有新下载证据。

## 保护与交付边界
945保护全文不变；334旧证据加48份前轮candidate02证据共382份核验不变；
原候选、模型回答、评分、账本全文及日志旧前缀不变，详见READ_CLOSE_CHECKS与IMPLEMENTATION_SNAPSHOT。
本轮追加证据和短交接可提交，未满足实际浏览器验收的实现不包装为整包业务交付。
未做新增模型准确率测量，人工校对成功也不会改写首次模型错误。

## 唯一下一动作
恢复原Edge测试窗口后，只补两条真实旅程：B08同页混合PDF本机校对保存恢复；C08核对新旧修订并确认有效新任务、刷新独立读库。
实现不变则复用本轮43项和类型/lint/构建，不重做计划、不重调模型、不重跑全量。
先检查原库现状，避免重复载入C08；旧已确认内容不得覆盖。通过后才精确业务提交推送。

