# 同包真实浏览器进展（尚未全包验收）

Edge用户标签763114724，真实App，同origin http://127.0.0.1:6631；原run real-input-d030c507-3f7c-4a2c-a511-8cd5bd534862。原隔离库前缀rco-mainline-01-02-i1-，没有换库、清库或用户数据库fallback。

## 实际完成

1. 用户截图中的初始化错误由首次写入被无条件拒绝引起；修为同事务仅空记录初始化。此前另一个自动化标签的ERR_BLOCKED_BY_CLIENT单列，不混成产品错误；用户实际标签正常打开。
2. B01 PNG/B02 JPEG有错字；B03 WebP/B04 PNG为空；B05文本PDF、B06扫描PDF、B07混合PDF读取正文正确；B08页内图文PDF的文本层缺新要求，另有完整OCR和差异警告。这是实际读取记录，非mock。原始错误不覆盖，不称全文全对。
3. 通过实际App输入面板恢复B01/02/03/04/08，逐键输入旧工程正文并显式保存。B01首次输入5秒超时，核对buffer后仅补末尾句号，再保存；不以fill替代，不当作新产品失败。其余3项不改。代理工程校对不是真人耗时研究。
4. 实际注入Source写入失败，独立新仓储实例读回前后完整SHA相同：f0ce130048e760e1395792a3ca2b9e741736543c7d4ae74ca9b58ab0ef2d1d06。正常重试保存A8来源成功。
5. 校对后独立读回16来源/0任务/0真实提醒jobs；SHA d25779b8e47532ea6f03bbbca949c1d80d1d578da2185f24bfa1bf089ab3016d。
6. 工程按钮准备A/B（不调用模型），来源先存产生16 Run/Draft、0任务/0jobs。独立读回SHA b7c08ed26349a7ad5e1a67236058aa9c91ed6b678a2cf210647f5d59473b758a。
7. 重复准备后所有requestText与实际库不变；只有handle.duplicate由false变true（16处），这是复用回执而非新请求。完整库SHA保持b7c08ed…。
8. 三个真实JSON文件通过UI下载，实际文件与对应独立读库基准相符，见DOWNLOAD.json；准备文件也来自UI，不是伪造文件。

## 原始读取错漏（校对前）

从实际下载receipt.pages[].chunks重建主读取文字，按Unicode字符Levenshtein距离计算；明确排除所有空白再比较旧工程正文，标点和数字不忽略。此口径是本机转写诊断，不是模型语义准确率；B08独立OCR全文仍在ocrChunks保留，表中评价的是默认主读取结果。

|单元|主读取路径|错漏距离/原文字数（去空白）|校对次数|
|---|---|---|---|
|B01|图像OCR|33/82|1|
|B02|图像OCR|1/17|1|
|B03|empty|21/21|1|
|B04|empty|21/21|1|
|B05|PDF文本层|0/21|0|
|B06|PDF OCR|0/21|0|
|B07|文本层+OCR两页|0/26|0|
|B08|PDF文本层，另有OCR差异提示|11/27|1|

逐字原文exact仅B05/06；B07只因页间换行与原一段不同，去空白内容一致。9页中2页原始empty，不能说完整读取9/9。五项人工工程校对后才准备B请求，未伪造OCR成功。下一步模型质量只评价这些明确校对后的B发送文字；读取完整性另表不能掩盖。

## 尚缺验收

目前运行模式seen_engineering_replay，未执行真实模型。正式任务逐项/批量确认、用户已确认关系保存、下游找回以及最终导出均待后续完整旅程验收。本次0正式任务不能证明保存能力全通过。U01/03/04/05/12/13为部分；U06–11、14–16未完成。自动选择NOT_ENABLED。

密钥未传前端、日志、下载或Git。当前浏览器effects foreignDatabase/legacyStorage/forbiddenNetwork均0。未新建语义材料/Expected；模型识别准确率本轮未测量。

## 随后首调状态（不改写前面浏览器阶段）

独立首调复核和固定绑定后，runner尝试A01一次，未取得完整响应；原浏览器仍为明确回放模式，没有冒称页面已live。账本停发、无重试；上游处理/实际费用不可观测，保留3.30元未知预留。详情见AUDIT/RESULT/CALL_LEDGER。模型识别准确率本轮未测量（没有可评分响应）。
