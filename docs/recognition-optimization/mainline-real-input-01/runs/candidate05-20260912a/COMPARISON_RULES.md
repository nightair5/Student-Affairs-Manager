# 同模型配对口径（派发前）

12份通知为单位，两臂同文字、时钟、时区、参数；种子20260912平衡6份03先/6份05先。
先固定候选与组装闭包，再编写本目录12份新合成材料及参考；由同一代理编写裁决，不称独立盲测。

- 实质问题：改变做什么、何时做、需要什么、条件成立与否、新旧要求有效性或归属；漏任务和多造义务均计。
- 等价粒度、措辞、局部ID、逐字但等价的依据范围另列，不拿严格相等代替业务判断。
- 状态或效果纯标签差异只有改变安全选择/实际操作时计实质错误；否则记录结构问题。
- 材料准备状态未见证据时不推定；P10明确照片已完成，其余状态由用户核对，不能将required代替ready。
- P11填写登记单是提交的前置：条件或依赖的等价表达均可，不能让用户提前提交。原严格评分仍保留。
- P09事件无待办仍需保留活动信息，不把空tasks当错误；事件时间独立关联完整性另核。
- 模糊时间可保留日期待定；不要求编造日期。明示无时间的否定句不生成时间实体。
- 每臂固定分母12；派发失败、Schema拒绝保留分母。不剔除差结果、不临时改候选、参考或口径。
- 原评分器逐字历史闭包，原始新模型回答不改写。为调用旧评分器生成明确标注的语义投影，不能冒称旧模型返回；05的原始事实和组装后结构分别保留和分析。
- 主要采用标准：无需重大纠正数量增加、时间/材料实质错减少，任务漏误、条件/修订不退化。未满足不替换当前路线。
- 这批不测真人编辑时间；代理核对动作只是工程操作证据。HTTP成功、结构通过、正确确认分别统计。

首次错误与人工纠错后保存不得混合；现有13任务和两臂不重复确认。0自动默认选择。
