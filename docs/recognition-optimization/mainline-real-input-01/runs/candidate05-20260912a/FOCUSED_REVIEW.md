# 本轮一次聚焦复核（发送前）

范围：candidate05/factAssembly、新模型身份分流、原账本paired05追加、发送绑定。由实施代理进行针对性复核，不冒称独立第三方审查。

- 组装只生成显式前向边的反向列表。无事实+null拒绝；空数组不推出not_stated；未提取/未知状态保留。错误引用、重复ID、模型selected与额外字段拒绝。38项候选/组装/旧wire定向通过，8种工程响应全对象等价。
- 原答保留，组装与评分投影另列。参考只由runner评分使用；请求闭包18文件无fixture/evaluation/Expected。旧评分10文件全SHA及编译bundle SHA一致，通过内存历史闭包复用；不修改现有评分器或伪造provider原答。
- 新model及80请求授权显式分支；旧policy、旧候选、旧已结算记录不重写。原56保留、A01未知预留保持，非法usage/解码重名JSON键不释放预留。102项预算/网关Node验证通过。新请求只由原账本顺序预留，代理TLS/凭据反射检查沿用。
- 派发之前绑定53源码、候选闭包、12输入、参考及原评分；第一个正式请求计入24。Schema或业务质量失败保留结果、不改候选追分；未知用量/身份/安全异常停止派发。
- 当前App回放新版本尚未接完，不能宣称真实产品通过。raw与组装状态分别保存的分流已实现；后续新接入口仍需非零正确确认、原记录不变及独立读库。

结论：允许进入已授权正式配对；产品闭环为NOT_RUN。自动选择NOT_ENABLED。未新增密钥路径/真实库权限/自动确认。

准备阶段反例：新参考初稿误用deadline枚举、数量字符串，以及P04未包含动作所在逗号分句；均在首次派发前按冻结契约订正。候选/组装器在新材料编写后未改，没有模型结果参与订正。保留准备失败摘要，不将其记为模型失败。
