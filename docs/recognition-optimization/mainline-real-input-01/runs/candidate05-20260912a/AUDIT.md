# DeepSeek-V4.1-Flash / candidate05 本轮审计

## 结论

完成新模型接入、一个减负候选和12份新通知的24次配对调用；不采用candidate05。工程检查通过，真实Edge两条旅程未取得证据，整包未完成。本次提交审计、原答和账本追加；业务源码保留未提交现场，见IMPLEMENTATION_SNAPSHOT.json。

不是因为费用而停止：24次已全部执行。剩余阻碍是官方控制不能取得Edge标签列表，不是已证实的产品故障。本轮尚未读取或修改原隔离库，不能把此前13任务当成本轮读回结果。

## 首次建议是否少错

同一新模型、同12份文字、参考时刻、时区及参数，03完整结构对比05单次语义归属+本机反向索引。固定种子20260912，两种先后各6份；候选18文件闭包先固定，再制作输入及参考。同一代理制作和裁决，不称独立盲测。

| 指标，固定分母12份通知 | 03路线 | 05路线 |
| --- | ---: | ---: |
| 请求成功并取得回答 | 12/12 | 12/12 |
| 通过解析/组装，可交给核对层 | 12/12 | 2/12 |
| 原始语义无需重大纠正，代理逐例裁决 | 11/12 | 9/12 |
| 原严格评分整份完全匹配 | 0/12 | 0/12 |
| 请求等待时间中位数 | 6.785秒 | 6.080秒 |

第三行只判断动作、时间、材料及归属、条件/修订，不包含05组装失败；不能用9/12宣称05有75%可用性。P10文字保留XLSX/PDF，但缺两项结构化材料，保守记需重大补正。逐例差异和严格汇总见COMPARISON.json。结构失败保留在分母。

本轮把null设计为“已有对应实体，由本机推导present”。模型反复把它当作“没有/不适用”，9份因此被拒绝；另1份取消关系引用未输出的旧任务。字段少了但状态含义更反直觉，职责调整失败。不能把null自动改为not_stated救分，也不能看完结果后改这批候选、组装器或参考。

两臂仍将“需要两个密封袋”多造为第三任务，袋子归属不对；05还漏登记单随样本提交的关系、两项表格格式材料和一项已取消旧要求。03在P11无依据写前置条件true，05为unknown有所改善，但不足抵消退化。两臂均保留模糊时间和明示具体时间，没有发现假日期；P09同日21:00仍有本机归一化和独立事件保存限制，不是模型未读到21:00。

03原严格任务19/19命中、多1；05只有2份可评分，其中任务1/1不代表12份100%。旧评分投影过滤部分不活动要求，分母不同于原始含作废项的21要求：03匹配21并多1，05匹配20、多1、漏1。时间、依据、枚举的严格差异与业务含义分开；旧评分未改，评分投影不冒充服务商原答。required没有被当成ready；结构化准备状态仍需现有用户核对，不能声称首次自动识别已经完成。

## 产品与分层检查

新响应已接入显式recorded-paired05入口，真实App复用原确认/隔离仓储。原答、组装、首次建议、编辑分别保留；回放绑定来源/请求/候选/响应，旧记录不覆盖、同臂重复幂等、另一臂拒绝同通知双份确认。正式确认仍需用户主动核对。自动选择NOT_ENABLED，不能据此证明模型安全。

5项新实际原答内存测试通过：P01-03两任务、P05-05条件成立、P03-03无日期，共4任务经工程人工核对后确认和独立new repository读回；另有坏身份/失败回答、重复/另一臂、事务失败后正常成功。它们不是原Edge库实测，不是真人操作时间。

官方控制发现Edge id2，但读取标签列表返回nodeRepl.fetch request failed。集中请求用户保持原标签前台，间隔后只读复核仍失败；没有循环导航、改防护/系统或更换数据库。原6631服务按最终代码启动，upstreamEnabled=false。缺P01-03和P05-05实际核对、确认、刷新及原13任务逐对象核验。新raw wire保存结构有变化，最终还需实际下载与独立原库全对象比较。浏览器、刷新、下载均NOT_RUN。

38候选/组装/wire、102预算/网关、5新原答确认测试通过；这些与全量重叠，不能相加充样本。全量首次1290通过、1失败、1既有跳过。唯一失败是编排漏传carrier路径，补齐非敏感参数后该1项通过，未改断言或旧夹具。最终同1292项为1291通过、0失败、1既有跳过。类型、lint、构建、Schema/时间契约、安全扫描通过。失败attempt保留，证据SHA见FINAL_CHECKS.json。

框架envFile=false且新env/cache/out，不读根.env，无新增依赖。944保护、522旧静态证据、53源码范围核验；18文件候选闭包派发后未变。旧117行88517字节前缀逐字不变，现166行账本、166收据一致、80次请求预留。日志仅追加，历史/冻结/旧候选/评分及全局组件未修改。

仅实施代理做新增转换/接入的聚焦复核，不称第三方独审；未发现绕过确认、重派发或半份事务。实际浏览器未验收仍是交付缺口。

## 费用与下一步

2026-09-12官方公开资料将deepseek-flash映射V4.1-Flash；24个原答均返回该别名。服务商未提供不可变权重身份。两臂都是新模型，历史别名成绩不能冒充本次旧模型对照。官方依据见PUBLIC_VERIFICATION.md。

本批usage按最高公开人民币价格结算上界0.508872元；历史已结算上界1.765809元，加A01永久未知预留3.30元，占用上界5.065809元，低于10元。账户实扣NOT_OBSERVABLE。历史累计80次，授权已全部消费，无额外试连接、retry、Repair或verifier。

当前唯一收尾动作：恢复官方Edge连接后，只补上述两条旅程和一次实际文件/独立读库，不重跑模型/OCR/时区或未改工程层。缺库不得创建替代库。后续识别优化应另行授权解决null状态编码歧义，不是继续加同义提示、放松校验或直接采用失败路线。

不接稳定或真实库，不部署，不采真实学生材料/真人研究。工程通过、候选退化、浏览器未验收三件事分别交付。
