# candidate08 独立实验审计

## 裁决

`PASS_WITH_WARNINGS`。没有发现阻止“candidate08 不采用、保持 candidate03 与 6632 现状、交付完整失败证据”的问题。candidate08 不具备产品接入条件，不能用其 3 份可评分结果掩盖 17 份转换失败。

## 独立复核通过项

- 20 份通知对应 40 个独立请求，40 份原始响应和 40 份结果齐全；17 个 candidate08 失败保留在 20 份分母中。
- 原账本 264 行前缀逐字一致，随后为 1 条授权、40 条预留和 40 条结算，哈希链与累计 168 次相符。
- 40 份 raw/result 文件哈希及评分依赖可独立复算；请求只含匿名文字、来源索引、参考时刻、时区和获准范围，参考事实未进入请求。
- 本批 182,242 输入 token、57,871 输出 token；按已固定公开价形成 1.067565 元费用上界。服务商实际账单仍为 `NOT_OBSERVABLE`。
- 未发现密钥、真实学生数据、生产接入、自动确认、Expected/评分器篡改或旧结果覆盖。
- 转换器对错误角色引用保持 fail-closed，未通过删除关系或补造事实制造通过。

## 警告

1. 审计过程中，同一分支由另一项已授权任务增加了两个 HTTPS 访问修复提交。旧 `IMPLEMENTATION_SNAPSHOT.json` 与 `CHECKS.json` 因此只代表开始 HEAD；本包用 `FINAL_IMPLEMENTATION_SNAPSHOT.json` 与 `FINAL_CHECKS.json` 纳入当前 HEAD 和受影响工程复验。
2. `evidenceRoleWire.ts` 当前没有验证 `parentTempId` 指向的目标是否存在。本批模型输出没有触发该缺口，且 candidate08 已决定不采用、未接入产品，所以不是本次“不采用”交付的阻断项；该实验协议不得直接复用为产品契约。
3. “先冻结 candidate08、再生成 8 份新材料”的顺序有本地文件与闭包证据，但没有独立可信时间戳。因此 8 份材料仍只能称“新材料首次开发验证”，不能称独立盲测。

## 结论边界

本审计支持提交 candidate08 实现、40 次真实响应、账本追加、比较报告及“不采用”结论；不支持启用 candidate08、不支持宣称首次准确率提高，也不支持运行以采用为前提的两条 6632 产品旅程。
