# 现有链路收尾：官方连接阻断记录

## 本轮实际完成

2026-09-13，基线本机与远端均为aa6f985042fbf3d2277fbcba251d969f4725bff1。55源码匹配原IMPLEMENTATION_SNAPSHOT；944保护、607旧静态证据、冻结依赖、215行完整账本和日志旧前缀无差异。21业务文件保持未提交，没有重新实现。

原6631起初ECONNREFUSED。使用现有serve脚本、原工程carrier清单及recorded-paired06配置恢复，输出mode=recorded_batch、upstreamEnabled=false。独立本机HTTP读取返回200且包含browser.js。没有读取密钥、创建数据库或发送模型。这证明服务器可提供入口，不证明浏览器已加载或库存在。当前启动会话不承诺在Codex退出后继续存活。

随后只进行一次官方浏览器恢复：cua.getState在约22秒后返回apps=[]、browsers=[]、nodeRepl.fetch request failed。失败发生在标签枚举/连接层，未取得测试标签，不能归因为真实App报错或已证实的Edge扩展拦截。本轮没有再次尝试Windows路径、刷新、切标签或降低安全设置。

## 验收结论

| 项目 | 本轮结论 |
|---|---|
| 原6631历史回放服务 | 启动成功；本机HTTP 200，模型上游关闭 |
| Q01-06/Q07-06真实页面核对及确认 | NOT_RUN：官方连接未取得标签 |
| 刷新后任务/日历/详情找回 | NOT_RUN |
| 原库独立new repository全对象读回 | NOT_RUN |
| 实际下载与独立读库比较 | NOT_RUN |
| 原已确认任务数量及不覆盖 | 本轮未访问原库，不能宣称当前已核实 |
| 工程 | 源码未变，复用原1310项（1309通过、1既有跳过）及工程证据；未重新执行 |
| 模型准确率 | 本轮未测量；没有新增请求，candidate06不采用结论不变 |

本轮只交付阻断证据和恢复说明，不提交未验收业务代码。未新增候选、数据、依赖或产品逻辑；未改原评分、回答、账本；不部署、不接稳定或真实用户库。

## 一次性最少人工交接（尚未执行）

不再要求循环重启或反复刷新。用户可以直接完成下列实际操作，由后续文件核验闭合证据。只使用原地址和原库，不加new=1：

http://127.0.0.1:6631/?run=real-input-d030c507-3f7c-4a2c-a511-8cd5bd534862

1. 先展开工程工具，点击“独立仓储读回全对象”，紧接着“下载本次工程证据JSON”，保留操作前文件。该按钮源码实际调用new CanonicalWorkspaceRepository(new IsolatedTestStore(name)).load()并进行联合校验；不是把页面显示值组装成存储对象。
2. 查看是否已有Q01-06和Q07-06草稿/确认记录。仅在未载入时点相应“载入Q01-06原回答（零调用）”或Q07-06按钮。若已有确认结果，不重复确认同一通知或另一候选臂。
3. 收件箱核对Q01两个任务各自的时间和材料；分别核对材料必需性与准备状态，明确保存，再主动选择、部分或批量确认。Q07只确认有效的新上传要求，旧寄送要求保留作废记录。无必要不改正确文字；若必须编辑，逐键输入并明确保存。
4. 刷新一次后到任务中心、日历、详情找到结果；记录错误或必要纠正，不把页面提示当存储证据。
5. 再点“独立仓储读回全对象”，紧接着下载工程证据JSON，保留操作后文件。提供两个实际文件或完整路径，并说明完成的步骤。任何错误只保留截图，不清库或请求模型。

文件核验时应记录独立仓储读回按钮的实际执行来源；仅文件内部自带sha或同文件重复散列不足以单独证明数据库一致性。缺少操作来源时明确缺口，不编造浏览器记录。通过后才提交21业务文件；当前阻断不是新增Prompt优化的理由。
