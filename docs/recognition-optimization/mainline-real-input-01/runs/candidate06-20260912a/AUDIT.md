# candidate06 同材料配对：不采用；模型比较与工程完成，真实浏览器未验收

## 结论

本轮完成一个更短的候选06、同期03/06共24次真实请求、逐例比较和零调用确认保存验证。**不采用candidate06，不再增加调用。** 它没有解决材料说明多拆任务的问题，反而增加材料对象和一条悬空取消引用。现有03默认路线不变；candidate05和factAssembly仍只作为历史实验保存。

这不是商业准确率验证。12份材料沿用上一轮已见通知，两臂同文字、时刻、时区、模型和参数，种子20260913平衡6/6先后；不是24份独立材料。原参考、评分器、回答、请求闭包和旧账本未改。

## 实际改动与检查

- 候选06沿用03完整wire，重新组织任务/材料/前置条件判定；没有新增wire或组装器。新增显式06身份、配对批次授权和零调用回放，旧默认保持。
- 55路径包含此前17个未提交实现；本轮完成后共21个业务文件仍保留未提交现场。浏览器必要证据不足，本次只交付审计、结果和账本，不包装为业务验收完成。
- 发送前107项预算/网关检查、11项候选检查和当前派发绑定检查通过；发送审核绑定55源码。全部调用后仅acceptance.test.tsx增加真实回答保存测试，未改候选、发送、解析或评分依赖。
- 最终全量Vitest：1310项，1309通过、1项既有跳过、0失败。定向7项包含3份实际Q回答和4项边界测试，已包含在全量，不能相加。
- 最终类型和受影响lint通过；完整lint 0错误、4项既有警告。构建、Schema/时间契约、安全检查已有日志按未变生产源码复用。根.env读取禁用，临时envDir/cache/build输出独立，无依赖变动。
- SEND_PREFLIGHT_ATTEMPT_1因启动未传已有carrier清单失败，补启动参数后ATTEMPT_2通过；保留失败日志，不计为产品反例。旧checker历史HEAD结论不改，本阶段用显式paired06保护核验。

## 新旧结果：原严格评分不改

| 指标，固定每臂12份 | candidate03 | candidate06 |
|---|---:|---:|
| HTTP成功 / 可解析 | 12/12 | 12/12 |
| 原严格整份正确 | 0/12 | 0/12 |
| 任务严格匹配 / 参考19 | 19 | 18 |
| 任务严格误项 / 漏项 | 1 / 0 | 1 / 1 |
| 材料严格匹配 / 参考17 | 10 | 10 |
| 材料严格误项 / 漏项 | 7 / 7 | 12 / 7 |
| 时间严格匹配 / 参考16 | 4 | 5 |
| 条件严格匹配 / 参考19 | 17 | 18 |
| 等待中位数 | 6.609秒 | 6.774秒 |

原始数字与费用见STRICT_COMPARISON_AND_LEDGER.json；结构与时间诊断见COMPOSER_DIAGNOSTIC.json。可解析不等于引用无误，更不等于语义正确或能够正确保存。

## 逐例业务裁决

沿用上一轮COMPARISON_RULES：改变动作、实际时间约束、材料义务/归属、条件或取消有效性才计重大语义纠正；纯措辞、等价ID和关系粒度单列。以下是代理开发裁决，不是独立盲审。raw事实、结构承接、人工保存分开。

| 通知 | 03与06实际差异 | 尚需操作/归类 |
|---|---|---|
| Q01 影展送审 | 两项任务、各自截止、三项材料及主要规格正确。06截图渠道为空，但同任务说明及授权书渠道仍保留授权区，没有交叉归属。 | 正常核对材料实际准备状态；截图渠道可补齐，非多造任务。 |
| Q02 近期整理 | 03有“近期”和“另行通知”两时间实体，第二个反向列表遗漏，并保留未解决来源；06只把“另行通知”建为时间，**“近期”仍在description但不在时间实体**。两者均normalized=null，不是假无日期。 | 06需将“近期”补入完整时间依据；03需核对完整引用及未解决来源。不能宣称06已解决模糊时间，结构错误减少不等于事实更完整。 |
| Q03 保存照片 | 两者动作/真正无日期正确；06额外把动作对象照片列为required材料，03无材料。 | 这是增加核对对象，原答未新增拍摄/提交义务；不能把粒度差异直接算新增任务。 |
| Q04 条件为假 | 两者保留红色预警条件false，独立检查正常；06额外采样瓶材料。 | 条件不退化；材料对象增加核对，无新增购买义务。 |
| Q05 条件成立 | 两者补交PDF、已成立条件及截止正确；03额外表示财务退回事件，06省去该背景事件实体，但条件事实保留。 | 正常核对准备状态；非任务遗漏。 |
| Q06 条件未知 | 两者领取条件unknown、学生证要求正确；06另造投影仪材料对象。 | 不可推定有现货或已准备好；多一项材料核对，未新增自行购买任务。 |
| Q07 新旧替代 | 两者旧寄送作废、新PDF上传生效、命名/渠道/截止及有效修订正确。 | 只确认新要求，旧项保留历史；实际内存确认已覆盖。 |
| Q08 取消与独立提交 | 两者取消参访且没有替代，DOCX反思独立有效，截止正确。 | 正常核对，不因取消连坐独立任务。 |
| Q09 纯信息 | 两者0任务、观星事件/开始/结束说法保留；结束仅21:00，本机未据开始日期补成同日日期。 | 共同的本机归一化/纯信息承接问题，不是06识别提升；不得制造任务或日期。 |
| Q10 各自附件 | 两者表格格式、两张照片/收据数量和归属正确；照片已完成与收据未知的依据仍在scope。 | 当前wire没有材料准备状态独立输出字段，依然要由用户核对保存。不能把Prompt强调状态当作新增可表达字段。 |
| Q11 送样 | **两者都多造“需要两个密封袋”任务**。06把袋子只归到该多余任务，漏掉实际提交任务关联；新增检测样本材料对象。03已有填写→提交依赖；06再标condition=unknown，按原口径不把03等价依赖判为条件成立错误。 | 两者删除多余任务；06另需修复袋子真实归属。严格任务漏项来自unknown导致评分行动投影不同，原回答并未漏掉提交动作。06填写时间type更合适，但不能抵消材料/任务问题。 |
| Q12 可选材料与取消 | 03保留取消旧发送任务，二维码required=false但反向列表少一项。06二维码列表完整，却**漏掉旧发送任务实体，取消仍指向不存在的task_send_source_files**；另加保存→制作依赖导致无日期coverage待核对。 | 06需补旧要求并修复修订、审查额外依赖；不能仅清空修订或全部拒绝救分。 |

按原答“无需重大语义改写”口径，03为11/12、06为10/12：03的Q11、06的Q11/Q12需要重大纠正。这个数**不是完整结构无错率或用户零操作率**；Q02的时间结构遗漏/反向引用、Q09本机时间及其他材料核对另列。按严格整份口径两臂仍均0/12，不能把11/12或10/12当最终识别转化率。

06材料总数17→22，新增5个对象出现在Q03/Q04/Q06/Q11/Q12；其中数量增加本身不全是多造义务，但没有减少用户核对负担。取消旧任务漏项及真实材料归属退化已足以否决采用，不需要追加调用追分。

## 确认保存：哪些真实验证过

- 使用Q01-06、Q07-06、Q03-06**实际已结算回答**，不改raw/首次建议、不调用模型，在MemoryWorkspaceRecordStore中走公开回放、材料准备状态的显式测试用户核对、事实核对、确认、独立new CanonicalWorkspaceRepository读回。
- 确认4个任务：Q01两项、Q07新要求一项（旧寄送未成为正式任务）、Q03无日期一项。回放/确认重复不增项；未核对确认拒绝；raw/first/修订保留；Q03 0时间、0提醒、实际jobs函数0作业。
- 材料missing是测试用户主动选择，不是模型准备状态预测；这3份使用独立内存库，不是原Edge库。不能证明原13任务本轮不变，也不替代真实逐键操作和刷新验收。
- 官方浏览器本次一次恢复仍在列出标签时报nodeRepl.fetch request failed（apps/browsers空），没有取得目标标签。此前官方Windows控制也因无法可靠判定URL而停止。没有绕过限制、关闭防护、读取密钥页或循环刷新。
- **两条真实Edge旅程、原库现状读回、实际新下载对独立读库：NOT_RUN。** 原13任务只是上次已验收数量，本轮未能读取核实。产品闭环尚未交付，不将连接故障写成产品故障。

## 请求、保护和Git边界

- 本批24次、历史104次已用完，0重试/Repair/verifier；本次恢复0新增请求。请求/返回别名deepseek-flash，无不可变权重版本声明。
- 本批usage按已核价格结算上界0.530872元，历史结算上界2.296681元；A01未知3.30元永久保持，总占用上界5.596681元≤10元。服务商实扣NOT_OBSERVABLE，不能将上界叫实扣。
- 原账本215行、215收据，全链及旧166行前缀核验；944保护、607静态证据、旧候选闭包、历史回答/评分保持。最新55源码逐字SHA见IMPLEMENTATION_SNAPSHOT.json。
- 基线本机/远端c419aea63a1f6595cb0140cde00eb89dcc092759；审计精确提交后核对远端。未验收的21业务文件保留未提交，禁止重复实现或回切。Git交付清单和最终保护见FINAL_CHECKS.json。

## 唯一下一动作

先恢复官方浏览器连接后，用现成响应完成两条原库确认/刷新/下载读回，收尾现有业务实现；**不再写candidate07或付费追分**。模型比较已给出不采用结论，浏览器缺证据不应引发新识别规则。之后若继续优化，须针对Q11任务/材料边界、Q12取消实体完整性和准备状态可表达性提出一个有证据的改动，而不是继续追加同义Prompt警告。

可复制提示词：

> 继续同一MAINLINE-REAL-INPUT-01，仅收尾candidate06已实现回放的原库验收与Git交付，0模型调用。以最新审计提交及candidate06-20260912a/FINAL_CHECKS、IMPLEMENTATION_SNAPSHOT核对55源码、944保护、607旧证据和215行原账本。保留21未提交业务文件，不重做计划、不修candidate06分数。官方浏览器连接恢复后，原6631/原run/原库选择Q01-06多任务和Q07-06替代要求各一次，核对保存、主动确认、刷新找回、独立仓储及实际下载；原任务不覆盖，同通知不确认两臂。无变化部分复用1310项工程结果；只有受影响部分重验。官方连接仍不能定位标签时停止UI重试，说明具体工具错误，不把手工页面提示当读库证据。通过后精确提交已有业务实现并推送核对远端，不增加候选、调用、数据或部署。
