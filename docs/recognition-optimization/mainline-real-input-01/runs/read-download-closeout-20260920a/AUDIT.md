# 当前真实保存闭环与双下载收口审计

日期：2026-09-20  
主线：MAINLINE-REAL-INPUT-01  
起点提交：`00330d91f53503a07b6adf3eb21238a3dc3792a9`
结论：**当前隔离库的持久化、独立只读读回和双路径实际下载证据闭环通过。**

## 本轮边界

- 本轮读取用户实际下载的两个 JSON，不再重复浏览器下载、连接诊断或旧回放。
- 产品源码、候选、Expected、评分器、模型原答和调用账本均未修改。
- 新增模型调用 0 次；累计请求仍为 290 次。
- 当前基线仍为 `deepseek-flash + candidate03 + reasoning=none`。
- 本轮只更新这一份主审计、短交接、追加日志和一份未派发的下一轮准备材料。

## 两个实际读取路径

### A. 完整测试库导出

- 文件：`mainline-real-input-01-workspace (11).json`
- 文件大小：393016 bytes
- 原始文件 SHA-256：`7EA80DA638C379A986F66077EFEE22AEC2B9212FCC74D883793040AB038D16CC`
- Schema：v8
- Workspace：`rco-mainline-01-02-i1-real-input-https-preview-1`
- `savedAt`：`2026-09-13T14:27:36.528Z`

### B. 独立 new repository 读回证据

- 文件：`real-input-local-evidence (14).json`
- 文件大小：307607 bytes
- 原始文件 SHA-256：`B49CA7D644CB7E7C58EF7D37F343893847CF4736946A869F4F17856620B1D452`
- 证据内 workspace SHA-256：`cc62dcd655603d2e7ff11b6b497d3da08899405bc7de982331362ed541a1a41a`
- `effects.writes=0`
- `effects.foreignDatabase=0`
- `effects.forbiddenNetwork=0`
- `effects.blockedDatabaseUpgrades=0`
- `realJobs=0`

两个原始文件哈希本来就应不同：A 是完整库对象，B 还包含读库来源、SHA 和副作用记录。比较的是 B 中的 `workspace` 与 A 的完整 workspace，而不是比较两个外层文件字节。

## 全工作区一致性

将 A 的完整 workspace 与 B 的 `workspace` 用同一深度 JSON 序列化后逐对象比较，结果为完全相等。业务数组计数如下：

| 对象 | 完整库 | 独立读回 | 结果 |
|---|---:|---:|---|
| tasks | 4 | 4 | 相等 |
| materials | 5 | 5 | 相等 |
| timePoints | 4 | 4 | 相等 |
| sources | 3 | 3 | 相等 |
| sourceVersions | 3 | 3 | 相等 |
| recognitionRuns | 3 | 3 | 相等 |
| extractionDrafts | 3 | 3 | 相等 |
| historyRecords | 16 | 16 | 相等 |
| 顶层 revisions | 0 | 0 | 两边均无该集合 |

这证明完整导出与独立只读仓储读回看到了同一个 canonical workspace；`writes=0` 证明独立读库没有为取得证据而写入或修改数据库。

## 当前正式保存结果

当前 canonical tasks 共 4 项：

1. `填写送样登记单`，时间 `2026年9月17日14:00前`。
2. `提交放映授权书`，时间 `2026年9月23日16:00前`。
3. `上传预告片`，时间 `2026年9月25日12:00前`。
4. `上传校样PDF`，时间 `2026年9月24日10:00前`。

当前 canonical materials 共 5 份，全部 `required=true` 且 `status=unverified`：

- 送样登记单；
- 放映授权书；
- 版权方邮件许可截图；
- 预告片；
- 校样PDF。

`unverified` 表示“材料准备情况尚未核实”，不等于缺少、不需要、正在准备或已备齐。确认任务也没有把它们自动改成已备齐。

三个来源分别为：

- `校园影展送审安排`：confirmed；
- `材料送样前的准备`：partially_confirmed；
- `校刊稿件交接方式修订`：partially_confirmed。

三份草稿和 16 条历史记录在两个读取路径中完全相等，原回答、首次建议、材料核对、任务核对与确认历史均被保留。

## 替代关系的准确边界

当前工作区**没有顶层 `revisions` 集合**，因此不能把草稿里的替代关系表述为已经物化的顶层 revision 实体。

真实情况是：

- `source:09fec1bb:draft:1:1` 的原回答草稿仍保存一条 `supersedes` 关系；
- 它由 `task-upload-proof-pdf` 指向被替代的 `task-old-mail-proof`；
- 相关草稿核对和处置历史仍保存该上下文；
- canonical tasks 中没有 `task-old-mail-proof`，所以旧寄送要求没有生成待办；
- 有效新要求 `上传校样PDF` 已成为正式任务。

这证明“旧要求没有进入待办”与“替代事实仍可追溯”，但不等于系统已经生成顶层 revision 实体。

## 读库和下载阻塞结论

先前临时无头会话的下载缺文件，已定位为自动化会话没有可观察的下载落盘策略；不是 Blob、导出内容或数据库损坏。独立读库永久 busy 在全新临时会话中没有复现，不能据此猜测性修改仓储。

本次用户当前库已经实际取得两个文件，因此此前仅针对“当前用户库”的 `NOT_RUN` 缺口关闭：

- 当前库独立 new repository 读回：PASS；
- 独立读库副作用为零：PASS；
- 完整测试库实际下载：PASS；
- 独立仓储工程证据实际下载：PASS；
- 两路径全工作区业务对象比较：PASS。

没有再次执行浏览器点击或刷新旅程；本轮按授权直接复用已通过的保存、刷新和交互工程证据，并用两个实际文件完成持久化与导出收口。

## 下一轮定点准备（0 调用）

唯一优先问题固定为：**先分清“需要执行的动作任务”与“材料、数量、格式、渠道等要求”，同时保证取消、替代和依赖只引用实际存在的目标。**

证据来自剩余错误：W11 把材料说明多拆为任务并错归材料，W12 为正确任务添加无依据依赖。此前更换模型、提高思考强度或增加结构复杂度都没有稳定净收益，因此下一轮不应继续盲目叠加模式。

`NEXT_SCOPE.json` 只准备 6 份新匿名通知和固定业务裁决，状态为 `PREPARED_NOT_DISPATCHED`：

- 没有创建新候选；
- 没有改 Expected 或原评分器；
- 没有进入 runner 或模型请求；
- 未来派发前仍须单独冻结候选、输入哈希、顺序、评价口径与预算授权。

未来实施所需最小职责范围仅为：一个新候选指令及其测试、现有 runner 的显式候选分流和测试、以及对本文件夹冻结材料的只读装载。全局 Schema、正式仓储、确认保存、时间 AST 和历史数据不需要修改。

预期直接减少三类用户操作：删除把材料说明多造出来的任务；补回遗漏或错归的材料关系；清理指向不存在目标的取消、替代或依赖。该预期尚未经过新模型比较，不能写成已实现收益。

## 最终结论

当前真实保存闭环在“正式对象已持久化、独立仓储只读读回、两个实际下载文件业务对象一致、旧要求未生成待办、原答和处置历史可追溯”这一交付口径下完整通过。

本轮没有提高模型首次识别准确率。下一轮只有在新候选和真正未见材料另行授权冻结后，才值得进行一次受控配对比较。
