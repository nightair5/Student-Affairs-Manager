# 独立读库与下载阻塞定位审计

日期：2026-09-20  
主线：MAINLINE-REAL-INPUT-01  
起点：`f74b6e7746df35ef8ce3508e720b570afb4d836f`

## 结论

本轮没有发现需要修改产品源码的稳定缺陷。

- 独立仓储读取在全新临时 Edge 配置和新建的 6632 隔离库中约 1 秒完成，联合校验、SHA 计算和工程证据链接均正常。上一轮临时会话中的永久 busy 现象本轮不能复现，因此其底层原因仍未确定，不能据此猜测性修改仓储或增加空库回退。
- 完整库和工程证据都能生成 Blob 下载链接。为无头 Edge 显式配置下载目录后，两个实际文件均成功落地；上轮“链接已生成但文件不存在”的直接差异是无头/CDP 会话没有可观察的下载落盘策略，而不是导出数据或链接本身损坏。
- 两个本轮实际下载文件中的 canonical workspace 完全一致，SHA-256 均为 `2d8a3754c9385e26b4682a701bec3fa3f69efa04514b8c650bf75eb7ff456b31`。
- 该验证使用全新空隔离库，仅证明读库和下载链路；它没有读取或代替用户当前浏览器的 6632 数据库，也不证明当前库中的任务处置已完成。

## 现场与保护

- 分支：`codex/e2-multimodal-recognition-exp`。
- 开始时本机、远端均为 `f74b6e7...`，工作区干净。
- 模型配置仍为 `deepseek-flash + candidate03 + reasoning=none`。
- 本轮新增模型调用 0 次；累计请求仍为 290 次，账本未改。
- 没有读取密钥、剪贴板、真实学生材料或用户浏览器个人资料。
- 没有清库、迁移、覆盖或创建用户当前库的替代库。
- 官方浏览器控制没有新的恢复证据，因此未再次连接、重装或诊断。

## 独立读库定位

实际路径为：

`IndexedDB open` → `readonly transaction/get` → `CanonicalWorkspaceRepository.load` → `validateSemanticWorkspace` → `JSON serialization` → 页面结果与下载链接。

本轮在全新临时 Edge profile 中：

1. 打开当前 6632 产物。
2. 用户式创建一个新的空隔离实验库。
3. 展开工程工具并触发“独立仓储读回全对象”。
4. 约 1011 ms 后按钮恢复可用，页面状态为操作完成，结果包含真实数据库名、workspace 和 SHA。
5. 没有发生外库访问、被阻止升级、旧 localStorage 访问或禁止网络请求。

因此可以确认当前代码在同类新会话中能够完整返回；不能确认上一轮 busy 究竟停在数据库打开、事务、序列化，还是自动化会话本身。按本轮“只修有证据原因”的规则，没有加入超时掩盖、静默重试、空库 fallback 或存储层改动。

## 下载定位与两个实际文件

完整导出先异步准备数据，再展示用户可点击的 `<a download>`；工程证据也使用相同的显式下载方式。该设计保留用户激活，不使用异步结束后的程序化 `link.click()`。

本轮给临时无头 Edge 明确设置下载目录后，实际取得：

- `mainline-real-input-01-workspace.json`：792 bytes。
- `real-input-local-evidence.json`：1211 bytes。

工程文件中的 `workspace` 与完整库文件逐对象相等；任务、材料、时间、来源、来源版本、识别运行、草稿、历史和修订计数均为 0，符合本轮新建空库事实。这个空库结果不能冒充用户当前库的业务对象证明。

## 验证

- 定向 Vitest：7 通过、79 跳过、0 失败。
- 覆盖显式下载链接、共享材料独立截止保存、来源到确认再到独立读回等既有路径。
- 测试使用 `scripts/mainline-01.vitest.config.mts`，`envDir:false`，不读取根 `.env`。
- 6632 首页：HTTP 200。
- `/api/status`：HTTP 200，列出 9 份受控历史回答，`modelCallsEnabled=false`，`candidateAdopted=false`。
- 模型识别 POST：HTTP 405，返回 `MODEL_AND_WRITES_DISABLED`。
- 产品源码和公开产物未变化，因此没有重复全量 type、lint、build，也没有重新构建第二份产物。

## 当前用户库状态

由于没有官方控制恢复证据，以下仍为 `NOT_RUN`：

- 当前用户 6632 库中的 L01-A、L02-A、L04-A 实际处置。
- 当前库刷新后的任务中心、日历和详情找回。
- 当前库独立 new repository 实读。
- 当前库工程证据 JSON 的实际下载。
- 当前库完整测试库 JSON 的实际下载。
- 当前库两个实际文件的业务对象比较与旧记录不覆盖证明。

上一轮已通过的 L04 关系编辑器真实组件事件链继续有效，但没有在本轮重复执行，也没有冒充用户当前库结果。

## 唯一剩余人工动作

在当前 6632 页面，只需先在“真实输入工程工具”点击“独立仓储读回全对象”，完成后点击“下载本次工程证据JSON”；再点击页面上方“准备完整测试库 JSON”，随后点击“下载已准备的完整测试库 JSON”。把两个实际文件交回即可核对当前库。若独立读库在当前浏览器再次长时间不返回，停止重复点击并保留页面状态；此时才有同一用户会话的可定位新证据。
