# MAINLINE-REAL-INPUT-01 — A02 source-label and confirmation delivery

Date: 2026-09-07. Scoped outcome: **A02_REPLAY_LOOP_PASS**. Entire MAINLINE-REAL-INPUT-01: **NOT_COMPLETE**. Model recognition accuracy: **本轮未测量**。

## What moved forward

The existing real A02 answer now reaches the actual App review panel with an honest source label. In original Edge/original database, material necessity and preparation state were reviewed separately, the title was edited key by key and explicitly saved, the task was actively selected and confirmed, and the saved task was recovered on refresh in the homepage/task center/calendar/details. Actual downloaded JSON equals an independent repository read. This establishes one real-response confirmation-and-save success, not model/general product accuracy.

## Scope and exact changes

Start HEAD/live remote: ef76d6e80b230398b30ef2a3b4cc490c96736b84. All 42 prior implementation hashes, 945 protected files, 160 old evidence records, CONTINUATION and RECOVERY records, ledger/five receipts and original log prefix matched. No reset/reinstall/repeated implementation.

Only three files changed this continuation:

- App.tsx: forward existing recognitionDescription only when runtime.realInput exists.
- DraftReviewPanel.tsx: optional source-description banner; without it, all old/default labels and capabilities unchanged.
- realInput01/acceptance.test.tsx: two new tests covering panel rendering and actual App conditional forwarding; old assertions preserved byte-for-byte.

Independent inverse-diff SHA verification proved these were the only increments and the other 39 files stayed identical. Existing fixes and all earlier 42 implementation files were reused, not reapplied. No original response, model identity, score, budget, global schema/repository or frozen component changed.

## Verification by layer

| Layer | Result |
| --- | --- |
| Nonsecret execution-isolation fixture | Positive control detects dummy temporary value; envFile:false negative control excludes it; actual root-env reads 0. Fresh temporary env/cache/build locations. |
| Before / after regression | 14 pass + 2 intended new failures before repair; final related tests 66/66. The former 64 tests remain. |
| Fresh independent code review | PASS for exact three-file increment; 39 other SHA unchanged. Reviewer did not run tests. |
| Current lint/type/function | lint 0 errors / 4 existing warnings; App and Node type checks PASS; functional 1233 pass / 1 skip / 0 fail. No timeout or assertion weakened. |
| Current contracts/core Node | Schema and time generated checks PASS; unchanged core Node suite freshly run 62/62. |
| Current applicable old-checker tests | Prepared-input identity and real local App/server tests 7/7, including five server subtests. Original test file and assertions not modified. |
| Historical paid-checker scope | Three tests bound to historical paid recovery HEAD excluded explicitly from the current invocation; NOT_RUN here, not relabelled PASS. No resetting HEAD, new grant, paid/recover or model call. |
| Fresh build/security | Explicit environment-disabled build PASS in new output directory; 14 stable JS bundles lack experimental markers; secret scan 1226 source/build files, 0 findings. Scanner predicates unchanged; only execution inventory uses NUL paths and this round's fresh build location. |
| SHA-based unchanged-layer reuse | Previous budget/gateway 83/83, historical 20 exact files + original library 4/4, dependency compatibility 13 and public dependency audit 0 vulnerabilities retained as previous evidence, not newly run counts. Their implementation/dependency inputs remain protected/unchanged. |
| Actual Edge | Correct label, existing-draft recovery, material review, per-key suffix edit/explicit save, active selection, confirmation, refresh/find and real JSON download PASS for one A02 task. Two injected transaction failures fully rolled back. |
| Actual file | 218242 bytes; existing v8 + joint semantic validator PASS; whole-object digest matches separately observed new repository read. Six original/first/input comparisons unchanged. |

Execution notes: one command-construction syntax error occurred before any command ran. An unnecessary comparison of unfiltered working-tree Git hash against a normalized Git blob failed because of line endings; the actual 945-file raw-byte SHA protection already matched and was rechecked unchanged. That inappropriate extra comparison was removed, not a protected-file mismatch waived. A large live-server bundle-input listing was truncated; the observed live bundle digest is recorded without claiming the missing inventory. These are orchestration observations, not model/semantic failures or extra samples.

## Browser result and limits

See LABEL_BROWSER_RESULT and LABEL_DOWNLOAD. Actual canonical result is one 保存活动手册-A02 Task, one material with explicit test-user observation required=true/status=ready, 0 time nodes, reminders and actual jobs. Correct confirmation was nonzero; selection was active, not automatic. The two simulated failures preserved the complete pre-operation database, not merely a page message. Replaying A02 after confirmation left the whole object unchanged.

The old task-detail history placeholder is not yet integrated with new semantic history; the correctly populated semantic history is separately visible and exported. Keep this as a nonblocking legacy-display gap for a later explicitly scoped UI consistency change, not as evidence of lost edits or completed formal-task editing. A02's one-task browser path is not batch/cross-tab, Chinese IME or all-format acceptance. No real human editing time is reported.

## Delivery and remaining mainline

The user authorized the complete 42-file business snapshot to be submitted once this scoped replay acceptance passes. This delivery is that isolated implementation milestone, not a declaration that all original package calls/formats or commercial gates passed. Eligible staging is exactly the 42 source paths from LABEL_IMPLEMENTATION_SNAPSHOT plus this continuation's LABEL reports, short CURRENT_CONTEXT and appended log. Historical reports and failures remain unchanged. Final Git/remote IDs are verified after commit, not invented inside this report.

A01 retains permanent failure/one count/3300000 microyuan unknown reserve; A02 retains its consumed grant and 16392 microyuan upper-bound settlement. No new request, receipt, fee, credential/clipboard access, real-user database, stable connection or deployment. All previous model scores remain unchanged; this round did not measure model recognition accuracy.

## Unique next mainline recommendation

Next, validate a small remaining batch of already-prepared anonymous notices through this now-working confirmation loop, concentrating on missing/wrong tasks and time/material/condition/revision ownership. Do not keep polishing A02 or create a new semantic dataset. New model requests still need explicit request identities, safe append-only ledger authorization and stopping rules; their semantic outcome must be measured separately from the green engineering checks.

Suggested next prompt (proposal only, not active authorization):

> 继续同一MAINLINE-REAL-INPUT-01，复用本轮已交付的原App确认保存链路，不重做PLAN或重测A02。先核实际Git/远端、LABEL快照、945保护和A01/A02账本；只读给出原已准备A03–A08六单元的最小追加调用及回放范围、精确脚本职责、同一候选/输入/评分绑定、参数/次数/金额与失败停止条件，供一次性批准。不得重发A01/A02或释放未知预留，不新建数据集。批准后在一个工作包内完成小批真实识别、逐例语义裁决、实际确认保存验证和Git交付；普通范围内缺陷连续处理，不把HTTP/JSON/工程通过当正确率，不自动调verifier/Repair/retry或部署。未新增调用授权前保持0模型请求。
