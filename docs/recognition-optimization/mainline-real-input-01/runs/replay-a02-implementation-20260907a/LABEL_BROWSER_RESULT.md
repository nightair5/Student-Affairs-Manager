# A02 original-database real App acceptance

Status: PASS for the authorized A02 replay / review / atomic confirmation / retrieval / actual export journey. This does not mean the entire real-input package, unseen model quality, all-format browser matrix or commercialization is complete.

## Environment and actual user operations

Official browser control used original Edge browser 2, tab 763114724, origin http://127.0.0.1:6631, run real-input-d030c507-3f7c-4a2c-a511-8cd5bd534862. Original database `rco-mainline-01-02-i1-real-input-d030c507-3f7c-4a2c-a511-8cd5bd534862` was not replaced, cleared or initialized. No `new=1`; no browser/system setting changes. Recorded-only local server was rebuilt from the reviewed current sources. See LABEL_RECORDED_BUNDLE and LABEL_IMPLEMENTATION_SNAPSHOT.

1. Refreshed the original page. Existing A02 pending draft was found through the real Inbox, without replaying the model or creating a Source. The actual dialog correctly displayed “A02历史真实模型响应回放 · 本轮零调用”. Old human-engineering label no longer appeared in the dialog header.
2. Opened material review. Requirement and availability initially required separate active choices; no model-derived missing/ready state was silently accepted. For this anonymous engineering operation, explicitly selected “需要” and “已具备”. These are test-user observations, not a new claim that the source proves availability.
3. Before saving, instantiated an independent repository through the existing engineering UI. Complete workspace SHA: `9c26d8881ba1719f4c4b0899ebaf9536a8e12d2b0bc47cf0d13329278acc2961`, formal Tasks 0.
4. Injected the supported next-transaction failure, then clicked the real “保存材料核对”. It reported INJECTED_ATOMIC_FAILURE, retained unsaved choices and disabled task confirmation. Independently read the whole workspace again: byte-serialized object exactly equal to the pre-save object. No history or formal entity was partially written.
5. Clicked the same material save normally; it succeeded. Opened the actual task-title editor. Appended `-`, `A`, `0`, `2` as four separate keyboard actions, observing each intermediate value: 保存活动手册- / 保存活动手册-A / 保存活动手册-A0 / 保存活动手册-A02. No fill was used. An initial attempt to enter a Chinese parenthesis through press was rejected by the automation key API without changing the input; this run validates the observed ASCII suffix keystrokes, not Chinese IME acceptance.
6. Before explicit title save the page reported unsaved changes and blocked “加入任务”. Clicked “保存修改：任务名称”, then “本项事实已核对”. Neither action auto-selected the task. Actively checked “选择该任务”; the deadline remained blank and correctly showed 原文未说明截止时间.
7. Independent pre-confirmation workspace SHA: `9f36fb6832077a0b5f95c4838b7b0eee26737d71cf7668f24ec9a946c6a18b49`, Tasks 0, history records 4. Injected another transaction failure and clicked “加入任务”. The App explicitly retained the unconfirmed suggestion and saved edits. Independent whole-object comparison was equal to the pre-confirmation object, with Tasks 0 and the same four history records.
8. Clicked “加入任务” again after the simulated failure was consumed. Exactly one canonical Task was created: 保存活动手册-A02. One related Material was saved as required=true, status=ready, explicitly marked `requirementAndAvailabilityOrigin=user_observation`. There were 0 timePoints, 0 reminderRecords and 0 jobs from the actual jobs function. History count became 5; one confirm operation only.
9. Refreshed the page. Found the task on the actual homepage and task center. Opened real task details: action/object, original notice including no-deadline sentence, first response and the new semantic operation history were available. On the actual calendar the task appeared in the separate “无截止日期任务” list; month cells and upcoming dated list did not acquire a fake date.
10. Clicked the real App “导出完整测试库 JSON”. A new actual file appeared in Downloads: mainline-real-input-01-workspace.json, 218242 bytes. No prior file with that name was present in the pre-download inventory. Independently instantiated the repository again after download and compared its full object with the confirmed object; unchanged. File-side v8 and joint semantic validators passed and its full-object SHA matched that independently observed repository digest. See LABEL_DOWNLOAD.
11. Clicked the existing recorded-A02 replay again after confirmation. Independently read the repository: entire object unchanged, still exactly one Task. This is replay idempotence, not a new model request or an artificially forced duplicate confirmation.

## Evidence origin and invariants

Full objects were captured in the same official CUA REPL as labelBefore, labelAfterFailure, labelBeforeConfirm, labelConfirmFailure, labelConfirmed, labelFinalRead and labelAfterDuplicate. Every independent read came from the real engineering action `new CanonicalWorkspaceRepository(new IsolatedTestStore(name)).load()` and joint validation, not DOM claims about saved status. The final full-object digest was observed before reading the downloaded file.

- Final repository / parsed-file JSON.stringify SHA: `cf3aa420daee3f6c66a2d206440e8f5ef67fff92c5955f8f5aecefcdb1e38770`.
- Before-versus-final comparison: rawHttpText, rawResponse, first, inputReceipt, sendSnapshot and all SourceVersions unchanged (6/6 exact comparisons).
- Context authority remained live_model_candidate; original A02 identity and old scoring were not relabelled human_engineering.
- Operations: prior enable_material_review, review_material, title edit, review_task, confirm. Failed transactions contributed no successful history.
- Nonzero success: one actual confirmed Task. Observed premature formal writes, duplicate Tasks, overwritten original evidence, fake time/reminder/jobs: 0.
- Automatic model selection remains NOT_ENABLED. This is not a measured accuracy rate for default selection.
- Read instrumentation was reset by refresh; final effects.writes=0 means no writes since that refresh, not zero writes for the entire journey. The two intended failures and the successful writes are reported above.

## Remaining limits

A02 contains one task, so this browser journey cannot establish multi-task batch/partial confirmation or cross-tab expiry. Existing anonymous normal controls cover related semantics in memory; those are not substituted for new browser measurements. No new paid call, new semantic dataset, real-user material or human-time study occurred. Chinese IME, all-format reading and the full commercial browser matrix remain outside this evidence.

The existing read-only task-detail component still shows its legacy-history empty placeholder beneath the correctly displayed semantic edit history. Its old estimated-time projection also remains a compatibility estimate. These are retained UI integration gaps, not lost canonical edits; the true semantic history is visible above and present in the download. This turn does not claim that legacy panel, formal editing/execution, ICS or real reminders have been integrated. No out-of-scope source was changed to conceal that limitation.
