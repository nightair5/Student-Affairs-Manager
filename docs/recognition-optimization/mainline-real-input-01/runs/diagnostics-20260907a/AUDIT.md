# MAINLINE-REAL-INPUT-01：零调用失败诊断补充

## 本次解决什么

截图显示的原库初始化失败已经在上一轮修复；本轮没有重复修改入口、清库或换库。当前主线障碍是首次A01没有取得完整模型响应，而旧网关把所有异常归为同一个安全错误，无法知道失败在哪一步。

本轮在原42路径中只修改3个脚本：gateway增加固定的失败阶段/原因，gateway Node测试增加正常与异常对照，runner为未来结果保留安全诊断。没有修改读取、语义、确认、预算规则、模型参数或任何旧结果。原浏览器错误body保持不变，诊断是服务端附加元信息。

可区分配置/预留、连接/DNS/TLS、已观察的HTTP状态、协议、响应读取/解码/JSON、凭据反射检查、原文写盘与结算。未知仍报未知；不复制异常message/stack、头部、正文或密钥。局部总超时与传输库自己的超时分开。迟到响应不能改写已返回诊断；失败仍保留全额未知预留并拒绝继续。

`uncertaintyAttempt`只表示失败后调用uncertain的结果，不表示整个账本是否安全。结算校验失败可能已先写入RESPONSE_OR_USAGE_INVALID且租约已用过，随后uncertain会拒绝；测试仍必须证明pending预留、持久HALT及下一请求拒绝。不能据FAILED一词推断“账本没有记录”。

## 实际验证和边界

- 修前：gateway旧24项通过、新19项失败，复现缺少诊断；不是19次真实网络失败。
- 第一次修后：预算+gateway共72通过/1失败。新测试误认为已结束租约可以再次uncertain；查真实只读接口后修正新测试假设并明确字段名，没有改变预算策略或旧断言。
- 最终：73/73（原54项+新19项）；各attempt不累加成样本。覆盖正常结算、缺配置、DNS/连接/TLS/协议/JSON/反射/落盘/结算故障、恶意getter、迟到响应、重开及下一请求拒绝。
- 原gateway测试在内存仅移除新增区块并恢复插入边界后，逐字SHA仍为ae1dd78231be79f752011888a53dbf5c7fcd7d50c8278a35f71f4d3f38ac2b21；未删除或弱化旧测试。
- 独立审查与适用检查最终结论见REVIEW及ENGINEERING。整包完整工程、最终真实模型确认/浏览器下游仍未完成；不把本轮范围通过说成产品交付。
- 独立复核PASS_SCOPE_ONLY，73/73复跑及2项额外模拟检查分列。安全扫描1130文件通过。新增运行完整lint发现2错误/4警告：App.tsx:1490在render中传递被判定为ref相关的回调，FactCorrectionEditor.tsx:17在render中更新ref。两文件本轮SHA未变；没有隐藏错误、关闭规则或修改UI。整包工程仍未通过，后续原白名单内工程需处理；本轮零调用诊断局部通过不覆盖此FAIL。

## 本机连接线索

当前Node v24.18.0 / undici7.28.0；HTTP_PROXY、HTTPS_PROXY、ALL_PROXY、NO_PROXY存在，Windows用户代理启用；当前Node未设置NODE_USE_ENV_PROXY、NODE_OPTIONS或--use-env-proxy。Node本机help说明--use-env-proxy用于把这些代理环境变量应用到全局HTTP/HTTPS客户端。

这是当前运行配置的线索，**不是A01历史故障的已证实原因**。没有读取.env、代理地址或凭据，没有修改系统/浏览器/Node代理设置，没有网络试连接，不能据此断言代理可达、路由可信、Key无效或上游未扣费。

## 模型、预算与历史证据

本轮0新增模型请求、0新增费用、0重试。总历史仍为1次派发尝试、0完整可评分响应；另外15单元NOT_RUN。原A01的502为本机网关状态，不是已观测上游502。3.30元是未知最坏预留，实际消费NOT_OBSERVABLE。**模型识别准确率：本轮未测量。**

原STATE、manifest、CALL_LEDGER、RAW_RESULTS、RESULT和上一run全部41文件按SHA只读保留。没有回填新诊断解释A01、没有创建新付费账本、没有释放预留。新源码使原发送审核SHA不再匹配，旧发送批准不可复用，也没有修改checker的旧HEAD绕过检查。

945保护、70旧静态证据及日志前缀按原逐字规则核验。42实现仍留本机未提交，只有本轮明确列出的审计文件、短交接和追加日志进入Git。失败证据审计交付不等于业务整包通过；无真实用户库/稳定入口/部署。

## 唯一下一主线建议

先验证Node实际连接路线，再恢复有限真实识别。不要换模型、新建数据或重发A01；历史原因不可恢复时诚实保留未知，用新受控运行观察真正故障阶段。

建议下一授权提示词（本报告不自动授权）：

> 继续同一MAINLINE-REAL-INPUT-01，保留现有42路径与全部历史。先核最新审计Git/远端、diagnostics快照和原A01账本。允许进行最多2次不带API Key、不发送正文、不调用模型的定向连接诊断，仅限api.deepseek.com的TLS连接，对照当前Node直连与经核验可信的现有代理路线；不关闭证书验证、不改变系统或浏览器设置、不读取/输出代理凭据。缺少可信路线或需要扩大范围立即停。证据足够后，提出原账本追加式恢复的精确规则及一次未运行单元A02方案，保留A01失败、已消耗次数和3.30元未知预留，24次/10元总上限不变；未经新的恢复批准不得发A02，更不得重发A01。范围内零调用工程继续完成，不重做PLAN、不新数据/依赖、不接稳定入口或真实库、不部署；审计与Git精确交付。

为何现在做：入口和输入绑定已有工程证据，目前缺的是可核验连接及合法停发恢复。继续堆识别规则不能解决这个障碍；诊断通过也不等于模型已经接通。
