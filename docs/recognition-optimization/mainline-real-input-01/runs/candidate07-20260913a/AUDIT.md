# candidate07：任务与材料边界配对验证

状态：配对比较完成；candidate07不采用。工程与HTTPS发布通过；真实浏览器确认/刷新/读库/下载未验证，不能称整包完成。起点8319bc6129c2893cb5bf8ba0877900afc3e713b5，本机及远端一致。原三份HTTPS业务修改保留。

## 派发前固定的改动与判断口径

candidate07替换整段指令，按“义务边界→附属要求归属→执行语义→引用覆盖”填写既有wire。与candidate06不同，强调不要先逐句枚举任务再补附属信息；通过一组抽象对照区分材料说明和明确制作义务。不生成中间推理结构，不加关键词黑名单，不改解析、全局Schema或评分。

12份原candidate05材料，candidate03/candidate07同模型同文字同参考时刻，种子20260914，平衡两臂先后。新身份R01…R12；不覆盖旧Q/P/N/A/B/C/D。原参考事实与COMPARISON_RULES复用。失败仍留分母；严格评分和业务实质差异分开。任务材料目标错误减少、无需重大纠正增加且其他关键事实不退化才考虑采用；否则不采用，不加调用。

真实回放代表性选择规则在看结果前确定：优先R11材料归属及R12取消；若其无法进入现有核对链，报告阻断，不将其排除成绩。为验证可用产品链路，可另用编号最前且符合多任务或修订类别的可接入响应，明确标为补充对照。只确认一臂。模型按钮始终关闭，无恢复消息不尝试浏览器。

## 初始验证

58原源码SHA与944保护/607历史静态证据匹配；原账本215行、104请求未变；BASELINE记录日志全前缀。新增两候选文件纳入60实现路径（原55实验路径加3HTTPS新增路径及本次2文件）。

candidate07工程响应11项通过；它们证明现有接口承接，不证明模型正确。复用candidate06工程正反形式，新版本请求检查绑定candidate07。
首次Node预算测试因未设置REAL_INPUT_TEST_TEMP在夹具创建前拒绝，0账本/网络影响；随后使用新临时根执行，不修改旧测试断言。

## 24次实际比较与采用结论

2026-09-13，同模型deepseek-flash，实际返回亦为deepseek-flash；服务商未给不可变版本号。24/24均取得有效usage并结算，0传输失败，0重试/Repair/verifier。原104请求及失败保持，累计128。模型发送文字、候选、评分依赖由BINDING_FINAL/SEND_REVIEW绑定；首次派发后候选与请求闭包未再改动。

| 指标（各臂12份） | candidate03 | candidate07 |
|---|---:|---:|
| 严格任务正确/参考 | 19/19 | 18/19 |
| 严格多余/遗漏任务实体 | 1/0 | 0/1 |
| 严格材料正确/参考 | 10/17 | 10/17 |
| 严格材料FP/FN | 6/7 | 11/7 |
| 严格整份全部匹配 | 0/12 | 0/12 |
| 人工业务裁决：无需重大语义改写 | 10/12 | 9/12 |
| 加上结构/转换阻碍后无需重大修正 | 9/12 | 7/12 |
| Schema解析成功 | 12/12 | 12/12 |

完整逐对事实、分层判断及全部原严格指标见COMPARISON.json，原RESULT不改。后三个业务数字不是点击率、保存率或盲测准确率；正常主动核对和准备状态未知的公共限制单列，不冒充零操作。

- 唯一明确目标改善：R11不再多造“准备两个密封袋”任务；登记单前置依赖保留，密封袋能归提交任务。03还错误填条件true；07采用依赖表达，没有宣称登记单已完成。
- 未形成净改善：R03/R04/R12将操作对象额外列为材料，增加准备核对；R12缺取消旧实体，悬空修订及无日期却time=present仍在。07的R04还出现not_applicable带事实列表矛盾，R05将准备状态未说明留unresolved，形成额外结构阻碍。
- 等价差异不罚成任务错误：R01截图渠道null但归属/任务渠道明确；R07旧纸质材料仅属已作废任务；R11样本包装细分及反向袋关联由现有factAssets承接，没说它们都是新材料遗漏。
- R02的“近期”在07描述中而非时间实体，不谎称完全漏时间或无日期；R09两臂原始21:00都存在，本机归一化未承接同一天，属于共同转换/事件承接限制，不是候选07改善。
- 原wire不能表达准备状态；P10照片已完成仍需人工核对。缺少“尚未核实”处置选项是现有产品限制，本轮没猜missing来通过。

结论NOT_ADOPT：目标只改善一例，通知级无需重大纠正没有增加，材料/取消关系出现退化。当前默认路线不变，不继续加候选或调用。下一轮依据应是任务对象/附带材料的可表达边界，而非再次堆同义警告；本轮不实施下一轮。

## 费用与等待

官方峰值人民币价格已在PUBLIC_VERIFICATION/BILLING核验一次：未命中输入2元/百万、输出8元/百万；按完整合法usage计算保守上界，忽略缓存优惠，不冒充服务商账单。3.30元预留覆盖新模型上下文最坏2.162688元。

- 本批费用上界0.533736元（03:0.261536，07:0.272200）；实际扣费NOT_OBSERVABLE。
- 全账本费用上界6.130417元，其中A01永久未知预留3.30元；已结算请求上界合计2.830417元，未突破10元。
- 账本215→264行；原163134字节前缀保持；128请求中127结算+A01未知，新增未知0。尾hash见COMPARISON/CHECKS。
- 请求等待累计163.406秒，03平均6.682秒，07平均6.936秒。不是整体工程工时，也不是用户主动编辑时间；真人省时NOT_RUN。

## 接线修复与真实交付边界

在既有实验runtime/semanticRepository/semanticState中增加07身份，不改全局仓储。R11可通过真实App入口载入。R12坏引用仍被原完成函数拒绝；本轮仅允许该已绑定07原答以终态failed原子追加，保存原始失败正文，不改为succeeded、不派发、不自动打开纠错。用户可主动调用已有“保留失败原答，进入人工纠错”。旧版本限制与确认规则不变。

最小公开接口检查：R11仅在模拟用户明确选择材料“正在准备”、事实核对后确认首项；其截止9月17日14:00及材料/原答/first独立new repository内存读回一致，重复不增。R12失败与原答保留，主动进入纠错后坏引用/时间覆盖仍拒绝直接确认。额外原子失败反例、来源/请求错配、旧记录全对象不变及旧版本正常对照通过。以上全是工程内存证据，不是用户真的选了“正在准备”，不证明HTTPS库已经新增任务。

HTTPS仅更新同一个独立Worker，保留Q01/Q07并追加R11/R12；入口“核对送样与共享材料”“核对展签与取消要求”。未采用的候选与错误原因明确标注，模型发送关闭、无Secret/数据库/生产路由绑定，仅ASSETS。

- 固定地址：https://student-affairs-real-input-preview.nightsdell.workers.dev/
- 版本e07c1a4a-97a8-4c6e-85c5-3fdd10b06cce，2026-09-13T08:07Z部署。
- 初次普通HTTP仍返回旧内容，保留online.json失败；版本标记状态确认新版。以no-cache/no-store及identity编码重新验证，7/7实际资产SHA匹配，状态四示例/模型关闭；online-final.json。未修改系统/浏览器设置、未重复部署。
- 最初按原要求不重连，集中交接一次人工步骤。随后用户明确要求“在Codex浏览器里面自己打开网页操作然后下载”，仅尝试一次官方createBrowserTab("iab", 固定HTTPS地址, {visible:true})。30.0494秒后工具返回“js execution timed out; kernel reset, rerun your request”。没有取得页面状态，未发起核对/确认/下载，也未循环重连或改权限。此错误只能证明控制调用超时，不能证明网站不可用或定位为扩展故障。
- 未收到真实文件；R11/R12确认、刷新、独立IndexedDB、实际下载全部NOT_VERIFIED，HTTPS原任务数量未观测。没有把HTTP资产一致、内存测试或页面截图当成真实存储证据。
- 不改旧库/生产站，不创建第二份同通知两臂任务。不重复OCR/时区；模型0默认选择。

## 工程、保护与Git

候选11、预算62、网关50、实际输入绑定1通过。全Vitest初跑1326通过/1失败/1跳过：唯一失败为旧载体启动测试缺显式manifest；配置补齐后原测试通过，未改断言。新回放最初暴露07身份未登记、R12坏引用不能进入回放；按上述终态失败归档修复，不删除坏引用或弱化正常确认校验。最终受影响7文件121项通过，含全部62 acceptance；旧未变层复用初跑证据，不将多次运行相加。

最终类型通过；lint0错误/旧FastRefresh警告保留；全Vite安全构建及最终独立HTTPS esbuild通过。Secret scan1784、契约与时间AST生成检查通过；全部框架运行config:false/envFile:false、新envDir/cacheDir/临时输出，只有批准的派发进程读服务端.env。Cloudflare从临时配置工作目录运行且禁用dotenv和遥测。

聚焦复核仅新增派发和终态失败接入：整数预算/原前缀/重复身份不可释放、原答绑定、用户明确确认、旧对象保持、事务失败不落半份；见SEND_REVIEW、预算/网关/回放反例。为实施者聚焦复核，不冒充独立盲审。旧开发依赖2moderate/3high历史风险保留，未升级、不宣称全依赖审计通过。

最终944保护/607旧静态/18冻结依赖及旧日志账本前缀保持；源码与Git换行存储SHA分开见IMPLEMENTATION_SNAPSHOT。实际路径60=原58+候选07两文件，不扩大产品文件或依赖。

按用户“未验收业务改动保留现场”：提交已完成候选/派发比较的9源码及必要报告/原账本追加；HTTPS实际验收未得证据的7源码保留未提交（含原3文件及本轮4个接入/发布文件）。发布状态与未验收状态明确分开，不包装业务闭环完成。精确清单、提交与远端回执见DELIVERY.json；不强推、不自动变基。完成已授权模型比较后停止，无后续调用授权。
