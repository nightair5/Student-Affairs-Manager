# MAINLINE-REAL-INPUT-01 独立范围复核

日期：2026-09-06。结论：**PASS_SCOPE_REVIEW**，仅表示方案边界可供批准，不是产品、模型或预算执行门通过。

独立审查对象为最终三份方案及其直接依赖源码；未采用历史测试成绩代替本轮验证。唯一仓库 `C:\Users\Winner\student-affairs-multimodal-exp`，分支 `codex/e2-multimodal-recognition-exp`，现场 HEAD `0eedbf7fc1523d6be476c704ed02b273bdb513bf`。当前实施与付费授权均未生效。

## 范围判断

41 个路径互不重复，其中 13 个已有文件的现场 SHA 全部与白名单一致，28 个新增路径均尚不存在。按当前设计，白名单能够承接真实 App → 本机读取/OCR → 校对与发送快照 → 真实模型来源 → 用户纠错确认 → 隔离保存和独立读回；未发现必须增加第 42 路径或修改受保护公共文件的证据。这个判断不是实现可运行的证明。

| 核验点 | 可定位依据与判断 |
|---|---|
| 新来源与多个人工限制 | `semanticComposer.ts:9/36/183`、`semanticCapture.ts:18–39`、`semanticState.ts:75–79/233–255`、`mainline02/runtime.ts:84` 的身份、选择、版本与库名限制分别由 E05–E09/E12 覆盖。新 provider 使用已有 `deepseek` 枚举，显式新 state/profile 保留人工分支；不能只删除一个限制或伪称人工来源。 |
| source-only 与版本链 | `domain/v2/capture.ts:147/196/219/345` 已有保存来源、修订与本地建 Run 的方法。PLAN §4.1 现明确：未改内容复用版本，正文更正创建版本，仅首尾空白变化保存前拒绝并保留缓冲；不把方法名 beginRetry 当网络重试许可。 |
| 写入前校验 | 旧 `semanticRepository.ts:41–42` 是 capture 写入后再 load，`semanticState.ts:229` 仅逐 draft 遍历。E07/E08 与 PLAN §4.1 已要求新 profile 在独立内存 store 复用 capture 构造，遍历全部 Source/Version 并联合校验，再真实事务 CAS；source-only 不得漏检。 |
| 历史草稿与当前来源 | 旧 `semanticState.ts:233–234/270–273` 把各草稿绑定当前来源版本/状态。方案现明确历史草稿核验自身版本，当前状态取当前版本最新 Run，旧未确认明确过期，旧确认与历史保留。`App.tsx:185–195/505–521` 已有历史原文读取保护；方案要求复用，不将其误报为既有错引故障。 |
| 事实纠错的全部消费者 | E07/E10/E11/E12/E13 与 N06 覆盖 life、canAct、时间编辑支持、canonical 重建、信息处置、联合校验、草稿/日期/优先级、事件数量与完整事实显示。PLAN §4.4 已要求统一 effectiveFacts，人工补充独立标 manual，raw/first 保持不可变；不能只改显示值或伪造原文依据。 |
| 公共接口可保持只读 | `domain/v2/domainCommit.ts:793–834` 已接收完整 nextDraft 与 canonical 计划并保护已确认实体；v8 provider/legacyData/引用支持现有形状。新 profile 的状态与来源约束可放入白名单实验层，当前没有必须改变全局 Schema、capture、repository、validator、confirmationV2 或时间 AST 的证据。 |
| 输入与真实页面 | E01–E04、N01/N02/N07–N10/N19/N20/N28 覆盖完整读取凭据、取消/恢复、页内混合检测、资源本机化、真实 App 插槽及格式载体。U01–U16 要求实际 OCR、真实 Edge、失败回滚、下游查找与真实下载后独立读库；mock/DOM 提示不能替代这些门。 |
| 默认未选不是质量通过 | PLAN §4.3 将 live 初始不默认选择明确列为待批准策略；自动选择能力记 NOT_ENABLED，错误候选仍计分，要求非零真实正确确认、条件真正例、新要求及独立兄弟成功，不能靠全拒绝/全未知通过。 |

## 已解决的方案阻断

本次先反馈后复核的阻断已闭合：未改文字与 beginRevision 的冲突；历史草稿与当前版本/来源状态的冲突；source-only 漏检及直接写后才联合校验。纠错消费者和人工补充来源也已明确。当前未决的**方案范围阻断为 0**。

## 执行前条件与非阻断风险

- **付费执行：PAID_BLOCKED，直至计费上界证据补齐。** A8+B8、可选新候选 C8 共用持久账本且最多 24 次，单并发，传输 retry/Repair/verifier 为 0；浏览器和 runner 不各自领取预算。24 次按每次 0.40 元不可逆预留为 9.60 元。文档正确将 100,000 输入 token 上界及价格条件标为未证明，不能用 65,536 UTF-8 字节直接证明计费上界；缺证据时首个付费请求前必须阻断。公开价格/参数仅核对方案的引用和条件设计，本审查未联网复核，账号/Secret/余额未检查。
- **实现风险仍须实测。** 新 profile 的历史版本并存、同版本新 Run、旧核对失效、重复确认、失败前后全对象不变与共享事实保护是 U05/U07/U08/U10/U12 的必测点；不能以版本分支存在或全部拒绝代替正例。既有全图校验严格，任何无法在白名单内表达的冲突须报告，不能弱化公共校验。
- **提取和格式风险仍须实测。** 既有 `fileExtraction.ts:218–228/298–365` 尚非本方案本机资源与页内混合策略；真实 OCR、超限/坏页/空页终态以及 raw/display/edit/send 映射均未运行。PLAN §3 与 U01/U03/U04 已明确：文件本体不持久化，关闭/刷新只能恢复已保存文字、页状态和核对记录；未完成 OCR 须主动重选并核验同一文件 hash/范围，尚无 hash 则作为新读取操作，不得无文件自动续读或将未处理页恢复为成功。这一恢复边界须做正反例验证。仅首尾空白修改不支持已公开为保存前限制，不计成功。
- **效用范围有限。** 旧 8 来源及其工程 PNG/JPEG/WebP/PDF 是已见 Development；A/B/C 不能当作 24 个独立样本，C 不覆盖首次结果、不冒称盲测。首次质量与用户修正后确认分表；真人时间、未见泛化、商业门、稳定入口和部署均未通过且未获本包授权。

## 三份被审方案的完整 SHA-256

| 文件 | SHA-256 |
|---|---|
| `PLAN.md` | `b2c06b066cbb1e33bbdd86f56d2cae9df4e6ebe972da8654d84b0549c2208635` |
| `IMPLEMENTATION_WHITELIST.json` | `3bdd211da5adda56bc38fe9dd4b48be5c2b8c1a682f37d2404cb7dd7a156aff1` |
| `VALIDATION_AND_BUDGET.md` | `a81afc9a1946550a64d0a82bbc43365e319489a28f0c87f3396b12e6e5fa4a1b` |

上述任一文件改变后，本报告不能自动沿用为新版本的独立复核。

2026-09-06增量复核：仅亲读 PLAN §3 与 VALIDATION U01/U03/U04 新增恢复边界并更新上述 SHA；未重审源码或历史。补充与不持久化文件本体的既定职责一致，未扩大 41 路径或授权，PASS_SCOPE_REVIEW 结论不变。

本审查仅新增本文件；产品改动 0、工程/浏览器/OCR运行 0、网络/模型调用 0、费用 0、密钥/剪贴板/真实库访问 0、数据创建与部署 0、Git 写入 0。模型识别准确率：本轮未测量。
