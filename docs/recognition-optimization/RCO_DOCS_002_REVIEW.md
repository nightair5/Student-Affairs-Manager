# RCO-DOCS-002 文档交付检查

日期：2026-09-05；范围：文档重整，不是产品/模型/浏览器验收。

## 交付内容

- 根AGENTS保留原安全与发布边界，补充完整通知主线、语义/本机职责、反弃答刷分和短上下文流程。
- PRD更新为v0.9文档版，保留历史阶段与商业门槛，新增12项功能清单、事实分工、交互状态、测量及待决策事项。
- 总计划重写当前RCO-5三个工作包，明确先零调用承接正确响应，再验证候选表达和付费模型净收益；之后才是模态消融、真人与发布。
- CODEX_PROMPTS提供下一步授权模板、长期目标和压缩恢复模板；CURRENT_CONTEXT为短交接；日志只追加第67条及后续记录。
- 不另建冲突的agent.md，不增加另一份PRD权威副本。历史总计划/提示词版本可从Git回看，不覆盖冻结实验计划或结果。

## 无上下文读者测试

方法：使用doc-coauthoring技能，由一个不继承聊天的辅助读者读取当前文档，回答8项问题并检查矛盾；属于同系列开发审查，不是独立人工语义真值。

| 问题 | 实际回答与判定 |
|---|---|
| 当前授权与下一步 | 正确区分文档授权与MAINLINE-01待授权 |
| 零调用证明什么 | 正确解释为工程上限，不能称模型准确率或G5通过 |
| 可修改范围与冻结缺口 | 正确限定新增隔离产物/测试存储，遇保护边界停止提案 |
| 时间材料/无日期/unknown/单项隔离 | 正确区分不造日期、不转false、单项与根完整性失败 |
| 弃答/Expected/B9 | 正确要求覆盖率及全部分母，禁止决策链读Expected和旧runner重跑 |
| 付费/数据/真人/部署 | 正确识别各自授权与晋级依赖 |
| 优先格式与商业矩阵 | 正确理解为开发排序，不削减原商业门槛 |
| 上下文恢复 | 正确使用最新日志/现场Git，不从RCO-0重启 |

首次读者发现1处矛盾：PRD14.6旧句仍把逐字证据列作模型自由输出，与新引用式边界冲突。
已改为“受控来源引用由模型选择，逐字证据由本机按已验证scope还原”，不改冻结契约。
修正复核：辅助读者确认该阻断已解除，8问通过，无剩余阻断。判定仅为文档可理解性通过。

## 验证与保护范围

- 初轮文档自动检查34/34：编码、围栏、相对链接、12项功能ID、三个工作包、短交接限制、旧日志内容前缀、PRD商业阈值表；加入本报告后的最终计数见机器记录。
- 644个原tracked文件中，六个可修改文档之外的638个文件逐个SHA-256相同；包含历史Expected/freeze/dataset/checkpoint、代码、商业验证契约及runner/result。未对Git忽略缓存做全盘哈希；本轮未执行写入缓存的模型或工程runner。
- 旧日志按换行规范化后内容前缀保持原样；不修订旧状态快照，采用追加更正和最新记录优先。
- diff空白检查通过；安全扫描通过。最终机器记录见RCO_DOCS_002_CHECKS.json，以最后验证更新为准。
- 本轮未重跑lint/test/build、依赖审计、Cloudflare检查或899项代码测试；这是文档阶段，不能把前轮成绩登记为本轮执行。CI在推送后按仓库配置运行，结果须另行核验。

## 结论边界与交付

- 识别模型调用0、外部模型请求0、Secret访问NONE、产品实验费用0 CNY；开发协作Codex用量不冒充识别实验账单。
- 不修改产品代码/稳定路由，不创建B10，不运行旧B9 runner，不启动RCO-6，不部署。
- 新语义契约、计时、跨块处理、无日期确认等是待实施需求，不能称已修复。
- 文档读者检查不能证明商业正确率、真人省时、线上可用或Production获批。
- 计划提交：`docs(product): refocus recognition roadmap on complete task outcomes`；实际提交/推送以Git和交付答复为准，不预报成功。
- 下一步只建议另行授权RCO-5-MAINLINE-01，文档交付后停止，不执行模板。
