# 下一轮唯一建议提示词（待用户明确授权）

本轮MAINLINE-03-SCOPE仅完成范围文档，以下文字不是现有执行许可。

```text
授权执行 RCO-5-MAINLINE-03-I1：按已提交mainline-03-scope/IMPLEMENTATION_WHITELIST.md和VALIDATION_DESIGN.md，仅实施零调用的来源绑定与可表达性接入验证。

唯一仓库C:\Users\Winner\student-affairs-multimodal-exp，分支codex/e2-multimodal-recognition-exp。先核对SCOPE最终提交/远端、BASELINE/CHECKS/REVIEW、CURRENT_CONTEXT及最新日志，R1原19实现和保护文件应与SCOPE基线一致。不得回切235e365或R2，不套旧FAILED补丁；有重叠修改/保护变化停止。

已有文件只允许src/experiments/mainline02/runtime.ts增加可选隔离handoff与来源凭据/提示，src/App.tsx仅runtime分支来源提示接线；默认路径与旧人工入口不变。
新增只限白名单6个mainline03源码/测试、2脚本及新mainline-03-i1报告，CURRENT_CONTEXT约80行，日志仅追加。其余源码、Schema、repository、capture、confirmationV2、domainCommit、validator、时间AST、冻结候选只读。

先用旧完整工程响应的服务形状内存变形复现pending-source/无位置无法进入V2。新适配仅将唯一逐字证据绑定本次CaptureHandle的来源ID和位置；已有错误来源/位置、重复证据不得猜测修正。除来源/位置外全结构保真，不改任务、时间、材料、selected，不新增语义规则。
原文、原始回放响应、适配结果、首次建议、真实编辑分别保留。允许在新Source的legacyData.mainline03Handoff内保存获准匿名receipt，经现有capture写入，不改Schema或仓储。
B8-01/07/09仅已见只读候选诊断，不能强转完整2.0；无等价条件/修订/三值/事件承接时显式阻断并留原始响应。不能将空引用当原文无信息，不能把unknown变false，也不能全拒绝过门。

复用真实App+已验证runtime/V2，新空隔离v8库；先存来源，再适配，实际逐键编辑/保存/部分及批量确认/刷新/导出文件与canonical读回。
旧42字段口径新路径须42/42，并用差异账本检查其外字段；旧40/42、17测试和历史FAIL不改。通过现行完整校验且仅单项依据位置待核对的响应，有效兄弟项仍须能单独确认；悬空引用等Schema/reference/semantic校验失败，整个capture失败，只保留Source receipt与失败Run/Draft，不能伪造有效draft.result。这一冻结契约局部承接限制不计产品通过，不改校验器救验收。0错默认选、未确认正式写入、重复、覆盖、丢失。
同根因最多两轮局部修补。定向/对抗通过后新无上下文独立审查，无阻断才一次完整适用工程门和实际Edge浏览器验收。保护变化、Expected进决策链、错误默认选、测试弱化、稳定默认变化、真实库访问、范围扩大或两轮无效立即停止，失败只提交审计证据并保留源码现场。

外部识别模型/verifier/Repair/retry/模型网络/费用/密钥/剪贴板访问均0；不创建数据集/盲测/B10、不运行旧一次性runner、不改Expected/freeze/dataset/checkpoint/cache/历史结果。不接稳定入口、不部署/RCO-6、不采真实材料/真人。正式任务编辑/执行、ICS/真实提醒不在范围。
成功单独提交推送并核对远端，交付审计、真实链路覆盖/字段和安全数字、保护状态、缺口、下一步理由与授权提示词。模型识别准确率写“本轮未测量”。完成后停止，不自动补新语义契约或付费。
```

为什么先做这个：模型即使给对，未绑定的依据也会让当前V2挡住；来源接线属于确定性工程，不需要买模型调用。
为什么不直接把010候选接上：其表达尚不足以保留完整时间材料/条件修订；本包先把事实交接和能力缺口显出来，后续才能只修有证据的主瓶颈。
付费方案见VALIDATION_DESIGN第4节，当前NOT_READY且未授权。
