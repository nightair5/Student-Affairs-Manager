# D6录制结果工程回放

日期：2026-09-22。入口：`http://127.0.0.1:6634/?automation=1`。数据库：`rco-mainline-01-02-i1-real-input-candidate13-d6-engineering-1`。

这是24份已结算模型回答的只读载入工具；新模型调用关闭，界面明确显示“工程回放 / 录制结果 / 非真人试用”。原6633入口及其数据库没有读取或修改。

## 已通过

- 24份录制结果可按冻结身份选择；原始响应hash与请求身份验证后才载入。
- 来源原文、scope依据、首次建议、用户编辑和操作历史分开保留。
- 浏览器实际完成双任务案例：编辑第一项标题和日期、核对并选择第一项、拒绝第二项、只保存第一项。
- 注入下一次原子写入失败后，页面显示`D6_INJECTED_ATOMIC_FAILURE`，正式任务没有增加，待确认草稿仍保留；显式重试后成功。
- 独立repository读回与浏览器刷新后，已保存任务仍存在；控制台无error或warning。
- 新模型请求、OCR、旧库访问、localStorage、外部网络、WebSocket和Beacon均被独立入口阻断。

## 未通过的产品边界

S09 Candidate13首次输出正确识别为0任务，没有误建正式任务；但回答保留了“周三晚维护”的事件/时间信息。当前确认界面因此报告`TIME_NEEDS_REVIEW`和`EVENT_NOT_TASK_ASSOCIATED`，并禁用“标记已核对（不创建任务）”。这说明零任务识别本身正确，但用户仍不能完成正确无任务处置。该项不能计作正确处置率。

## 指标边界

以上回放只验证工程链路和失败恢复。自动化操作时间、自动化保存次数以及数据库事件均不计入正确处置率、低修改正确处置率或主动修改时间；三项继续为`NOT_OBSERVABLE`。
