# RCO-5-007 本机任务形成与安全决策计划

## 1. 授权与边界

- authorization: `RCO-5-007`
- status: `AUTHORIZED_ZERO_CALL`
- 输入只读：B1 已有冻结数据、scopeIndex 与 `20260903b/raw-results.json`。
- 输出只新增：独立契约、实现、测试、回放预测、评分结果和报告。
- 模型调用、网络请求、Repair、retry：全部为 `0`。
- 禁止修改既有 `Expected`、freeze、dataset、checkpoint、cache；禁止接稳定路径、启动 RCO-6 或部署。

通用实验输出模板在本机缺失，本轮沿用项目已有的“计划—跟踪表—组件冻结—追加日志”体系。

## 2. 第一性原理问题

产品真正需要的不是“模型能填多少字段”，而是：从原文形成少而正确、可追溯、不会误执行的待确认任务。B1 暴露的主要失败属于决策口径不统一，而非单纯漏字：同一句复合动作被拆碎、否定状态漂移、命令时态漂移、说明文字归错任务，以及评分器依赖没有完整哈希绑定。

因此本轮把职责重新切开：

1. 模型仅提供 scope 引用、动作/对象表面文字与候选引用，不再拥有 `selected`、`requiresAction` 和最终语义决定权。
2. 本机确定性规则统一任务合并、否定/条件/修订状态、命令时态、说明归属与安全默认。
3. 只有本机验证通过的明确当前任务才可成为默认勾选建议；仍然只是待确认建议。

## 3. 固定规则

- 复合动作：同一 scope、同一对象链、无独立截止/接收方/外发效果时合成一个任务；首个业务动作是主动作，其余为步骤。不同 scope 默认不合并。
- 否定状态：`不要/不得/禁止/无需/不用/不再/暂勿/先不要/不要求/不强制` 统一为当前不可执行，`polarity=negative`、`status=cancelled`、`selected=false`。
- 条件命令：`若/如果/如/当…时` 在无触发证据时为 `hypothetical + uncertain + unknown`，不计入 `requiresAction`。
- 命令时态：面向收件人的待办命令统一为 `future`；描述已发生事实为 `past`；纯说明为 `present`。
- 修订关系：旧要求保留但标为 `superseded/cancelled`；新要求单独形成当前任务。
- 说明归属：紧邻任务且含“该/上述/两项/本次”等回指的时间、材料、地点或限制，附着到对应任务；不得独立冒充任务。
- 默认勾选：只允许 `explicit + addressee + directive + affirmative + future + pending + active + required`，且效果仅为本地改变或实体动作；外发、付款、联系、条件、否定、已完成、已作废和不确定项一律不默认。

## 4. 声明、反声明与证据

| ID | 声明 | 证据 | 反声明/停止条件 |
|---|---|---|---|
| C1 | 本机层能消除 B1 中已知的口径漂移 | 契约测试、属性变形测试、12 例只读回放 | 任一规则依赖 Expected 文本或 case ID 特判即失败 |
| C2 | 模型不再决定是否形成正式建议或默认勾选 | 类型、运行时校验、输出 provenance | 输出仍接受模型 `selected/requiresAction/semantics` 即失败 |
| C3 | 安全指标不退化 | Forbidden Default=0；不确定项 fail-closed | 任一禁止项默认勾选即失败 |
| C4 | 评分结果可追溯 | scorer 及传递依赖哈希清单、运行前校验 | 未绑定依赖或输入哈希变化即停止 |

回放是 `SEEN_DIAGNOSTIC_REPLAY`，只能证明实现是否按固定规则工作，不能证明对新材料的泛化能力，不能作为上线依据。

## 5. 执行顺序

1. M0：冻结本计划与语义政策；记录受保护文件哈希。
2. M1：实现独立的模型输入缩减、本机任务形成、安全决策和严格校验。
3. M2：运行契约、属性变形和新鲜对抗测试；失败则停止。
4. M3：创建组件冻结清单，哈希绑定 runner、scorer 及其传递依赖。
5. M4：只读回放 B1 既有 candidate；预测器不加载 Expected。
6. M5：评分器单独加载冻结 Expected，生成逐例结果与审查报告。
7. M6：跑全量 lint/test/build、安全扫描与冻结复核，更新短上下文和追加日志。

## 6. 验收线

- 模型/网络/密钥访问均为 0。
- 12/12 回放输出通过新契约；模型权威字段计数为 0。
- 所有固定规则、变形测试和恶意输入测试通过。
- Forbidden Default 必须为 0；否则结论为 `STOP`。
- scorer 和所有参与评分的本地依赖均有 SHA-256，运行前后匹配。
- 受保护文件 SHA-256 前后完全一致。
- 无论分数是否提高，结论最多为“可申请新的未见数据验证”，不得接稳定路径、不得启动 RCO-6、不得部署。

