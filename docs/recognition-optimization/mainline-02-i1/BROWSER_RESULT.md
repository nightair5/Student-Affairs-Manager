# 真实App浏览器记录 — NOT ACCEPTED

本轮使用官方Computer Use的Codex内置浏览器（不是Chrome/Edge/手机全面验收）。服务由新serve-mainline-02-i1.mjs以内存构建、127.0.0.1:12998启动，CSP connect-src none。只有旧人工响应、故障工具和新库是工程替身；首页/统一录入/App、V2面板、Capture、确认器、canonical仓储与IndexedDB为实际实现。

## 运行身份

- 标签8：run=d56c2a71-66ee-48ed-b229-cdbf0a08c61c；库rco-mainline-01-02-i1-d56c2a71-66ee-48ed-b229-cdbf0a08c61c。
- 标签9：run=i1-1788613315368-qwlt09ae2ym；库rco-mainline-01-02-i1-i1-1788613315368-qwlt09ae2ym。
- 都是先建立全新空v8，来源/任务0；没有预置已确认任务。当前网页地址不是Production/Preview。
- 每次刷新移除new=1后沿同run读取；最后同库完整JSON内容与刷新前相同。
- 所有数字来自工具UI操作与工程证据按钮的真实canonical读回，不是toast成功计数。
- 库属于该localhost端口的浏览器origin；后续重开旧库须保留同origin，不删除旧库来解决缺库。

## 实际链路与读回

1. 标签8：首页输入旧multi，来源/版本/run/draft各1、正式任务0；关闭→收件箱→刷新→重开。
2. 标题和时间用pressSequentially逐字符输入，未使用fill冒充编辑。未保存时第一项确认和批量确认均禁用；退出后读回0任务/0编辑历史，原文和首次响应相同。
3. 再逐字录入“提交活动报名表（复核）”和2026-09-12T09:30，分别点击明确保存；刷新后恢复。正式任务仍未创建，编辑历史2。
4. 第一项双击“加入任务”：只创建1任务。读回标题为用户修改值、normalizedValue=2026-09-12T09:30、timezone=Asia/Shanghai；rawText仍2026年9月10日18:00前，首次响应未覆写。
5. 收件箱剩余1项→面板确认剩余项：2任务/2时间；第一任务对象全等，刷新完整canonical全等。已确认项不再有“加入任务”按钮。
6. 标签9：真实统一Intake录入旧no-date。文件、链接、手动任务禁用；V2显示可无日期确认。
7. 在该未确认草稿设置下一笔事务故障→主动确认→INJECTED_ATOMIC_FAILURE。读回与故障前完整canonical全等、0任务；没有自动重试。再次由UI主动确认后1任务/0时间/0提醒。
8. 任务中心搜索“保存活动手册”命中1；日历独立“无截止日期任务”列表可打开只读详情。日期格/即将到来不出现该任务；首页无缺日期异常理由。详情编辑/完成/提醒/ICS禁用；实际buildBrowserReminderJobs返回0。
9. 通过同真实首页新增未编辑旧multi及condition-true，两草稿由真实Inbox选中批量。归档先试，App拒绝且0归档；再批量确认，总任务从1到4、时间2、提醒0，旧无日期任务全等。
10. 该新multi按旧42字段口径，从UI实际canonical JSON读取后评分42/42：动作/对象/标题/描述/完成标准、时间原文/值/时区/全天/精度及归属、材料格式/命名/数量/渠道及关联、证据来源与引文。评分只在读回之后，不进入产品决策。
11. 同库录入vague/information/condition-false/revision：各自任务checkbox错误默认勾选均0；没有新增正式任务。condition-unknown回调失败，来源/失败草稿保留。点击“本地规则重试”被App拒绝，run不增加。
12. 标签9最终8来源/8版本/8run/8草稿、4任务/2时间/0提醒，1个预期失败run；刷新后完整canonical相同，无日期任务保留且相关时间0。
13. Sidebar“项目”点击后App提示拒绝，日历仍挂载；不称该按钮已disabled。
14. 两个页面观察到的数据库open均仅其指定新库；recordReads/recordWrites目标仅current。localStorage/fetch/XHR/WebSocket/通知权限/通知发送尝试均0。最后刷新epoch：read3、write0；未发生旧投影自动保存。计数是调用尝试记录，不能将故障事务尝试计为成功写入。
15. 控制台末次warn/error记录为空；这不代表全部浏览器组合验收。

## 下载阻碍（不得计PASS）

- 标签8已确认2任务、标签9已确认无日期任务均从App真实“导出完整测试库 JSON”按钮尝试。
- 下载监听先注册再点击/Enter，各等待10秒，两次waitForEvent(download)超时。之后单独Enter诊断，不以重复尝试覆盖失败。
- 狭窄只读检查Downloads/mainline-02-i1-workspace*.json无文件；不能证明其他未授权目录中绝对不存在文件。
- 标签9计数诊断：导出前current读取18；导出后再按一次证据读取，计数21（增3=导出load、perform刷新、证据读取）。导出按钮已恢复enabled，无JS对话框与控制台错误。
- 因而可证明导出处理器进入本机读取链，不能证明浏览器生成了可下载文件。静态尚未确定根因：未挂DOM的anchor、立即revokeObjectURL是待验证时序风险；不能臆断是IAB限制，也不能臆称导出成功。
- 官方tab.content.export另返回“不支持tab_content_export”；这仅是辅助证据页导出能力缺失，不是App JSON失败根因。未绕过接口用隐藏浏览器状态/自建协议取文件。
- 不把runtime.exportJson单元通过或DOM canonical文本当作App下载成功。

## A–K覆盖与剩余

| 组 | 本轮实际证据与未测 |
| --- | --- |
| A | 新库/副作用实际检查及缺库坏记录定向通过；普通默认仅代码/SSR/回归检查，不打开用户旧库 |
| B | 首页、统一录入、来源先存、人工响应失败保留均实测 |
| C | 关闭、Inbox、刷新恢复及多草稿批量实测 |
| D | 两字段逐键/未保存/明确保存/刷新实测 |
| E | 默认选择正反、文件与详情禁用、归档/导航/重试App拒绝实测；planned_start/event/关系变形为定向测试 |
| F | 逐项、双击、剩余项、Inbox双项/跨草稿、42/42实测；面板直接双项一次性批量尚未单独实测 |
| G | 实际确认故障整笔回滚实测；保存故障/已保存后确认故障/跨标签过期仅定向测试，仍需浏览器补验 |
| H | 真无截止1任务/0时间/0提醒、任务中心/日历/详情与真实jobs0实测 |
| I | 实验date-only与planned_start代码/SSR通过；date-only输入到日历的真实UI补验尚未完成 |
| J | 提醒作业及禁用边界实测；App JSON实际下载/文件读回未通过验证 |
| K | 等待其他操作后刷新、完整canonical一致实测；未做跨浏览器组合 |

没有11/11或完成声明。错误默认选择、未确认正式写入、双击重复、已确认覆盖、已观察关键丢失为0；仅限本报告已跑场景。真实材料、真人操作时间、模型准确率及商业成功率未测。

## 收尾

本轮创建的两个测试标签8/9已关闭，身份核对后的本轮node启动器PID46028已停止，12998端口无监听。两套隔离IndexedDB与新临时证据保留，未删除缓存或数据库。未继续执行下一授权包。
