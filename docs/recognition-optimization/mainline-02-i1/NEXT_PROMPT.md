# 唯一下一实施包（待授权，不自动执行）

授权执行 RCO-5-MAINLINE-02-I1-R1：恢复当前本机未提交I1实现，仅诊断并闭合JSON下载与已登记浏览器验收缺口；不重做PLAN、不新建数据集、不调模型。

唯一仓库C:\Users\Winner\student-affairs-multimodal-exp；分支codex/e2-multimodal-recognition-exp。先核对Git/远端、mainline-02-i1/BASELINE.json、REJECTED_SNAPSHOT.json、AUDIT.md、BROWSER_RESULT.md、FINAL_CHECKS.json、CURRENT_CONTEXT及最新日志。19实现文件必须与拒收快照一致；有重叠用户修改或保护变化立即停止，不回切1657fb9或R2，不套旧FAILED补丁。

继续沿原SCOPE IMPLEMENTATION_WHITELIST与VALIDATION_DESIGN。已有实现只在原19源码/脚本内；本轮允许优先修改App.tsx的显式实验导出处理器、mainline02/browser.tsx的工程工具可操作性、mainlineAcceptance.test.tsx及两个隔离脚本。其他代码只读；若新根因需要超出这些精确职责，先申请。禁止修改Schema/repository/legacyView/confirmationV2/domainCommit/时间AST或任何旧夹具/Expected/freeze/dataset/checkpoint/cache/runner/result。

1. 先明确选择已安装Edge的新本机标签与全新隔离v8库；不访问已有API/用户标签、密钥或真实库。复用真实App，不另建下载玩具页，不预置已确认任务。
2. 使用旧人工工程通知实际录入→确认→导出，监听下载并获得真实文件路径，解析该JSON，与同库canonical、schema v8、原文/首次响应/编辑历史及无日期0时间0提醒比较。文件缺失不得称PASS。
3. 先核对下载监听、UI动作/读库、链接生命周期和浏览器支持。若只是IAB观察能力问题，不强改产品；若确认导出实现根因，再先补复现、做至多两轮局部修补。不能用内部exportJson或页面JSON文本代替实际下载验收。
4. 补完登记的实际浏览器项目：面板两项一次批量、保存失败回滚、已保存后确认失败保留历史、跨标签过期、date-only逐键补日期到日历显示。普通逐字编辑不能fill代替；现有部分/Inbox批量/42字段/无日期证据可只读复用，但修改影响到的链路必须重验。
5. 保留8类旧响应、42字段口径、新42/42、旧40/42/17测试和历史FAIL。错误默认勾选、未确认正式写入、重复、覆盖、丢失仍须0；不得全部拒绝过门。
6. 新无上下文独立审查→适用工程检查（产品源码若修改须新一轮完整门）→保护→实际文件/存储读回。无阻断才单独提交推送业务实现并核对远端；失败只提交审计，保持未提交源码。
7. 本机端口与测试库绑定须明确。旧I1 origin为http://127.0.0.1:12998；不要用换端口/清库掩盖旧库缺失。只新建获准测试库，保留旧库证据。

硬边界：外部识别模型/verifier/Repair/retry/模型网络/费用0，不访问密钥/剪贴板，不接稳定入口、不部署/RCO-6，不新盲测/B10，不采真人/真实材料。新错误默认选、保护变化、测试弱化、Expected污染、稳定默认改变、真实库访问、范围扩大或两轮无效立即停。

CURRENT_CONTEXT约80行；长日志留新文件，不反复读历史。最终交付改动、已验收与未验收数字、保护、Git、唯一下一建议/理由。模型准确率写“本轮未测量”。完成后停止。
