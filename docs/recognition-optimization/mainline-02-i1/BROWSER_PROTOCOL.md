# MAINLINE-02-I1 实际App验收执行表

状态：NOT RUN。须独立审查无阻断、全量工程门通过后执行。使用官方浏览器控制，仅本机回环新库；不打开生产/Preview，不运行旧runner，不用一次性fill冒充逐键编辑。

## 证据读法

启动脚本打印唯一随机run的新地址，挂载前创建全新空v8。刷新地址自动移除new=1，复用该库，缺库立即失败。页面上方工程工具只提供旧通知文本、下一事务故障与真实canonical读回，不替代App输入或确认。仅从真实App录入，不预置正式任务。

读回按钮实际调用runtime.load/真实CanonicalWorkspaceRepository/IsolatedTestStore；展开JSON后保存读取内容和计数，不能只记录toast。副作用计数显示从启动起的数据库open、current读/写目标、localStorage、fetch/XHR/WebSocket、通知权限/发送尝试。数据库不枚举、不删除；不访问其他库。CSP是第二层禁网，不是“没有尝试”的证据。

## 操作序列

1. 新入口空库：0来源/草稿/任务，真实首页无演示任务、不检查服务；记录数据库名和0副作用。
2. 首页输入旧multi文字→生成工程建议→关闭→收件箱→刷新→打开；原文/首次响应与正式任务0读回。
3. V2逐键修改标题/支持的时间；未保存确认受阻、历史未变；明确保存后读回编辑历史，刷新仍显示。原文和首次响应不变。
4. 部分确认→读回1→重复操作不增→剩余确认2→刷新仍2；第一项不覆盖。另一个新库原multi不编辑双项确认后，采用旧42字段口径读回42/42。
5. 统一文字录入旧no-date→确认；读回1任务、0时间/0提醒；在任务中心搜索、首页、日历独立列表与只读详情找到，不出现假日期/假提醒。
6. 收件箱多草稿选中批量确认，核对各自来源/ID归属与canonical任务数；不走旧选择投影。
7. 原vague/condition-false/revision/information响应不默认确认；unknown回调失败保留来源和失败草稿；不得称为模型准确率。
8. 白名单组件未支持操作预禁用；Inbox归档和Sidebar非核心导航点击后明确拒绝，快照不变、未挂载其他页。详情正式编辑/执行/ICS/提醒不承诺支持。
9. 故障工具→保存失败全回滚；独立已成功保存→故障确认回滚且保留已保存历史；恢复正常再确认。多标签过期/重复确认明确结果。
10. App真实JSON导出，读取下载内容并和同库canonical比较；无日期任务0时间/0提醒，真实jobs函数为0。保持自动保存等待期后刷新复查。

## 报告边界

每项记录实际浏览器、动作、持久化读取与结果。定向测试中的planned_start/event/稀疏ID/关系变形不是本机浏览器新人工材料，更不是模型预测。A–K按实际证据分别标识，不能从测试总数自动判11/11。任何用户硬停止条件触发立即留失败现场。
