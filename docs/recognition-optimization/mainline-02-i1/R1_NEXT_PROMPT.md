# 唯一下一建议：识别输出接线范围审计（待授权）

原因：人工工程响应已能在真实App隔离链路中确认、恢复和备份，下一瓶颈应回到“真实识别能否交出完整、可承接的建议”。不能把客户端工程通过当模型准确，也不应再为相同人工场景反复造数据。先用已有结果只读查清实际接口与信息缺口，再申请一个有明确输入/模型/次数/金额/停止条件的端到端实验。

可复制授权草案：

授权执行 RCO-5-MAINLINE-03-SCOPE：仅只读追踪当前识别服务/已冻结候选输出到已通过MAINLINE-02隔离确认V2的字段与引用交接，建立唯一最小实施白名单和验收设计；不修改产品代码，不新增识别规则，不新建数据集，不调用模型，不部署。

唯一仓库C:\Users\Winner\student-affairs-multimodal-exp，分支codex/e2-multimodal-recognition-exp。先核对Git/远端、CURRENT_CONTEXT、最新日志及I1-R1审计/检查/实现快照，不从RCO-0重审。仅阅读当前PRD相关节、真实识别入口和转换器、V2输入契约与需要的少量现有结果；旧模型结果只读且标明已见，不改历史FAIL。

1. 画清实际文字输入→当前识别输出→任务/时间/材料/条件/修订/来源→V2面板→确认存储的交接；明确哪些已实连、人工替身、兼容投影或缺口。
2. 检查用现有正确/失败输出能否定位字段丢失、契约不可表达和模型本身语义错误，三者分开；不靠新关键词或改答案救分。
3. 指定下一实施包的每一个文件、允许职责、隔离开关、失败保留与最小正反检查。若涉及冻结契约/confirmationV2/repository/Schema，列为需单独批准范围，不能暗改。
4. 下一付费实验只能作为待授权方案，锁定将使用的模型/输入来源/调用次数/金额/无重试/失败停止条件与完整指标，不自动调用或新冻结数据。
5. 仅允许新增mainline-03-scope范围报告、精确白名单、检查设计和下一授权提示词；更新约80行CURRENT_CONTEXT、追加日志。独立范围复核和保护检查后单独提交推送文档、核对远端，然后停止。
6. 保持Expected/freeze/dataset/checkpoint/cache、历史结果与稳定默认/用户库不变；密钥/剪贴板/模型网络/费用/真实材料/真人/部署/RCO-6均0。保护变化或范围需扩大立即停。

模型准确率明确写本轮未测量。正式任务编辑/执行、ICS/真实提醒、稳定接入和商业验收另行授权，不以当前范围替代。
