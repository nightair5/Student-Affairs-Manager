# 新测试构造核对（不改旧夹具/Expected）

初次扩展定向91项：86通过、5失败。已检查原fixtures全文与旧R2测试，5失败来自本轮新测试构造误用，不是对旧答案的订正：
- 3项使用不存在的t0/t1 ID；原夹具实际使用save、submit、print等。改为读取响应实际tempId，确认断言不变。
- planned_start是R2新测试在内存做的变形，原multi的两个时间均task_deadline；本轮也只在新测试内存设置该type，保留“不可编辑+兄弟可确认”强断言。不修改旧夹具。
- revision原响应有requiresDecision冲突，旧R2本来预期阻断。原新测试误列为正向；现明确放入“不能冒充无日期可确认”的反向组，测试总数不减少，确认能力不放宽。

SCOPE文字“multi含planned_start”不够准确；指此内存变形覆盖，非原人工响应。旧SCOPE文档保持原样，本记录追加澄清。42字段检查仍使用未变形原multi，两任务均task_deadline。旧Expected、17测试、40/42、历史FAIL与冻结文件未变；三个最初红测试的断言未删或放宽。所有工具编排语法/类型错误另记为开发错误，不计产品失败/成功。
