# MAINLINE05 当前审计：等待一项明确范围扩展

## 结果与主阻碍

尚未交付已验收的新产品功能。本机已实现来源先存、语义核对、编辑/确认/处置、原子隔离保存以及真实App接线；23个现场实现/测试/启动文件仍未提交。不能将定向通过说成浏览器已经可用。

唯一需追加权限的主因：只读MAINLINE04把“前置任务的截止时间”算入当前任务，两个各有合法日期的依赖任务因此被误判为时间冲突。新确认层保持拒绝，不能靠忽略问题、删关系或改测试放行。独立审查确认此问题不是测试接口拼错，当前白名单不能根治。

## 实际进展与验证

- 来源回调前已真实保存Source/SourceVersion/Run/Draft；新语义result=null，不伪造2.0；无效返回保留原始失败记录。
- 真条件、新要求、无日期、普通双任务可在真实canonical仓储的内存transport确认；假/未知和问题项拒绝。部分/批量、共享材料、失败回滚、已保存编辑保留、过期/重复已有定向证据。
- 实际App、核对面板、任务/事件详情与日历已接可选语义能力；尚未实际浏览器验收。启动器构建预检包含8类人工通知/3条B8只读诊断，禁止Expected/评分器进入bundle。
- 25获准路径中23个已建立；两个检查编排文件尚未建立。当前完整SHA和15份临时日志身份见IMPLEMENTATION_SNAPSHOT.json。

| 检查层 | 本轮最新结果 |
|---|---|
| 定向core-07 | 38项：37过、1失败；6文件 |
| 原42字段口径 | 本轮旧V2真实内存确认42/42；旧40/42不改 |
| 最新类型预检 | FAIL：CalendarPage.tsx:159，TS18047可空值；范围内普通缺陷，停止时尚未修复 |
| 启动器构建预检 | PASS，仅构建，不是产品验收 |
| 独立根因审查 | BLOCKED_SCOPE_ONLY；独立确认测试16过/1失败，不累加进38项 |
| 完整实现审查/分层工程 | NOT_RUN |
| J01–J12实际Edge | 全部NOT_RUN |
| 真实下载/独立读库 | NOT_RUN |
| 模型识别准确率 | 本轮未测量 |

已执行定向断言未发现错误默认选择、重复/覆盖、无日期假时间/提醒；不能因此宣称全包安全通过。新全字段/关系100%的全链证据、真人效率和商业水平仍未验收。

## 可复现根因

旧multi两任务各有独立截止d0/d1：均requiresAction=true、defaultSelected=true、issues=[]。新测试仅在内存增加print依赖submit，不改原fixture/Expected或日期。冻结composer沿依赖把d0和d1都归给print，产生MULTIPLE_DEADLINES；print仍requiresAction=true，却不能同批确认。

位置：mainline04/semanticComposer.ts:109–130；mainline05/semanticState.ts:75–79。具体失败断言保留在semanticConfirmation.test.ts。普通双任务、无日期、真条件、新要求、共享材料正例保持通过。

这项工程变形不证明原通知实际包含依赖，不是模型识别实验。独立内存诊断见登记临时目录dependency-diagnostic-02.log；第一次诊断命令仅PowerShell引号解析失败，未运行产品，不计业务样本。每次attempt单列，没有把多次重跑累加成更多样本。

## 保护与Git

864保护路径、4计划文件、8个MAINLINE04实现逐字SHA匹配；日志原前缀不变。旧17测试、40/42、历史FAIL、Expected/freeze/dataset/checkpoint/cache及旧runner不变。基线本机和远端均核对为8ffed8746e8b270168c5d1acb2629c631de946a7，没有回切、重装或应用旧补丁。

模型/模型网络/费用/密钥/剪贴板访问0；不接真实库、稳定入口或部署。只预检新Edge空白标签，未打开产品测试库。未新增依赖、数据集、盲测或采集真人材料。

本次按STAGING_MANIFEST.json仅提交审计文档、短交接与追加日志；23实现原样保留未提交。提交号以承载此文档的Git提交及最终远端核验为准；推送失败不强推、不自动变基。不是业务完成提交。

## 唯一下一建议

只追加一个文件的精确权限，然后继续当前完整MAINLINE05，不换模型、不另开补丁阶段。依赖是先后关系，不代表共用日期；付费模型不能修复这个客户端结构判断。

### 可直接使用的授权提示词

> 继续同一个RCO-5-MAINLINE-05完整工作包。先核对本轮审计交付提交/远端、IMPLEMENTATION_SNAPSHOT.json中23个现场SHA及原保护/日志边界，不回切、不重做PLAN、不重复应用实现。明确增加src/experiments/mainline04/semanticComposer.ts为唯一新保护例外（25路径扩展为26），仅增加MAINLINE05显式启用的“本任务资产归属与任务依赖安全传播分离”，旧默认行为保持不变；不删依赖、不忽略首次问题、不以05关键词规则绕过冻结判断。MAINLINE04其余7文件及旧测试断言/契约/冻结/Expected/dataset/checkpoint/cache/历史均只读。回归放现有获准05测试路径，保留当前失败断言；不同截止的依赖可先确认或同批确认，自身多截止、坏依赖、循环和未确认前置仍拒绝，原有正例不退化。新增05检查编排精确记录这一例外和审核SHA，不改历史freeze。随后修复CalendarPage类型问题，补齐当前包剩余编排、完整独立审查、分层工程、J01–J12真实Edge、实际下载与独立读库、精确Git交付；不为范围内普通缺陷拆新阶段。需第二个新文件/职责扩展、保护变化或重大安全问题才停止申请。0外部识别模型/模型网络/费用/密钥/剪贴板，不新增数据/依赖、不接真实库/稳定入口、不部署。完成后停止，模型准确率本轮未测量。
