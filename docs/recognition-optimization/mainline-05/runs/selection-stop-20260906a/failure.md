# MAINLINE05 同包继续：安全停止审计

日期：2026-09-06。状态：**FAIL / 未完成 / 仅交付审计，25个实现文件留本机未提交**。

## 这次做成了什么

已恢复e9b87e7dc4546e91d7f5559807b83465ba857cb0现场并核对远端、原23个SHA、864项原保护及日志。用户批准唯一composer例外后，执行26路径范围，未回切、重装或重复套实现。

- composer增加MAINLINE05显式模式：本任务资产不吞并前置任务资产，但前置问题仍传播；旧默认不变。保留原失败断言，不删除依赖。不同截止的依赖顺序/同批确认、自身多截止/坏依赖/循环/未知前置的正反定向已通过。
- 修正CalendarPage可空值类型判断；新增真实App/Calendar服务端渲染检查和旧42字段检查。两种Node宿主TZ不是实际浏览器时区验收。
- 创建检查主脚本草稿，但尚未验证；其Node编排测试未创建。不能用未完成checker宣称保护或工程已通过。

## 为什么现在必须停止

PLAN第7节把“错误默认选择”列为重大安全停止条件。本次不是白名单遗忘，也不是缺少新模型。独立审查通过真实内存仓储接口找到了当前实现的3个问题：

| 问题 | 用户会遇到什么 | 已观测边界 |
| --- | --- | --- |
| S01 当前选择与处置脱节 | 已拒绝前置任务，后续任务仍被默认勾选 | 确认器拒绝写入，正式任务0；仍违反错误默认选为0 |
| S02 把漏读当无任务 | 内容未完全归类，却能标为纯信息已确认 | 未创建任务，但完整性结论错误 |
| S03 共同使用被当成截止归属 | 共享材料被增加原响应没有的时间关系；后确认兄弟项改变材料截止 | 隔离内存中d0变null，不能宣称无覆盖或关系保真100% |

根因分别位于semanticView默认选择、semanticState信息处置、canonicalFacts关系投影，详见review.md与failure.json。三项仅登记，发现安全问题后没有继续改产品、测试或编排。没有真实用户数据受影响。

## 分层数字（不是识别准确率）

- 原失败复现：16过/1失败，保留日志。
- targeted-01：116/116（MAINLINE05 44 + 原MAINLINE04 72）。
- 新SSR测试初次45/46，具名导入纠正后，最新MAINLINE05 46/46。不把重跑相加。
- 旧V2等价内存字段42/42。新语义全字段/关系验收FAIL，不能用42/42替代。
- 应用类型检查本次执行通过；完整最终工程门NOT_RUN。
- 独立核心审查BLOCKED，3个实接口反例；完整审查、checker和checker测试未完成。
- **J01–J12真实Edge：全部NOT_RUN，0/12完成；实际下载、独立浏览器读库NOT_RUN。** SSR/内存不是用户闭环交付。
- 外部识别模型/模型网络/费用/密钥/剪贴板/新数据/新依赖/真实库/稳定接入/部署：均0。模型识别准确率：**本轮未测量**。

## 保护与现场

863项仍受保护文件、4份原计划、引用快照和234733字节日志前缀检查一致。其余MAINLINE04七文件及旧断言/契约/冻结/Expected/dataset/checkpoint/cache/历史只读。原MAINLINE05历史FAIL、旧40/42、17测试、B8 FAIL、旧环境3/1保持。

failure.json保存本次25个完整SHA及1个NOT_CREATED；targeted.json绑定本次5份长日志。旧IMPLEMENTATION_SNAPSHOT仍保留上轮23个SHA，不把它改成当前现场。

只暂存failure.json列明的8个文档路径。业务实现全部不暂存；提交/远端准确号以本报告所在提交及实际交付回执为准。若推送失败，保留本地提交，不强推/变基。

## 唯一下一步及理由

继续**同一个MAINLINE05工作包**，先获准修复此次登记的S01–S03，不扩到第27个路径、不重做PLAN。它们直接决定“勾选的是用户当前同意的任务、保存的是原文明确的关系、没读全不会被假称完成”；先修这些才有意义进入真实浏览器验收，换模型不能修复本机状态/保存错误。

### 建议授权提示词（尚未执行）

> 继续同一RCO-5-MAINLINE-05完整包，明确授权修复selection-stop-20260906a已登记S01–S03重大反例；不新增阶段、不重做PLAN。以本次已推送审计提交/远端和该目录failure.json的25个现场SHA及1个未创建路径恢复，核对863保护、4计划、日志前缀，不回切/重复套实现。
>
> 保持26路径与既定职责；优先semanticView.ts、semanticState.ts、SemanticFacts.tsx及既有获准05测试，必要App显式选择props仍在原职责内。先补登记反例和正常对照：前置拒绝/暂缓/取消勾选后，依赖任务不得保持错误默认选；前置已确认或同批实际选中时可正常确认，不能以全部不选过门。首次建议/当前处置/实际勾选分开，提交器仍独立验证。
>
> 有UNACCOUNTED_SOURCE_SCOPE或其他未解决信息问题时，不能把空tasks当正确纯信息；保留待核对与失败原因。完整纯信息能正常核对，人工已读不冒充语义完整。
>
> 时间与材料仅按已有结构明确的关系映射，不由共同任务owner补造截止。共享材料先后、反序、同批确认均不得改写已确认字段含义，保留显式真实关系/独立兄弟。原文/首次响应/编辑历史不动，旧42/42与原断言不弱化。
>
> 保留composer唯一05显式模式与旧默认正反测试，不动MAINLINE04其余7项和历史freeze。完成剩余checker及Node测试，精确绑定当前授权HEAD、26路径、863保护与最新审核SHA；主脚本目前只是未验收草稿，不能直接沿用旧硬编码HEAD作为最终基线。
>
> 范围内普通问题连续修复。定向通过后重新无上下文完整独立审查，再完整分层工程、J01–J12真实Edge逐键/保存/部分批量/故障/刷新/实际下载对独立读库、精确业务提交推送。未变证据按SHA复用，变化处重验。若新未登记重大安全问题、保护/重叠用户修改、第二新增文件或职责扩大才停止申请。
>
> 0外部模型/模型网络/费用/密钥/剪贴板/新数据/新依赖/真实库/稳定入口/部署。CURRENT_CONTEXT约80行，日志追加。失败保留现场只交付审计；成功后停止。模型识别准确率写本轮未测量。
