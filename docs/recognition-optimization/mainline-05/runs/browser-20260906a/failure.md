# MAINLINE05 浏览器首轮局部问题（非重大安全停止）

基线：safety-fix-20260906a/review.json 的26SHA；完整工程 full-20260906a 已PASS。Edge标签763114631/763114632；origin http://127.0.0.1:6627；唯一新测试库 rco-mainline-01-02-i1-mainline05-688bf3e0-6e60-449a-acec-01a11d1fdfc4。

1. 收件箱两来源批量，一笔故障/一笔成功，真实status为“人工工程通知（非模型预测）（:draft:1）：该草稿未确认：INJECTED_ATOMIC_FAILURE；人工工程通知（非模型预测）（:draft:1）：已确认保存1项”。两条ID截尾相同，无法逐来源区分。修正仅App显式semantic分支显示完整draftId，不改提交逻辑。实际浏览器构成失败复现；不以缺模块或静态字符串检查冒充业务反例。
2. 工程工具固定右下角遮住面板确认按钮；键盘Enter可以真实确认，不能把超时写成保存失败。仅将获准browser.tsx工程工具移到左上，不改产品样式或确认行为。
3. 工程读库是异步，新读回完成前DOM保留旧快照；首轮一次读数误取旧0项，随后等待完成和新仓储日志，核实来源/草稿均1、正式任务0。旧读数不作验收。工具增加读中状态、禁重复读取、清空旧展示，完成后才展示新结果；不改数据库或导出。
4. 服务器为内存bundle。为重验上述变更且保留同origin/同库，现有启动脚本增加唯一--port整数参数，仍只绑定127.0.0.1；默认随机端口与--check不变，非法/额外参数拒绝。继续使用6627和原run，不访问启动输出产生的新run，不清库。

已实测的原版本证据保留；变化处须在最终SHA下重验。S01取消/暂缓/拒绝与同批/先后正例、S02完整/未覆盖信息、S03共享顺序/反序/同批均未发现新错默认选或字段覆盖。保存故障整库回滚、确认故障保留编辑、两标签过期与重复均已实测，最终下载仍待补齐。

工具能力限制单列：当前Edge只公布viewport能力，没有timezone覆盖；Asia/Shanghai真实浏览器已测，其他时区当前NOT_RUN，不能用Node双时区SSR冒充真实Edge。未擅自改系统时区或使用非获准CDP接口。
