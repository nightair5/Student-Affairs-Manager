# MAINLINE05 真实Edge执行账本与固定协议

状态：NOT_RUN；只有完整独立审查、当前分层工程无阻断后才开始。不是新PLAN或数据集。

## 身份与方法

- 用新Edge标签、新空隔离库，mount真实App；origin/库名/标签ID/最终26SHA在首次启动后写browser.json。
- 只用旧8类人工工程通知和获准工程变形，不调模型、不接真实库/网络服务。
- 同一run固定origin和库。刷新与第二标签恢复去掉new参数的同一库；不能清库/换端口掩盖失败。
- 用户编辑必须逐键输入/退格并显式保存；录入通知可按粘贴流程。每笔操作记来源与预期任务，不以DOM提示替代正式读库。
- 工程工具“独立读取测试库”使用全新CanonicalWorkspaceRepository/IsolatedTestStore；记录读回时刻、完整对象和摘要。它不是App内存或exportJson。
- 实际App下载JSON文件，保留真实路径、字节、SHA；与上述独立对象全结构比较并跑联合validator。文件缺失不PASS。
- 发生范围内普通问题先记失败、修补/定向/审查并重验受影响步骤；新重大安全或保护问题停止。

## 顺序与最低实际证据

| 旅程 | 实际执行与读回 | 初始状态 |
| --- | --- | --- |
| J01 | 首页及统一录入multi，关闭/恢复草稿，面板两项一次批量；Source先存，完整字段落库 | NOT_RUN |
| J02 | no-date不填日期确认；刷新后任务中心/首页/日历独立列表/详情找回；关联时间、提醒、实际jobs均0 | NOT_RUN |
| J03 | 仅开始/事件时刻不变截止；用户逐键补合法仅日期后日历对应日显示、不添时刻；不同实际浏览器时区单列 | NOT_RUN |
| J04 | vague、坏引用、单项坏scope；坏capture保留Source/失败信息，独立有效兄弟可确认 | NOT_RUN |
| J05 | 条件真实际确认，假/未知不默认且无法强行确认；暂缓刷新可核对 | NOT_RUN |
| J06 | 旧要求保留作废/新要求确认；完整information可核对无空项目，未覆盖负例按钮禁用且仍待核对 | NOT_RUN |
| J07 | linked-event实际保存、任务/事件详情/日历读回，完整时间材料依据保留；共享关系单列 | NOT_RUN |
| J08 | 名称/截止逐键编辑/退格/明确保存；无日期补日期；planned_start/事件编辑预禁用；原文/first/历史不互覆 | NOT_RUN |
| J09 | shared-material先一项，刷新，确认余项；材料属性/显式关系稳定、共享不重复，独立兄弟不连坐 | NOT_RUN |
| J10 | 保存失败回滚；保存成功后确认失败保留历史；第二标签过期拒绝/刷新恢复；重复不增项 | NOT_RUN |
| J11 | 收件箱多草稿批量，逐来源标题/短ID说明成败；每草稿独立事务 | NOT_RUN |
| J12 | 实际JSON下载 vs 独立仓储全对象；真实副作用记录/库绑定/联合校验，稳定bundle工程证据只作对应层 | NOT_RUN |

S01专项：依赖变形中拒绝/暂缓/取消前置后，后项不保留错误默认选；前置已确认或实际同批选择为正例。S02/S03随J06/J09读回，不只检查文字提示。

最终需列每项PASS/FAIL/NOT_RUN、操作和独立读取来源；不累计重试冒充更多用户样本。旧42/42与本轮全字段/关系保真分层，模型准确率、真人效率、商业效果本轮未测量。
