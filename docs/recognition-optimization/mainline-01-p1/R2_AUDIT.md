# RCO-5-MAINLINE-01-P1-R2 审计报告

结论：**本轮限定的隔离V2修复及工程验收通过；实际产品入口未接入，RCO-G5未测量/未通过，未部署。**
模型识别准确率：**本轮未测量**。

## 1. 推进主线哪一环

本轮修复“用户编辑→明确确认→可靠保存”。现在不能采用的计划开始/事件时间修改在保存前明确拒绝；能够保存的修改必须通过真实提交逻辑试算，确认时也不能静默忽略。即使模型给出正确响应，客户端也必须能忠实承接；这是产品主线所需的工程条件，不是识别效果证明。

只修R1已登记的时间编辑/提交采用范围，主修补1轮。原PLAN、旧数据、旧17测试、40/42和历史FAIL未改，不重复应用失败补丁。

## 2. 实际实现

- domainCommit.ts增加显式V2共用时间类型判断，集合与旧提交器既有支持范围一致；planned_start/event_start/event_end不开放编辑，原接口/default不变。
- confirmationV2.ts在写历史前拒绝不支持类型，并用真实DomainCommitPlan进行无写入试算。已保存但不支持的旧编辑不能直接确认；有效兄弟不连坐。
- V2编译器核对用户值、计划值与模拟canonical值及时间类型、时区、精度、原文一致；原有canonical不能被静默覆盖，也不能悄悄挡掉新编辑后仍返回成功。
- DraftReviewPanel显式V2提示并禁用不能承接的时间编辑；未传V2配置保持旧行为。
- 复用此前未提交的V2隔离链路、真实面板输入缓冲/显式保存、新测试库及回放工具，没有另写一套持久化引擎或改Schema。
- 保持普通已支持时间与无日期补日期的成功路径，不依靠全部拒绝过门；result_announcement沿用旧编译器已支持的行为，本轮不增加语义解释。

## 3. 实测数字

| 检查 | 本轮结果 |
| --- | --- |
| 新增失败复现（修补前） | 6失败、62通过；见R2_RED_TESTS.json |
| 第一轮修补后定向/对抗 | 68/68，旧17+原V2 28+R1 13+R2 10 |
| 同口径人工响应字段 | 旧40/42保持，新V2 42/42 |
| 新鲜独立审查 | 无阻断；独立复跑68/68；另有14/14内存检查 |
| 完整工程测试 | 967通过、1项live OCR跳过、0失败 |
| 工程测试分解 | Vitest901；server8、worker25、时间一致性1、多模态评测库23、已见回放库4、Functions5 |
| lint/应用及Node类型/Schema与时间契约 | 全通过 |
| 新临时构建/源码安全/新构建安全与隔离 | 全通过；新构建18文件，源码/既有产物扫描716文件 |
| 依赖审计 | 0漏洞 |
| 实际浏览器与IndexedDB | 协议8/8，加双项批量确认；5个新隔离库，页面error日志0 |
| 保护核验 | 683/683原保护文件不变；日志旧前缀不变 |
| 外部产品模型/密钥或剪贴板访问/人民币费用 | 0/0/0 |

967已包含定向68，不重复相加；独立补充14也不混入工程分母。字段42/42仅是旧人工工程响应的承接验证，不等于模型或真实材料正确率100%。

完整工程仅运行一次，详情及原始长日志目录见R2_ENGINEERING_CHECKS.json。不加载.env、不使用旧实验缓存，生成物进入新临时目录。cloudflare:check本轮非发布、不适用且未运行，避免访问Wrangler凭证；没有部署。

## 4. 真实确认与安全观察

- 多项分次确认、重复、刷新：0→1→1→2→刷新2；时间和材料关联匹配，首次建议与原文不覆写。
- 无日期：真实确认后1任务0时间；人工补日期为date_only/manual，原响应仍无时间；没有假时刻或假证据。
- 未保存逐键输入不写编辑历史/正式任务，并阻止受影响确认；保存后刷新可恢复。
- 模糊时间保持未选/待核对，不能用无日期通道绕过；事件共享与不可安全编辑的关系继续明确阻断。
- 故障事务回滚并刷新读回完整；未观察到未确认正式写入、错误默认勾选、重复、已确认覆盖或关键字段丢失。
- 上述“0”仅适用于已执行工程场景，不宣称总体风险为0。浏览器定位/观察脚本错误及纠正详见R2_BROWSER_OBSERVATION.md，未当作产品通过记录。

## 5. 保护、Git与剩余限制

- 起始HEAD/远端d45467d3cae849342dac9e6b3cc222127e650fa9；R1九源码哈希全匹配，未见重叠用户修改。
- R2_BASELINE记录687原tracked；只有两公共代码和两动态文档可变，其余683保护。新源码限原PLAN名单。
- App、Workspace Schema、repository、validator、旧Expected/freeze/dataset/checkpoint/cache、冻结识别组件、历史结果与旧runner不变。
- 源码检查清单见R2_IMPLEMENTATION_SNAPSHOT.json；这是本次可审计实现清单，不修改旧组件freeze，也不冻结公共接口未来演进。
- 单独业务提交计划信息：fix(app): verify isolated V2 time edit adoption。完成提交推送后核对远端；最终SHA及工作区状态以Git实际回执和本轮交付答复为准。
- 当前仍只是隔离V2能力；旧App日期拦截、任务中心/日历/提醒的无日期行为未在本轮改变或验收。
- unknown条件表达、事件确认及共享/间接时间结构编辑仍有明确限制，未绕过契约或用空字段掩盖。
- 构建主bundle约525.68 kB，保留大于500 kB警告，不属于本轮时间一致性根因；未借机扩展性能改造。
- 开发协作Codex用量不属于产品模型实验调用数或0元账目。无人类实验、无真实材料、无新盲测/B10、无RCO-6、无稳定路径接入或部署。

## 6. 下一步：从隔离能力走向实际入口

先申请实际入口和无日期下游的最小接入范围审计，确认真正需要修改的文件、版本和开关，再明确授权实施。不是再次换模型或新建数据。

原因：本轮证明“这条隔离确认链能保存正确”，尚不能说明用户日常入口已经用上它；旧App仍只读。下一阶段必须围绕实际录入/草稿→V2核对→隔离仓储→任务中心/日历/提醒完整验证，避免停在演示页面。范围/授权提示词见R2_NEXT_PROMPT.md。本轮完成后停止。
