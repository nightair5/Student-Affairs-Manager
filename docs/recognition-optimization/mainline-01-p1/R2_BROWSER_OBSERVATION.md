# P1-R2 真实隔离面板与存储验收

状态：**R1_BROWSER_PROTOCOL 8/8完成；补充双项批量确认通过**。
日期：2026-09-05；模型识别准确率：本轮未测量。

## 环境与真实/模拟界线

- 官方Computer Use浏览器接口，Codex In-app Browser，临时标签页3/4/5/6/7。
- 回环服务http://127.0.0.1:2623，由serve-mainline-01-p1.mjs生成内存bundle，无.env、代理、外部资源或文件读取服务；CSP connect-src none。
- 真实组件DraftReviewPanel显式V2，真实CanonicalWorkspaceRepository及IsolatedTestStore在新IndexedDB内确认、事务回滚和刷新读回；不挂载App、不触及用户工作区。
- 识别响应仍为旧manual/engineering人工夹具，不是模型预测、新盲测或真人操作时间样本。
- 数据库前缀rco-mainline-01-p1-0745ca5e-4330-4f20-a426-dcb413354994，分别为后缀-multi、-no-date、-added-no-date、-vague、-batch-multi。
- 页面“隔离存储读回结果”来自repository.load；展开完整存储详情后核对canonical字段，未调用隐藏应用方法、未直接注入IndexedDB数据。
- 正常输入使用pressSequentially，先单字符“2”再其余字符；没有用一次性fill/paste掩盖逐字编辑。

## 协议逐项证据

| 协议 | 实际结果 |
| --- | --- |
| 1. multi初始化 | 两项explicit默认选中；任务/时间/材料均0，编辑历史0 |
| 2. 逐键修改 | 首字符2及完整2026-09-12T09:00均停留在缓冲；任务0、编辑0；该项与批量确认禁用，有效兄弟确认可用 |
| 3. 显式保存与刷新 | 仅新增before=2026-09-10T18:00、after=2026-09-12T09:00的编辑记录；任务仍0。刷新重开后显示用户修改，首次时间和原文仍旧值 |
| 4. 第一项与重复 | 真实“加入任务”后任务/时间/材料各1；重复确认后仍各1，另一项待确认 |
| 5. 余项/刷新/回滚 | 剩余确认后各2；刷新仍各2。真实事务抛TEST_ABORT后before/after全记录相同，再刷新读回各2 |
| 6. 无日期确认 | 不补日期直接确认保存活动手册；刷新后1任务、0时间、0材料，无编辑历史、首次响应无时间 |
| 7. 人工补日期 | 独立库空框逐键输入2026-09-15；保存前0记录，保存后仅编辑历史；确认/刷新后1任务1时间。precision=date_only、isAllDay=true、timezone=null、extractionMethod=manual、evidenceIds=[]；首次响应timePoints仍[] |
| 8. 模糊时间 | “近期”保持原文与待核对状态，任务未选、确认按钮禁用、正式任务0；不假装正常无日期 |

补充：第五个独立库两项同时点“加入已选任务（2）”，任务/时间/材料由0变2，刷新读回仍2，原时间文本保持。

## 关键实际存储值

- 提交任务：action=提交、object=活动报名表。
- 修改后时间：normalizedValue=2026-09-12T09:00，timezone=Asia/Shanghai；rawText仍2026年9月10日18:00前。
- 首次响应normalizedValue仍2026-09-10T18:00；打印兄弟项仍2026-09-11T18:00。
- 活动报名表：PDF、命名组别、1份、提交渠道活动平台；relatedTaskIds指向submit，deadlineTimePointId指向d0。
- 入场凭证：A4、1份；归属print及d1不变。canonical证据记录1条，人工响应包含完整原始通知。
- 无日期任务0假时间；人工日期没有附加00:00或18:00。
- 检查的5个测试页控制台error记录均为空；不是对所有浏览器或所有输入的无错误保证。

## 观察工具问题与纠正（不隐藏失败）

1. “打印入场凭证”的宽文本定位同时命中第一张卡的合并下拉选项，最初读到第一项禁用状态；按当前DOM明确第二张卡后，兄弟确认实际为enabled=true。未修改产品来迎合定位。
2. 展开完整详情成功，但用provider/firstResponse文本定位提取大JSON超时；改为解析已经可见的无障碍文本JSON，不改变任何存储状态。
3. 观察助手函数闭包保留标签页3，三条拟标no-date的读数实际带-multi库名。这三条读数无效，不作为无日期证据；操作本身在标签页4。随后显式传入标签页4，读回-no-date库的1任务/0时间，完成正确核验。没有重新识别或重建响应刷结果。
4. vague页的歧义说明也是article，首次通用article定位不唯一；使用当前DOM已暴露的article.review-item后完成阻断核验。

以上是观察脚本/定位问题，与产品模型请求失败和产品数据损坏分开记录；没有删掉产品失败或修改Expected。后续自动验收应把标签页ID和数据库名作为每条读数的必检字段。

## 边界与收尾

本次所测场景未确认写入、错误默认选择、重复、已确认覆盖、关键字段丢失均观察为0；分母仅本机工程场景，不能推出真实产品总体错误率0。

未做Chrome/Edge/手机商业验收、真实App接入、真人耗时或付费模型测试。测试页3–7已关闭，本机服务已停止；五个测试数据库保留，不删除。
