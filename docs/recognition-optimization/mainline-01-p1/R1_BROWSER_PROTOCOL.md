# P1-R1 隔离浏览器验证协议

状态：等待独立审查无阻断后执行；不是预报PASS。旧人工工程响应，不新建dataset，不测模型或真人时间。

1. 新测试库multi：实际读取响应，确认正式任务0；两项原文明示任务默认选中。
2. 展开第一项，逐键修改时间，检查未保存提示、正式任务/编辑记录仍0、该项和批量确认禁用；有效兄弟项不被禁用。
3. 点击保存修改，读回原文、首次建议、前后值；仍0正式任务。刷新、重开，修改与首次建议仍分开。
4. 确认第一项，读回1任务/对应材料与时间；重复确认仍1项；剩余项仍待确认。
5. 确认剩余项，刷新读回2项且原时间rawText不变；通过测试按钮触发真实IndexedDB失败回滚，读回原记录不变。
6. 独立no-date测试库：不补日期即可确认并刷新读回1任务/0时间。
7. 另一独立no-date测试库：在空日期框逐键输入完整日期，保存前无记录且禁确认；保存并确认后只有date-only人工时间，0假时刻，首次响应无时间不变。
8. 独立vague测试库：明确显示待核对，不能按无日期确认。非explicit默认/关联冲突/源引用篡改等由定向对抗测试单列，不伪装为浏览器已测。

每步实际结果另存R1_BROWSER_OBSERVATION.md。全部操作仅本机回环服务器、真实DraftReviewPanel显式V2、真实仓储和新IndexedDB；不挂载App，不触及用户工作区，不代表Chrome/Edge/手机商业验收。
