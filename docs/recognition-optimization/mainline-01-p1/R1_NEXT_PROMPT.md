# 下一步授权建议：P1-R2（尚未授权）

## 为什么

唯一阻断是“允许保存的编辑，并不一定被正式提交采用”。先让编辑入口和提交器对时间类型达成一致，修复真实确认可信度；不涉及模型调优，不扩展时间语义，不新建数据。

## 可复制执行提示词

授权执行 RCO-5-MAINLINE-01-P1-R2：仅修复R1独立审查已登记的时间编辑/提交采用范围不一致，恢复当前本机未提交V2实现，不重做PLAN、不重复应用FAILED_IMPLEMENTATION.patch。

先核对Git、R1_BASELINE.json、R1_REJECTED_SNAPSHOT.json、R1_AUDIT.md、R1_REPRODUCTION.md、CURRENT_CONTEXT及最新追加日志。源码哈希不符或有重叠用户修改，立即停止。

沿用原PLAN白名单：已有公共文件仅domainCommit.ts和DraftReviewPanel.tsx显式V2增量，旧接口/default行为不改；新增模块、隔离入口、两脚本、测试及新报告仍限原获准路径。App、Schema、repository、validator与冻结组件只读。扩大文件或语义范围先申请。

唯一根因与验收不变量：
编辑界面/保存入口接受的时间修改，确认提交必须实际采用；不能采用的修改要在保存前明确拒绝，不能显示已保存后悄悄用旧值确认。原文、首次建议和用户编辑分开保留。

执行顺序：

1. 先新增测试复现旧multi工程响应中d0.type=planned_start、保存新deadline后确认仍落旧值的问题；不修改原夹具或Expected，只在新测试中内存变形。
2. 使用现有结构化时间type明确可编辑范围，与提交器实际采用范围一致；本轮不新增planned_start/event时间编辑能力。禁止按固定句子、案例编号或中文关键词打补丁。
3. 本阶段不能承接的修改在历史写入前失败并给出明确说明；同时验证普通截止时间、无日期补日期正常成功，相关有效兄弟项仍能确认，不得全部拒绝过门。
4. 验证用户保存值、确认计划和canonical读回值/时区一致；不能写入时整笔确认回滚，不出现“编辑历史成功、正式时间却没变”。原文和首次建议不得覆写。
5. 沿用旧42字段口径及旧17测试；旧40/42和历史FAIL不改，新V2保持42/42。补齐已登记时间类型正反测试与保存/确认回滚；其余既有对抗测试不得删除或弱化。
6. 同根因最多两轮局部修补；定向通过后请一名无上下文独立审查者，报告不超过800字。发现新错误默认选择、保护变化、范围扩大、测试弱化或两轮无效，立即停止并只交付失败证据。
7. 审查无阻断后才运行一次完整适用lint/test/build、安全、依赖及保护检查，并实际完成R1_BROWSER_PROTOCOL.md中的真实面板V2→新测试库→确认→刷新读回。正常逐字输入不能用一次性fill代替；不能把DOM提示当作存储实测。
8. 成功才单独提交推送业务实现并核对远端；失败仅上传审计证据，保留未提交源码。交付审计、数字、保护状态、未解决问题、Git状态及下一步理由/提示词。完成后停止。

硬边界：0次外部识别模型调用、0次模型网络请求、0元、不访问密钥或剪贴板；不改既有Expected/freeze/dataset/checkpoint/cache、历史结果/runner、冻结组件；不运行旧一次性runner、不创建B10或新盲测、不接稳定入口、不启动RCO-6、不部署。

上下文控制：只读当前授权链和失败相关片段，不反复读完整历史；一轮一个根因；长日志写新文件；CURRENT_CONTEXT约80行。模型准确率写“本轮未测量”。

通过R2之后，停止并申请实际产品入口与无日期任务下游验收，不自动调用模型或创建下一组数据。
