# P1-R1 新鲜独立审查：BLOCKED

- 日期：2026-09-05；审查者：/root/p1r1_fresh_review。
- fork_turns=none；仅提供当前授权、文件路径与待审范围，不提供主代理通过结论。
- 只读审查与本机内存反例；0次产品模型调用、0密钥访问、未修改文件。
- 审查结论：**BLOCKED / NO_PROMOTION**。下面是审查者最终结论（800字以内）；主代理已停止修补、全量工程检查和浏览器验收。

## 审查结论

发现1个确认边界缺口：非截止类型的单个时间允许保存编辑，最终提交却静默忽略。位置：confirmationV2.ts:179与domainCommit.ts:552。

最小复现：沿用已见multi，仅将d0.type改成planned_start；正常调用编辑入口，将deadline保存为2026-09-12T09:00。界面状态显示“用户修改”、defaultSelected=true、无阻断；确认成功后，canonical时间仍是2026-09-10T18:00。修改历史记录新值，正式值却保留旧值。

原因是提交器排除planned_start/event_start/event_end，编辑入口未作对应阻断。最小处理应明确拒绝该类编辑，无需扩展时间语义。

独立复跑58/58，保护检查673/673，旧17项及40/42对照保留，git diff --check通过。实际逐键浏览器验收和全量工程门未运行。

## 证据边界

- 自动测试证据：C:\Users\Winner\AppData\Local\Temp\rco-mainline-01-p1-r1-agzTk4\results.json。
- 新反例来自审查者实际执行的内存程序，不是新增冻结数据或模型输出；主代理不把它冒充自己的浏览器复现。
- 58项通过不包含这个独立探索反例；它作为另列阻断证据，不篡改既有测试结果。
- defaultSelected=true表明修改失效后仍被允许确认；不能据此单独推出原任务本身是非任务，也不能估算总体误选率。
- 本轮未建立“错误默认勾选为0”的完整验收证据，不能填0或宣称验收通过。
