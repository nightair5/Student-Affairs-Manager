# RCO-5-MAINLINE-01-P1-R1 失败审计

状态：**REJECTED_STOPPED_WAIT_AUTHORIZATION / NO_PROMOTION**

日期：2026-09-05。模型识别准确率：**本轮未测量**。

## 1. 结论与主线位置

本轮改善“可核对建议→用户编辑/确认→隔离保存”这段客户端承接链路，未测量识别质量，未进入真实产品入口或Commercial Preview。修补后58项定向/对抗测试通过，同一旧人工响应的新V2字段对照为42/42；但独立审查发现1个新的保存一致性阻断，因此本轮功能验收失败。

大白话：用户把一个“计划开始时间”改了，界面记下了新值，最后真正保存的任务时间却还是旧值。模型就算识别完全正确，这种客户端问题仍会让用户改错、漏事；优先修它推进主线，但测试通过不能代替保存正确。

## 2. 起始现场与范围

- 实际仓库：C:\Users\Winner\student-affairs-multimodal-exp。
- 分支：codex/e2-multimodal-recognition-exp。
- 起始HEAD/远端：644789d9be866bf5e048f3014f673b177a68be5d。
- 先核对IMPLEMENTATION_BASELINE.json、REJECTED_SNAPSHOT.json、既有独立审查和CURRENT_CONTEXT；8个失败源码文件实际字节哈希完全匹配，无重叠用户修改。
- 未重复应用FAILED_IMPLEMENTATION.patch，未重做PLAN，未新建数据集。
- R1_BASELINE.json记录677个原tracked文件；4个动态/公共文件获准修改，其余673个受保护。
- 公共代码只修改domainCommit.ts与DraftReviewPanel.tsx的显式V2路径；新增代码限原PLAN白名单。
- 修改依据仅为已登记确认边界反例；主修补1轮。新审查阻断后未进行第二轮扩展修补。

## 3. 已做的局部改动（未验收交付）

1. 安全默认选择与明确用户意图分开：非explicit不默认勾选，有效建议仍可由用户主动确认；不可确认项不能靠selected越权。
2. 沿任务、材料、时间、事件引用定位相关实体和冲突，不把材料关联时间当成无日期，也不让无关兄弟项连坐。
3. 本阶段不能承接的事件关联明确阻断并保留草稿，不假装保存了事件；共享/多时间/间接材料时间的不安全编辑明确拒绝。
4. 来源原文、首次建议与用户编辑分别保留；修正date-only补具体时刻的时区，保持旧默认接口行为。
5. 真实面板V2加入输入缓冲和显式“保存修改”，未保存输入阻止受影响确认并说明刷新会丢弃；单字符输入不再立即充当已保存修改。
6. 准备隔离测试页、准确的存储读回摘要和零模型验证脚本。逐键交互与完整浏览器链路尚未执行，不能以静态渲染测试替代。

## 4. 验证数字与限制

| 检查 | 本轮证据 |
| --- | --- |
| 修补前新反例测试 | 9失败、45通过；5组已登记问题被复现 |
| 修补后定向/对抗 | 58通过、0失败；4个测试文件 |
| 组成 | 旧受保护17项 + 原V2 28项 + R1新增13项 |
| 独立复跑 | 同样58/58，另行发现1个阻断反例 |
| 旧工程响应字段对照 | 旧40/42不改，新V2同42字段为42/42 |
| app TypeScript | 初步检查通过；不等于完整工程门 |
| 全量lint/test/build、安全/依赖门 | 未运行：独立审查未过，按授权顺序停止 |
| 实际面板→新测试库→确认→刷新读回 | 本轮未运行；协议仅待执行 |
| 保护核验 | 673/673不变；旧日志前缀不变；范围无额外文件 |
| 模型调用/密钥访问/费用 | 0 / 0 / 0元 |
| 模型识别准确率 | 本轮未测量 |

新V2 42/42只说明原来那组人工响应能保留所测字段，不覆盖所有时间类型、所有操作，也不是成功率100%。旧17项、40/42及所有历史FAIL保持不变；未运行旧一次性runner。

## 5. 新阻断与根因

证据见R1_INDEPENDENT_REVIEW.md和R1_REPRODUCTION.md。

把旧multi响应的d0.type改成planned_start，再正常保存deadline为2026-09-12T09:00：
- 编辑记录与面板状态接受新值；
- 无阻断，defaultSelected仍为true；
- 确认成功后，canonical时间仍是2026-09-10T18:00。

不是“汉字关键词不够多”，也不是模型识别错误。直接原因是编辑入口按“单个关联时间”允许编辑，而提交器按“可覆盖的时间类型”拒绝应用该覆盖；两个层面对“这项修改能否真正保存”没有共用同一规则。测试原先主要覆盖常规截止时间和已登记关联反例，遗漏了这种类型差异。

这不自动证明原任务本身不应选择；它证明用户修改被接受后仍能以旧值完成确认，属于新的已保存编辑丢失反例。依“仅修已登记反例”边界立即停止，不能用58/58或42/42覆盖这个失败。

## 6. 保护、失败现场与Git交付

- 不改既有Expected/freeze/dataset/checkpoint/cache、旧结果、冻结组件或旧runner；R1_REJECTED_SNAPSHOT.json记录当前9个代码文件的失败现场哈希。
- 该快照不是通过版本或组件冻结；旧REJECTED_SNAPSHOT和FAILED_IMPLEMENTATION.patch原样保留。恢复时核对R1新快照，禁止重复套旧补丁。
- 本轮只提交推送新的失败审计、验证数字与交接文档；业务实现和新测试/脚本保留本机未提交，不作可用功能上传。
- 工作区因此预期非干净；最终文档提交SHA及远端核验以Git和交付答复为准。
- 未接App/稳定入口，未启动RCO-6，未部署，未采集真人数据，未运行模型或读取剪贴板。
- 停止后只做交付文档内容/泄密检查、diff与保护核验，不冒充补跑全量工程门。

## 7. 下一步及原因

只申请P1-R2：统一可编辑时间类型与提交采用范围，确保“接受的修改最终必被采用，否则明确失败并不写入”。先复现这个已登记反例，拒绝本阶段不能修改的非截止类型，保留正常截止时间和无日期补日期的成功路径；不得通过全部拒绝来过门。

这是消除用户操作与正式保存不一致的最小修复，不需要换模型、新数据或扩展Schema。详细授权提示词见R1_NEXT_PROMPT.md。R2通过后，才另外申请真实入口与任务中心/日历/提醒对无日期任务的验收，不继续无限堆隔离实验。本轮已停止，提示词不是自动执行许可。
