# P1范围与交付规则审计

日期2026-09-05。状态：`DOC_SCOPE_REVIEWED / P1_CODE_NOT_STARTED / NO_PROMOTION`。

## 本轮实际做了什么

- 用户要求先明确公共文件/新版本范围，并在每次执行后上传Git、给审计、下一步理由和提示词。本轮据此交付范围提案，不将其推定为公共代码/冻结组件修改许可。
- AGENTS12.6新增固定交付规则，短交接与追加日志同步；新增PLAN.md、NEXT_PROMPT.md、本报告及检查摘要。
- 拟申请仅两个公共文件的显式V2增量：domainCommit.ts和DraftReviewPanel.tsx，旧默认不变。App与稳定入口只读，其他新文件职责有白名单。
- 尚未实现修复；历史40/42、完整案例0/1、unknown不可表达及原报告保持不变。模型识别准确率：本轮未测量。

## 第一性原理与下一步

上一轮42/42→40/42的差异出现在同一正确人工响应的确认边界，不是模型输入变化。因此先区分原文、显示投影与用户修改，才能让已识别正确的信息不在保存时被改写。无日期与模糊日期必须分开，避免逼用户编造期限。

下一步需明确批准PLAN第2节文件范围，再执行P1隔离V2。通过线是同口径42/42和安全/存储零违规，同时保留unknown缺口。通过后还必须另批实际App/实验入口接入及无日期下游验收；不把隔离演示当线上修复，不自动跳到新数据、模型或部署。

## 独立读者复核

`/root/p1_scope_reader`从空对话只读审查计划、提示词和AGENTS交付段；5问均正确复述：当前授权状态、两文件范围与默认行为、历史FAIL与新成绩分离、无日期/模糊/冻结阻断、固定交付与之后真实接入。

提出2处执行表述歧义，均已澄清：verifier=0明确为“模型verifier调用0”，不禁本机validator；旧失败复现明确用新脚本读取已见夹具并调用旧公开路径，不运行旧一次性runner。读者测试不是实现审查、人工语义真值或模型测试。

## 检查与保护

- 文档结构、相对文件链接、边界/提示词一致性人工复核；git diff --check、源码/既有产物安全扫描通过。
- 本轮从e67314e取得666原tracked文件实际字节SHA-256。仅AGENTS、CURRENT_CONTEXT和追加日志获准变化；其余663/663不变。旧日志前缀字节未变；旧MAINLINE-01的19文件交付清单可读回复核。
- AGENTS此次变化是用户新要求的执行规则，不追溯篡改旧保护快照；不运行旧阶段保护脚本来冒充当前范围门，也不修改它的允许列表。
- 未运行lint/test/build、模型、浏览器或历史runner；上一轮916 passed/1 skipped只作为历史引用，不算本轮成绩。0次外部识别调用、API Key访问0、产品模型费用0，无真人/新数据/部署。
- 本轮CURRENT_CONTEXT约61行、约5.1 KB；完整计划与提示词独立存放。Git提交/推送状态以最终Git核验与答复为准，文档不递归写自身提交SHA。

## 未解决及终止条件

时间原文改写、旧App无日期拦截、条件/修订表达缺口仍未修复。本次文档就绪不解除产品验收FAIL。收到精确P1授权前不写业务代码；获批实施后发现冻结依赖/额外公共文件/Schema需求时停止申请增补；错误默认勾选、保护污染或弱化测试立即停止。

下一轮完整授权提示词见[NEXT_PROMPT.md](NEXT_PROMPT.md)。每次执行后仍交付Git/审计/下一步/原因/提示词。
