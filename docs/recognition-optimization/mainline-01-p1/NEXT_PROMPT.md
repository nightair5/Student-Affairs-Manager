# P1 下一轮授权提示词（复制发送才生效）

```text
授权执行 RCO-5-MAINLINE-01-P1，按 docs/recognition-optimization/mainline-01-p1/PLAN.md 实施隔离确认V2，0次外部识别模型调用。

允许且仅允许修改两个既有公共文件：src/domain/v2/domainCommit.ts 和 src/components/DraftReviewPanel.tsx，增加显式启用的V2支持；既有导出签名、旧调用及默认行为保持不变，不修改App.tsx、不切换稳定入口。
允许新增 PLAN.md 第2节列出的 confirmationV2、mainline01p1隔离入口、测试、工程脚本和阶段报告，更新CURRENT_CONTEXT并追加日志。若发现公共文件被真正冻结组件清单绑定，或需要额外公共文件/Schema/持久化契约变化，先停止申请增补范围，不修改旧freeze。

只解决确认边界：来源事实、界面显示与真实用户编辑分离；未编辑的显示值不作为覆盖补丁；已有时间原文和来源依据不变，真实编辑记录前后值；无日期可确认且不造日期，模糊/冲突/残缺仍待核对；精度、时区和兄弟关联不得被静默改变。

沿用已见MAINLINE-01人工工程响应和原42字段口径，只允许围绕这些输入做成对工程变形；不创建新的冻结dataset/B10/盲测，不改旧Expected/freeze/dataset/checkpoint/cache、冻结识别组件、MAINLINE-01旧代码/结果/保护清单，不运行旧一次性runner。未知条件仍保留不可表达缺口，不能转false。

至少验证真实核对面板V2→用户确认→新建测试数据库→刷新读回；首次建议、真实编辑、部分确认、重复提交、过期版本和失败回滚分别验证，不连接用户工作区。识别模型调用、模型verifier调用、模型Repair/retry、API Key访问和产品模型费用均为0；允许必要的本机确定性校验与独立开发审查。不接稳定路径、不启动RCO-6、不部署、不采集真人数据。

先核对授权、分支、保护；由新脚本读取旧工程夹具并调用旧公开路径复现，不运行旧一次性runner。一轮一个主因、最多两轮局部修补；定向/对抗→独立审查→适用工程门/安全/依赖/保护核验。新V2客户端同一42字段须42/42；错误默认勾选、未确认写入、假日期、重复、覆盖和关键字段丢失均为0，旧40/42及历史FAIL不改。保护变化、越权、测试弱化或错误默认勾选出现时停止。

完成后单独提交并推送当前分支，核验远程SHA；提交审计报告，说明改了什么、推进哪一环、测试数字、未解决问题和保护状态。给出下一步做什么、为什么做以及对应授权提示词。模型识别准确率明确写“本轮未测量”。CURRENT_CONTEXT保持短交接，长日志留文件。报告必须说明旧App尚未接入V2，不能宣称线上修复。完成后停止，不自动进入下一阶段。
```
