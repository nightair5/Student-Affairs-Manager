# RCO-5-007-P2 已见 B3 回归对抗审查

## 裁定

`TECHNICAL_PASS_SEEN_B3 / ELIGIBLE_TO_FREEZE_P2_AND_CREATE_NEW_B4 / NO_GENERALIZATION_CLAIM`

P2 在冻结 B3 的 16 个案例上达到全部预登记回归指标 100%，但 B3 已用于诊断 P1，属于已见故障集。该结果只能证明已知错误被修复，不能证明新表达泛化、模型识别率或上线能力。

## 机制审查

- 条件层比较完整命题的肯定/否定关系；“无法闭合”作为同一完整命题重复出现时判为已发生，“没有获得资格”先判为对完整条件的显式否定。
- 动作层优先保留合法、逐字、已绑定 scope 的模型锚点；`actionType/effect` 只分类，不再把“办理线上缴费”改写成“缴费”。
- actor 只读取命令标记之前的主语片段；“正式成员请……”可判为群体，“不得把成员名单……”中的对象词不再污染主体。
- 修订层把原命令的历史时态，与当前 cancelled/superseded 状态分开；新要求仍独立保持 active。
- 默认选择继续只允许明确 addressee 的低风险本机/受控实体动作；群体资格不确定、外部交互和外发均不默认。

## 对抗边界

- 没有修改 B3 Expected、dataset、freeze、评分器、P1 或旧结果。
- P2 没有接入稳定 Worker、浏览器或 Workspace。
- 模型/网络/Repair/retry/Secret 为 0/0/0/0/NONE。
- 规则仍不是开放域中文语义证明器；未见同义结构、嵌套条件和跨段修订必须由 B4 首次门检验。

## 下一门

先冻结并提交 P2 全组件，再创建全新 B4。B4 必须在首次运行前冻结；若首次失败，只审计并停止，不得用 B4 修改 P2 后重测。
