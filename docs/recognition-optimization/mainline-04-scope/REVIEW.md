# MAINLINE-04-SCOPE 独立范围复核

- 审查者：mainline04_scope_independent；fork_turns=none。
- 范围：四份设计与必要的直接源码/已见输入投影；不运行产品、旧runner或模型。
- 结论：PASS_SCOPE_DESIGN；没有阻断问题，不代表实施或产品验收通过。

## 独立审查正文

三类瓶颈归因有直接证据：研究composer输入未含完整时间/材料，不能误记为交接丢失；旧布尔行动性缺少条件三值；事件已有表达字段但确认器明确阻断。B8-07的已完成内容与当前核对动作、B8-09的取消语义未被改写成新模型成绩。

I1新增成果是完整语义包与逐项核对决策：条件真、新要求和有效兄弟必须有正例，三值、状态、修订及多重关系逐项保真，不能仅靠旧拒绝测试交付。八个新增实施文件职责明确；公共契约、真实App、确认与持久化须另行逐项授权。新checker使用新白名单，仅复用旧分层方法，未发现范围与验证要求的阻断矛盾。

Expected隔离、全部输入列账、非零正例、局部问题与旧validator整体拒绝的区别均可验收；旧42字段回归与新语义正式保存NOT_RUN分开，未将既有能力误称新增产品成功。

一项非阻断提示：旧linked-event.location为null，相关通过只能证明未提供状态及关系保真，不能证明非空地点语义已验证。本次只读，无文件修改、网络、浏览器、旧runner或工程门运行。

## 提示处理

主代理已在VALIDATION_DESIGN输入边界及CURRENT_CONTEXT加入location=null的限制；不增加材料或新测试数据。
本轮是代码审查技能引导的主链/范围复核；不审命名样式，不另起无关修补轮。
独立审查不授权下一包；完成文档/保护检查、明确暂存和实际远端核验才完成本轮文档交付。

最终增量复核：PASS。只读复用既有语义/引用/定位与时间AST，不重建同义命题图；条件三值、覆盖及完整实体属性仍是必要增量，八文件和未接App/持久化边界不变。实施仍须完整满足T04/T06，不能借“复用轻量引用”省略修订或事件关系。
