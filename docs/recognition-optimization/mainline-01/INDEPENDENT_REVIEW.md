# MAINLINE-01 独立开发审查

审查者：`/root/mainline01_independent_review`；从空对话启动、只读审查。属于开发协作，不是被测识别模型、跨模型语义裁判或人工金标准。日期2026-09-05。

## 初次发现与处置

1. 新夹具将条件unknown硬填requiresAction=false，会混淆未知与否定。已在本轮新增适配中显式抛UNREPRESENTABLE_CONDITION_STATE；真实捕获服务保存来源并记录失败草稿，正式任务0。总案例仍8，正常响应7，阻断1，未删除困难场景。
2. 工程门的新build在临时目录，旧源扫描不能覆盖它。新增脚本已补该目录产物扫描与实验入口排除检查；npm audit参数现在必填，缺参数不得静默PASS。检查数字见ENGINEERING_CHECKS.json。

## 审查者最终意见（800字以内）

unknown阻断已解除，未发现其他必须修改公共代码的问题。允许在全量工程门、保护核验和新构建产物安全扫描完成后，提交失败诊断交付；不能标为产品通过或G5通过。

独立复跑17/17，重新诊断控制臂42/42、客户端40/42；诊断测试明确断言complete=false，无假通过。真实组件与测试回调边界清楚；仓储显式注入内存库或独立前缀测试库，未见用户数据库或外部识别入口路径。对照值只在提交后比较，不参与选择与提交。unknown仍在8例总分母，未改成false。

浏览器1场景7步是原执行者观察，审查者未启动浏览器或独立实测；拒绝、编辑持久化和完整App流程仍未验证。测量文档未把自动化时间冒充真人成绩。审查者未复跑全量工程门，新临时build须实际扫描，不得用旧dist替代。

结论：`DIAGNOSTIC_DELIVERY_CONDITIONALLY_APPROVED / PRODUCT_ACCEPTANCE_FAIL / NO_PROMOTION`。本报告不覆盖在审查后扩大功能范围的修改。
