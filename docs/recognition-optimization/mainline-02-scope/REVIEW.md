# MAINLINE-02-SCOPE 独立文档与范围复核

审查者：/root/mainline02_scope_review；fork_turns=none。只读AGENTS/PRD相关节、四份范围文档与直接相关源码；没有执行产品测试、模型或浏览器，没有修改文件。以下是原始结论的简短记录，不是代码实现审查。

## 首轮：两项文档阻断

1. 白名单不含InboxPage/Sidebar，二者没有禁用参数，不能要求所有未支持按钮预先disabled。建议白名单组件预禁用；只读组件保留按钮，由App在写入/挂载前拒绝并提示，验收0写入/0挂载。否则必须另批扩大文件。
2. 下一提示词缺准确仓库/分支/完整文档目录，存在在“比赛”工作区或旧HEAD实施的风险。

其余范围未见阻断：真实录入/恢复/面板及收件箱批量均追到旧路径；无日期下游结论与源码一致，没有将静态风险说成已误发或当前必造1970日期；9+8+2正确；核心正向确认/读回必过，未支持功能没有冒充验收。

## 修正与定向复核

- 仅修改四份文档，统一两类UI处理规则，没有扩大源码白名单。
- NEXT_PROMPT增加绝对仓库、分支、完整目录，明确以SCOPE交付为后续基线且禁止回切旧提交。
- 审查者复核：“两项原阻断均闭合，UI规则在四文档一致，9+8+2无需扩围；路径/分支/禁止回切已明确。”
- 另指出“9c5ec0b是R2起点”称谓不准；现已改为“R2交付提交、SCOPE起始HEAD”，非R2修复起始d45467d。审查者认为其余通过，无需扩大审查。

结论：修正后文档/范围无未解决阻断。唯一下一包MAINLINE-02-I1仍等待用户授权；本轮未实现、未部署，模型识别准确率本轮未测量。审查输出合计受控，不将文档通过当产品验收。
