# RCO-5-MAINLINE-03-I1 审计报告

结论：**NOT_ACCEPTED / REVIEW_BLOCKED**。来源绑定实现已形成，但独立审查发现凭据身份一致性漏洞，停止于完整工程检查和实际浏览器验收之前。只交付失败证据，业务源码保留本机未提交。模型识别准确率：**本轮未测量**。

## 1. 本轮改动与主线价值

唯一仓库 C:\Users\Winner\student-affairs-multimodal-exp，分支 codex/e2-multimodal-recognition-exp。起始本机/远端均为3439a8ee6ec2f1c96932b20b86b519544af7af52，工作区干净。SCOPE原747保护与R1原19实现起始均匹配，未回切旧提交、未应用FAILED补丁。

- 现有两文件：runtime 增加可选隔离回放及来源凭据；App 仅实验分支说明来源类型。未接普通入口。
- 新6源码/测试：唯一逐字依据的来源ID/位置绑定、已见回放类型隔离、真实App本机入口、三组定向测试。
- 新2脚本：限定输入的内存浏览器构建/本机启动器、定向与保护检查入口。
- 原文、原始回放响应、适配后的首次建议、用户编辑分别保留；来源凭据通过现有Capture写入新Source legacyData，不改变Schema/repository。
- 适配只改 evidence.sourceId/textStart/textEnd，其余字段深度对照；重复依据不猜位置，已有错来源/位置不修正。候选不可表达和完整校验失败明确保留失败，不伪造成功结果。

这推进的是“识别响应能否完整、可追溯地交到确认和保存”，不是提高模型识别准确率。仅工程内存场景有通过证据；真实App的新浏览器链尚未验收，完整通知条件/修订等表达缺口仍未解决。

## 2. 验证数字与边界

| 检查 | 本轮结果 | 不能据此宣称 |
| --- | --- | --- |
| 来源断点失败复现 | 1/1；旧multi服务形状下2项均被V2阻断，强制确认0写入 | 原服务已经兼容V2 |
| 当前定向/对抗 | 134/134，11文件；旧99+新35 | 独立审查无缺陷或整轮PASS |
| 可表达multi字段保真 | 42/42，真实canonical接口+内存repository确认/读回 | 实际Edge下载42/42、模型准确率100% |
| 范围内全结构差异 | 绑定外结构保持的定向断言通过 | 完整通知所有语义均可表达 |
| 保存/确认/刷新/重复/无日期 | 内存回归通过；无日期0时间、0提醒、实际jobs函数0 | 新浏览器逐键、跨标签、真实文件已验收 |
| 隔离浏览器入口预构建 | 内存构建通过；8旧人工类型/3已见候选，依赖图无Expected/评分/旧runner | 完整生产build或浏览器PASS |
| 独立凭据对抗 | 2/2矛盾凭据被错误接受；各默认选1项，用户明确确认后各落1任务 | 整体错误放行/错误默认选为0 |
| 完整lint/test/build/安全/依赖门 | NOT_RUN：独立审查阻断 | 全量工程通过 |
| 新Edge真实App/新库/实际下载 | NOT_RUN；未启动本轮server/浏览器 | I1-R1历史浏览器成绩可替代本轮 |

134为单次当前测试总数，不累计重复运行；最初1项复现已包含在定向集中。独立2例为额外诊断，不混入134分母。134日志PFkR6r，早期日志bvOBd9，首次复现UXMIfc；精确路径/哈希见CHECKS.json。

正常定向场景中未确认正式写入、重复、覆盖、丢失均未检出；独立两例从空库开始，capture成功且默认选中，明确确认后各读回1任务。该诊断未单独打印或断言capture后、确认前的Task数量，不能将该节点0项当作直接实测，也不能据此判断存在自动写任务。无效凭据进入可确认链是真实阻断，不能再笼统宣称安全全零。未发现语义任务误选，不等于已证明其为0。

旧40/42、旧17测试和历史FAIL不变；已见B8-01/07/09不变成新盲测或完整2.0成功。

## 3. 根因与为何停止

校验只证明了“每一份内容与自己的指纹相符”，没有证明“登记的版本、原始文本、解析对象描述的是同一次响应”。不同内容各自重新计算指纹仍能通过。问题位于输入来源可信边界，不是中文关键词或模型能力问题。

独立审查登记：版本与响应不一致；rawOutputText与rawResponse不一致。两者同属凭据内部身份一致性一个根因。代码查看还提示人工响应的originalModel混入服务形状说明，下一修复须明确把真实身份和展示说明分开，不篡改旧响应；本轮未另跑该诊断。

按当前授权“不合格身份不可错误放行/默认选择”的保守边界停止。没有利用两轮修补额度继续扩大已被阻断的实现；审查后的源码修补次数0。详见INDEPENDENT_REVIEW.md、REPRODUCTION.md。REJECTED_SNAPSHOT.json与审查快照相同，供下一轮核对本机现场。

## 4. 保护、费用与交付

- 752个只读文件哈希不变；日志旧字节前缀保留，改动无白名单外文件。冻结材料、旧结果/runner/cache、Schema、仓储、时间AST、confirmationV2均未改。
- 两个现有源码只做获准实验增量；普通入口未接线，但完整默认回归/构建未跑，不提前替其背书。
- 新正式浏览器数据库0、用户库访问0；只使用新内存repository及新临时构建/测试缓存。旧测试库、下载与旧缓存未删除/覆盖。
- 外部识别模型/verifier/Repair/retry/模型网络/费用/密钥/剪贴板访问均0；无真实材料/真人/新数据/盲测/B10/部署/RCO-6。
- Git仅提交本轮失败报告和两个动态文档，不提交10个业务源码/脚本，不回滚源码。提交、推送和远端SHA以最终Git回执为准；不可将docs交付称业务实现交付。

## 5. 唯一下一建议

申请 **MAINLINE-03-I1-R1：只修凭据内部一致性**。先核对失败源码快照，再写反例，严格区分可核验身份和展示说明；解析后的原始JSON须与原始对象结构一致、版本身份匹配，不能只重算哈希。合法字段重排/空白变化应保留正确通过，防止新的假失败。空响应失败记录、已见候选拒绝和有效兄弟项不得退化。

通过定向和新无上下文审查之后，才跑完整门和原浏览器协议；不要新建数据、换模型或引入新语义契约。完整授权提示词见NEXT_PROMPT.md。待此接线通过后，再单独批准完整语义表达与模型对照；当前不启动。
