# RCO-5-MAINLINE-03-I1-R3 审计报告

结论：**NOT_ACCEPTED_DOWNLOAD_UNVERIFIED / NO_PROMOTION**。分层工程门通过；实际 Edge 的真实 JSON 文件未取得，整轮未通过。只交付本轮审计，不提交业务实现或新编排脚本，不部署。

## 改了什么、推进哪一环

仅新增 scripts/check-mainline-03-i1-r3.mjs，解决“旧冻结环境证据”和“获准增加两开发类型后的当前环境”混在一个检查里的问题。原 12 实现/依赖本轮逐字未改，不重装依赖、不改测试、不改旧 freeze。
它证明当前来源绑定实现可以进入实际 App 工程验收；并没有提升或测量模型识别准确率。

历史层先核验 17 个 freeze 引用及原文件全 SHA；临时快照共 20 文件，原库测试前后均完整。package.json 保留原混合换行（58 CRLF、6 LF），只逆转已登记 R2 增量与两行上下文的换行；package-lock 精确 5510 CRLF。全 SHA 不匹配即失败，未忽略换行或重写旧文件。
快照只代表这些历史文件和原库测试完整性，不宣称复原所有历史运行软件；实际执行 Node v24.18.0，未安装旧依赖，未执行旧一次性 runner。

当前层核对旧 package/lock/根声明/357 原非根包对象和现有安装身份。仅 @types/node 24.13.3、undici-types 7.18.2 两项 MIT 开发类型及根声明可放行；原运行依赖和脚本不变。13 项包括 1 合法正例、10 依赖负例、2 历史字节/换行负例，不能全拒绝。

## 分层结果（不累计重跑）

| 层 | 数字/结论 |
|---|---|
| R2 旧当前工作区历史环境门 | 3 通过 / 1 失败原样保留，R3 未重跑或覆盖 |
| R3 历史快照完整性 | 17 原 freeze 引用、20 快照文件全 SHA，测试前后相同 |
| 原封历史库测试 | 4/4，仅在精确历史快照 |
| 当前兼容与反例 | 13/13，旧 357 锁对象零漂移，仅合法两类型增量 |
| 新无上下文审查 | PASS，无阻断；见 R3_INDEPENDENT_REVIEW |
| 当前完整工程门 | 一次执行：lint、app/node type、Schema/时间契约、build、安全均 PASS |
| 全量功能测试 | Vitest 986 通过 / 1 原有 opt-in 跳过；153 项定向包含其中，不相加 |
| 其余工程测试 | server 8/8、Worker 25/25、时间一致性 1/1、多模态库 23/23、Functions 5/5 |
| 构建隔离扫描 | 18 文件，0 findings；旧 lint 有 1 非阻断 React Refresh 警告 |
| 依赖漏洞审计 | 0 vulnerabilities，无安装或无关依赖升级 |
| 实际 Edge 原 12 项协议 | 10 PASS、1 PARTIAL、1 BLOCKED；不能称浏览器通过 |
| 字段保真 | 本轮实际 canonical 42/42；实际文件侧 NOT_RUN；旧 40/42/17/历史 FAIL 保持 |
| 本轮浏览器安全观察 | 错误默认选择、未确认正式写入、同操作重复新增、覆盖、丢失均 0 |
| 模型识别准确率 | 本轮未测量 |

定向初次 w1MOab 和加强两个历史负例后的 xDNFWN 分开保留，只报最终 13+4。完整门目录 C:/Users/Winner/AppData/Local/Temp/rco-mainline03-r3-1HdKiX；机器结果、每日志路径及 SHA 在 R3_ENGINEERING_CHECKS.json。不得将其 okay=true 解读为整轮交付成功。

## 实际主链证据与缺口

真实 App 已完成：来源先存、草稿恢复、两项一次批量、逐项/部分/Inbox 批量、逐键名称/仅日期、保存故障、确认故障、跨标签过期、重复幂等、刷新全对象读回。原始通知/raw receipt/首次适配响应/用户编辑分层保留。
真正无日期任务关联时间 0、提醒 0、实际 jobs 0；date-only 不造时刻。已见 B8 候选不可表达则保留 receipt、失败 Run/Draft，result=null，不伪造完整结果。

唯一交付阻断：真实 App 导出按钮触发三次，下载监听两次超时，精确同名 Downloads 定位没有新增文件。实际文件路径/大小/SHA、文件侧 42 字段及整库比对无法完成。
尚不能确定是产品、浏览器还是观察工具的下载机制问题；不把源码即时 revoke 线索当已证实根因。没有扩大 R3 权限去改 App、脚本职责、下载设置或浏览器权限。详细证据见 R3_BROWSER_RESULT。

仍未解决或未授权：条件/三值/修订/事件等完整表达、稳定识别接入、正式任务编辑/执行、ICS/真实提醒、Chrome/手机、真实材料/真人省时、付费模型与商业验收。工程通过不等于识别正确率或上线资格。

## 保护与 Git

- R3_BASELINE 799 项保护及原 12 现场匹配，新增编排脚本 SHA 与独立审查版本相同；日志前缀仅追加、无越界文件。
- 不修改既有 Expected/freeze/dataset/checkpoint/cache/历史结果/旧 runner/冻结组件；旧 R2 当前 FAIL 单列保留。
- 起始 HEAD/远端 beae935cba642027c8222644a6e541f2c636bfb2；不回切、不套补丁、不 reset、不清库。
- 本轮按失败规则只提交 R3 审计、CURRENT_CONTEXT、追加日志。原 12 + 新编排脚本保留未提交，见 R3_REJECTED_SNAPSHOT。
- 实际提交和远端核对以本轮 Git 回执为准；未推送前不声称成功。未部署、未触及稳定默认入口/用户真实库。

## 唯一下一建议

仅申请下载落地定向诊断，先证明 UI 触发、浏览器交付/落点和链接生命周期哪一环断开，并给出精确最小修改白名单；不重做模型计划、历史工程门或已通过的全部交互。
原因：当前保存和恢复已有实际证据，卡点是“用户拿到可恢复备份”，继续花钱调模型不能解决这个卡点。
授权提示词见 R3_NEXT_PROMPT.md。本轮已停止，不自动实施下一阶段。
