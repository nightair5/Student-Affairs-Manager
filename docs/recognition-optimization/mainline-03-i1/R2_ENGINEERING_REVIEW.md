# R2 工程停止原因独立复核

确认是授权依赖增量与历史整文件哈希绑定冲突。verifyRco5007Freeze第17行逐文件比较；旧freeze的prediction依赖同时绑定package-lock.json与package.json。独立只读复算全部17路径，仅这两文件不匹配；其冻结哈希均等于R2起始实际字节哈希，freeze自身未改。日志先遇锁文件即抛错，不能只处理第一处。

唯一下一最小方向：另批新增验证编排入口，在逐字哈希验证的历史快照中运行原封旧门；当前现场执行严格的两项开发类型增量及其余依赖零漂移检查，再执行其余工程门。保留当前3通过/1失败，分别报告历史冻结完整性与当前兼容性。不改旧freeze/Expected/测试，不删旧门，不以历史快照PASS覆盖当前FAIL。

复核者mainline03_r2_independent，仅只读；无代码、网络、模型或全量重跑，正文少于400字。这是下一授权方向，不代表本轮可以继续或兼容性已正式通过。
