# RCO-5-MAINLINE-03-I1-R1 审计

结论：**来源凭据修复及独立审查通过；整轮NOT_ACCEPTED_ENGINEERING_BLOCKED，需扩大工程依赖白名单后才能继续。** 模型识别准确率：**本轮未测量**。

## 改了什么、推进哪一环

唯一repo/branch保持，起始本机及远端c30cdb760a23353af191d6f9c1ee8dbb8d19eb40。10个本机未提交文件均匹配I1拒收快照，未重复应用补丁，未回切旧提交。

只改获准两实现与三测试，另五实现只读：

- recognitionHandoff：增加原始JSON与rawResponse全结构一致性；对象键顺序和空白不影响，数组顺序、缺失字段、值和类型差异必须拒绝；核对响应内嵌版本/模型，不重写原始文本。
- seenReplay：工程模型/版本保留响应实际身份，服务形状展示说明与身份分开；unknown在响应产生前失败时不编造响应版本；已见候选从冻结记录保留原模型，缺失prompt版本明确null，仍不转换完整任务。
- 三测试：新增19项同根因正反检查，原134项没有删改断言。保留原文、raw、适配后首次建议及编辑历史分层，不改任务、时间、材料或selected。

这闭合“来源记录与实际响应对不上仍进入确认”的已登记漏洞，推进识别结果到可信确认的交接。哈希只证明内容一致性，不证明外部服务商真实性；未新增语言识别能力，不代表G5通过。

## 分层数字与实际执行顺序

| 层 | 结果与证据 |
| --- | --- |
| 修复前反例 | 2失败+旧134通过，证明两种矛盾仍会被接受；XWF2mj |
| 第1轮功能修复 | 136/136，QaSg2T；增加同根因测试后153/153，最终初审快照日志2xMFm0 |
| 字段/安全定向 | 42/42内存canonical保真；原文/首次/编辑分开；正常无日期0时间/0提醒，真实jobs函数0；无未确认写入、重复、覆盖、丢失的用例断言通过 |
| 新无上下文审查 | PASS；独立6类矛盾重封存均拒绝，合法重排/空白可确认2项任务且原始/首次保留；三测试内存去新增块后精确匹配旧SHA，旧断言未弱化 |
| 工程门尝试1 | l1TQcP：lint两处新增测试调用换行错误，后续门未启动；修正仅两处换行，无断言/产品变化 |
| 换行修正后 | 153/153，ylFLCW；独立差异复核PASS，内存恢复换行精确匹配初审SHA |
| 工程门尝试2 | Rx2qfE：lint PASS，app类型检查FAIL（6个TS2307）；后续node类型/全量测试/契约/build/安全/依赖门均未运行 |
| Edge/实际下载 | NOT_RUN；未启动本轮server、Edge标签或真实IndexedDB库 |

这是同一工程门的两次提前中止尝试，不是两次完成全量。153为当前单次定向总数，不能累计重跑次数；独立6例不混入153分母。原134对应I1旧99+35，R1新增19。42/42属于内存工程读回，不是新浏览器文件成绩或模型准确率。

## 当前唯一阻碍与为何停

三个mainline03测试在I1原实现中已使用node:fs、node:crypto，项目未声明或安装@types/node，package-lock也无该包；本机Node v24.18.0，TypeScript ~5.7.2。浏览器构建和Vitest转译不会替代TypeScript完整类型检查，因此早期定向通过未暴露它。

Rx2qfE的6个TS2307均是上述三文件各两个Node模块类型缺失，没有发现新的语义误选或凭据漏洞。标准修复需新增开发类型依赖并更新package.json/package-lock.json，二者不在本轮白名单。当前停止级别：**NO_PROMOTION / NEEDS_SCOPE_APPROVAL**；只交付获准审计证据，不继续变更工程范围。

没有安装依赖、修改tsconfig、用ts-ignore/宽泛any/动态导入隐藏模块类型错误、排除测试或删除文件过门。也没有把范围问题称为模型失败。功能根因1轮修复有效；之后仅修2处lint写法，未继续处理需新文件授权的依赖根因。

## 保护和Git交付

- R1_BASELINE保护768项：原752、I1历史报告及5只读实现，最终仍不变；日志旧字节前缀保持。R1_REJECTED_SNAPSHOT保留当前10源码现场。
- Expected/freeze/dataset/checkpoint/cache、历史40/42/17测试/FAIL、Schema、repository、capture、validator、confirmationV2、domainCommit、时间AST均未改。
- 普通稳定入口未接线，RCO-6/部署/新数据/盲测/B10/旧一次性runner/真实材料/真人均0。
- 外部识别模型/verifier/Repair/retry/模型网络/费用/密钥/剪贴板/用户数据库访问均0。浏览器未运行，不冒称浏览器副作用计数实测0。
- 仅提交R1报告、CURRENT_CONTEXT和日志；10个业务源码/脚本保留本机未提交。提交/推送/远端SHA以最终Git回执为准，审计文档交付不等于业务交付。
- 本轮没有待关闭的server或新浏览器标签；新临时检查日志和所有历史库/文件保留，未清理。

## 唯一下一建议

申请 **MAINLINE-03-I1-R2工程依赖闭合**：新增批准package.json与package-lock.json，核实并精确锁定匹配Node主版本的@types/node开发依赖及必要传递依赖；只用新临时npm缓存，不升级其他依赖或扩语义。依赖影响独立复核后，继续完整工程门及原Edge协议，全部通过再提交获准业务实现。

为什么现在做：来源一致性漏洞已用正反与独立审查闭合，当前缺的是测试工程的类型依赖；无需再造数据、换模型或重写来源适配。若安装类型后暴露额外产品文件需改，再停止申请，不偷扩白名单。完整提示词见R1_NEXT_PROMPT.md。
