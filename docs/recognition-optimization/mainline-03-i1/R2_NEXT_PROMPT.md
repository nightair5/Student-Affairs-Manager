# 唯一下一建议：历史证据与当前工程环境分层验证

目的：保留旧实验的逐字可复现证据，同时允许本次仅开发类型依赖的显式增量接受当前工程验收。不是换模型、造数据、重做来源逻辑；不得把旧环境不匹配伪装PASS。

以下待用户另行批准，当前不执行：

> 授权执行RCO-5-MAINLINE-03-I1-R3：仅新增历史环境/当前兼容性验证编排，继续R2未完成工程与原Edge协议。唯一仓库C:\Users\Winner\student-affairs-multimodal-exp，分支codex/e2-multimodal-recognition-exp。起点为已推送R2失败审计提交，先核对Git/远端、R2_BASELINE/REJECTED_SNAPSHOT/FINAL_CHECKS/AUDIT、两份独立审查和CURRENT_CONTEXT。12个本机实现/依赖文件必须匹配R2现场，保护或重叠修改立即停，不回切、不重装升级、不套补丁。
>
> 只允许新增scripts/check-mainline-03-i1-r3.mjs与R3报告、约80行CURRENT_CONTEXT及追加日志。现有12文件、旧checker/库测试、Expected/freeze/dataset/checkpoint/cache/历史结果全部只读，不改测试断言或旧freeze。批准在新临时目录建立仅含已核验旧文件的历史验证快照，不含密钥/真实库/用户资料；不是新数据集。历史文件必须逐字匹配原freeze，换行差异也必须验证，不得忽略或猜测；不能构造准确快照则停止申请。
>
> 先验证旧freeze引用和历史快照的原文件全SHA，再在该快照运行原封历史库测试；不运行旧一次性runner。独立报告“历史快照完整性”，不覆盖R2当前现场3通过/1失败。当前候选单独验证：相对R2安装前依赖，只新增@types/node 24.13.3、undici-types 7.18.2两项MIT开发类型及根声明；全部原依赖/脚本/产品测试不变。记录旧新依赖环境身份，不能宣称当前环境与历史相同。新增检查须证明第三包、版本/运行依赖漂移及历史文件改变会拒绝，合法两类型增量会放行。
>
> 新无上下文独立审查无阻断后，运行当前lint/type/全量功能测试/契约/build/安全/依赖审计/保护，并纳入原封历史快照测试及新增兼容检查，作为明确分层的新工程门。旧当前工作区环境哈希FAIL保留且单列，不删旧门或把历史快照通过混成原工作区通过。任一新差异或需改现有源码/测试/冻结范围立即停。
>
> 新门全通过才执行原BROWSER_PROTOCOL的真实App新Edge标签/新空隔离v8库：录入→来源/适配→逐键编辑/显式保存→部分/批量→故障/过期/重复→刷新→真实JSON下载与canonical全对象核对；42字段原口径、无日期0时间0提醒和实际jobs检查。不能用fill、DOM提示或内部exportJson替代验收。
>
> 外部识别模型/verifier/Repair/retry/模型网络/费用/密钥/剪贴板0。不接稳定入口、不改语义、不建数据/盲测/B10、不部署/RCO-6、不采真人或真实材料。成功才提交推送原12实现/依赖及获准新入口与报告；失败只提交审计保留源码。最终报告分层数字、保护/Git/未解决项，模型准确率写本轮未测量；完成后停止。
