# R3 阶段进度记录

此文件下文保留独立审查前的阶段状态。最终结论以 R3_AUDIT / R3_FINAL_CHECKS / R3_BROWSER_RESULT 为准：新独立审查与分层完整工程门通过，实际 JSON 下载文件未取得，整轮 NOT_ACCEPTED；仅提交失败审计，原 12 与新编排脚本保留未提交。

当前唯一新增实现scripts/check-mainline-03-i1-r3.mjs；原12实现/依赖全部只读，799项保护不变。新增基线固定起始字节并在脚本中绑定SHA，不允许悄悄变更。

历史快照只含20个核验旧文件：17个旧freeze绑定文件、freeze自身、原封库测试、测试读用的旧result。除两依赖文件外直接逐字复制；package按已登记R2单行增量及其两上下文行CRLF逆变换，lock从R2起始Git内容按原CRLF重建；全部以旧freeze完整SHA为硬条件，不归一化后冒充同一文件。未执行任何旧一次性runner，未安装依赖，原Expected只留在历史核验快照，不进入产品。

当前兼容比较核对package全部字段、lock顶层/根/全部旧357非根包，以及两新增包完整对象/版本/integrity/dev/MIT身份；核对已安装两类型版本。仅2开发类型增量可通过，10种依赖负例及2种历史字节/换行负例通过真实拒绝函数。13项含1正常输入，不能全拒绝过门。

定向attempt1 w1MOab：13兼容与4历史库通过；随后把2个历史篡改检查加强为调用真实拒绝函数，不修改旧测试。新脚本lint与最终定向xDNFWN全部通过。只计最终13+4，不累计重跑。当前旧R2现场3通过/1失败保持。

完整门/实际Edge尚未开始，等待新无上下文独立审查。所有长日志在新临时目录，绑定见R3_REVIEW_SNAPSHOT；0模型/费用/密钥/剪贴板/部署，模型准确率本轮未测量。
