# 唯一下一主线：真实候选的最小语义承接范围（待授权）

当前补证解决的是“拿到完整备份”的证据问题。下一步应回到主线：模型识别出的条件、当前状态、旧要求作废与新要求生效、事件/时间/材料归属，如何完整进入待确认建议。不是再调下载或堆关键词，也不是重做全部历史审计。

以下只是下一轮建议，不构成当前授权：

> 授权执行 RCO-5-MAINLINE-04-SCOPE：只读针对 MAINLINE-03 已登记的语义承接缺口，确定唯一最小实施包，不修改产品代码、不调用模型、不新建数据。
>
> 唯一仓库 C:\Users\Winner\student-affairs-multimodal-exp，分支 codex/e2-multimodal-recognition-exp。以 R3-CLOSE 最终交付提交及远端为基线，先核对 CURRENT_CONTEXT、最新日志、R3_CLOSE_AUDIT/CHECKS/IMPLEMENTATION_SNAPSHOT；不要从 RCO-0 重审。
>
> 复用 MAINLINE-03-SCOPE 已有交接图和少量已见响应，直接列出当前不能无损承接的条件三值、状态、修订、事件以及时间/材料关联。区分模型未给出、现有契约装不下和转换器丢字段；不能把空引用当原文没有，不能把 unknown 变 false。
>
> 只提出足够形成可核对任务的最小语义结构，不建设通用知识图谱。AI 提出来源约束的语义事实；本机保留来源位置、引用有效性、时间归一化、安全选择及原子保存；用户最终确认。
>
> 每项变更明确具体文件、允许职责、数据版本、实验开关、兼容旧路径和回滚条件。涉及 confirmationV2、domainCommit、Schema、repository、validator 或任何冻结组件时，必须列为需单独批准的文件，不能暗改。
>
> 验收设计先用已见人工/历史响应零调用验证，覆盖真实存在的正反例和有效兄弟保留；不靠全部未知、全部拒绝或删除关系过门。规定事实完整性、错误默认勾选、漏项/误项及确认保存保真指标，工程成功与模型正确率分开。
>
> 仅允许新增 docs/recognition-optimization/mainline-04-scope/ 下 SCOPE.md、IMPLEMENTATION_WHITELIST.md、VALIDATION_DESIGN.md、REVIEW.md、CHECKS.json、NEXT_PROMPT.md；CURRENT_CONTEXT约80行，日志仅追加。原源码、所有旧证据/冻结/Expected/dataset/checkpoint/cache只读。
>
> 独立范围审查、保护和文档检查后单独提交推送并核对远端。发现保护变化、重叠修改或需扩大只读权限立即停止。0模型/模型网络/费用/密钥/剪贴板/真实库/真人/真实材料，不部署、不接稳定入口、不启动RCO-6。
>
> 最终只给出有证据的主瓶颈、唯一最小实施包及明确授权提示词。完成后停止，不自动实施语义升级或付费实验。
