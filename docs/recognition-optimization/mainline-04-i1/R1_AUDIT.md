# RCO-5-MAINLINE-04-I1-R1 审计：代码修补通过，阶段未完成

状态：BLOCKED_CHECKER_SCOPE / AUDIT_ONLY_DELIVERY。独立代码审查PASS；完整工程未运行，不提交业务实现为成功交付。

## 改了什么、推进哪一环

- 修订依据校验覆盖旧/新要求；缺失、坏scope、unknown均待核对，不让新要求绕过；合法新要求保留正例。
- 旧V2承接按新旧两侧真实相关实体并集比较，不因无关材料变化阻断独立任务。
- 独立审查发现composer仍经共享材料穿透其他任务私人时间；在同根因第二轮限制跨任务到明确dependency/parent，保留共有材料/时间/事件真实归属。
- 新增测试对象隔离与单侧归属断言，防止测试内共享数组污染旧对象。无固定句子、案例编号业务规则，无第三轮业务源码修补。
- 实际改变semanticComposer.ts、legacyHandoff.ts及composer/acceptance两个测试，共4文件；其他4候选文件只读。未改旧工程响应、契约、依赖、冻结或真实产品入口。

推进的是“语义关系→安全可核对建议→旧V2等价承接判断”，不是模型识别或新语义正式存储。

## 分层数字

| 层 | 结果 | 口径 |
|---|---|---|
| 最终定向/变形 | 72/72，失败0 | 原49项+23项；不同attempt不相加。最终报告R1_TARGETED_FINAL_RESULTS.json。 |
| 新增业务源码修补 | 修订1轮；实体范围2轮 | 首轮68/68未挡住独立发现，补反例后第二轮通过；最后只增强测试隔离。 |
| 独立审查 | 代码PASS，编排BLOCKED | 修订、共享/独立、单侧关系、重排真实helper内存核验；没有假称独立重跑72项。 |
| 8类人工工程输入 | 全字段/关系保真断言通过 | 只限给定人工响应；B8三条保持已见原输出、不能强转2.0。 |
| 旧V2内存确认 | 42/42 | 真实capture/confirmV2/内存repository读回；无日期0时间/0提醒及重复幂等原断言保留。 |
| 错误默认选择 | 最终定向0，独立未发现新例 | 不推断任意输入的总体安全率；有效新要求、独立兄弟有非零正例。 |
| 全量工程 | NOT_RUN | 只读checker阶段绑定不适用，不能绕过。 |
| 新语义实际App/正式保存 | NOT_RUN | 本轮禁止产品接入，不以旧V2内存代替。 |
| 模型识别准确率 | 本轮未测量 | 外部识别/模型网络/费用均0。 |

所有初次失败、首轮结果和I1历史FAIL均保留。R1_ATTEMPTS/REPRODUCTION说明各次证据与测试隔离修正，不把早先成绩改写为最终成功。

## 为什么现在停止

只读check-mainline-04-i1.mjs仍绑定旧HEAD 2405cf59、旧BLOCKED审查、旧临时目录和非R1输出。本轮起点是已推送失败审计1d039ac8。独立运行--protect在BASELINE_HEAD_CHANGED前置断言失败，没有创建临时目录或执行完整门；这不是新产品故障。

本轮明确禁止改checker，故只读登记R1_CHECKER_SCOPE，不回切、不改旧审查、不临时绕门。此时只能审计交付，不能业务完成。

## 保护和交付

- 起始本机/远端精确1d039ac8065235528b6a507244e66ef382ba6901。原8实现逐字匹配旧拒收现场后恢复。
- 845项受保护文件应逐字不变；原4测试完整前缀/49项不删改，日志旧前缀不变。最终实测以R1_CHECKS.json为准。
- 旧Expected/freeze/dataset/checkpoint/cache、历史40/42、旧17、B8 FAIL及旧R2环境3/1保留。
- 无模型/密钥/剪贴板/真实库/真实材料/真人/新数据集/盲测/稳定接入/部署/RCO-6。
- 只暂存R1_*审计、CURRENT_CONTEXT和追加日志，明确路径见R1_STAGING_MANIFEST.json；8实现仍未提交，最终SHA见R1_REJECTED_SNAPSHOT.json，该文件不是通过冻结。
- 提交、推送和远端精确SHA以本轮Git回执为准；未发生前不称已推送。推送失败不强推、不自动变基。

## 唯一下一建议

申请RCO-5-MAINLINE-04-I1-R2，只调整这一个检查脚本的当前阶段/审查/输出绑定，审查后继续完整分层工程。不要换模型、重做契约或接App。理由：业务定向和独立代码审查已通过，剩余证据缺口是合法运行完整工程门，而非再加识别规则。

详细待授权文字见R1_NEXT_PROMPT.md；当前不执行。若未来工程检查暴露额外源码问题，先报告申请，不能顺手扩大修复。
