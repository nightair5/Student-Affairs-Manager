# R1 检查编排的精确权限缺口

状态：BLOCKED_CHECKER_SCOPE。只读核对，未改脚本、旧BASELINE/REVIEW或历史工程结果；未绕过完整门。

## 证据

原只读脚本 `scripts/check-mainline-04-i1.mjs` SHA为 `71e7b771393fae15a454eb3e67bc6487c2e1d54f89c7535a6c2b8d5c5fbb8958`。

| 行 | 绑定 | 不能直接用于R1的原因 |
|---:|---|---|
| 18、22 | 旧BASELINE.json与HEAD断言 | 旧基线HEAD是2405cf59，本轮获准HEAD是已推送失败审计1d039ac8；不能回切或改旧基线。 |
| 23、27–30 | 旧保护清单与宽目录放行 | 本轮需父825项+追加20项保护以及R1_*写入范围，不能只沿用旧清单。 |
| 38–44 | TEMP_DIRECTORY.json与小写attempt名 | 仍使用旧报告身份，不能记录本轮独立临时目录及R1_*报告边界。 |
| 46–49 | 旧REVIEW.json及其SHA | 旧审查必须保持BLOCKED和原拒收SHA，不能改成PASS或以旧审查授权本轮新字节。 |
| 62、186 | attempt.json、ENGINEERING_CHECKS.json | 输出不是本轮R1_*追加文件；不符合本轮获准路径。 |

独立审查者只运行 `node scripts/check-mainline-04-i1.mjs --protect`，exit=1，报告 `BASELINE_HEAD_CHANGED`。该断言在任何临时目录创建和完整工程执行之前；这不是产品功能失败，也不是原845文件变化。主线程独立只读核对了以上实际源码行，未机械复跑相同失败。

## 唯一最小后续范围

另行授权仅调整这一个checker的显式阶段配置、保护与报告绑定；保持原默认模式及历史基线不变。新阶段必须绑定当前已推送R1审计HEAD、R1最终8实现SHA、最新独立审查；允许的代码变化只能是该checker本身。

新报告单列R2_*、新临时目录与新日志；原文件不能覆盖。保护原845项中除获准checker以外的844项，并追加绑定全部R1静态证据和7个只读语义源码/测试当前SHA，日志旧前缀同样绑定。检查应证明错误HEAD、错误实现SHA、审查不通过、输出越界和只读文件变化都会拒绝。

保持既有分层工程内容：历史快照逐字完整性、原封历史库测试与当前依赖兼容分开；现行lint/type/全量功能/契约/build/稳定bundle隔离/安全/依赖审核不删减。模型或一次性runner仍禁止。

新编排需独立审查后才能运行完整工程。任何产品/测试/依赖/Schema等额外修改需求立即停止申请，不能在工程检查阶段顺手修。新语义App/正式保存仍NOT_RUN；模型识别准确率本轮未测量。
