# RCO-5-MAINLINE-04-I1-R2 工程交付审计

状态：PASS_ENGINEERING_WITH_DECLARED_LIVE_OCR_SKIP。这是本阶段组件与检查编排验收，不是新语义App、模型准确率或商业验收通过。

## 本轮改动和实际推进

- 唯一修改实现为scripts/check-mainline-04-i1.mjs：新增显式--stage=R2入口，绑定当前基线、真实独立审核与8文件SHA；报告只追加R2_*，临时目录独立，旧默认/旧证据不变。
- 7个语义源码/测试完全沿用R1审定字节；没有重做契约、增加识别规则、修改依赖或新建数据。
- 22项内存编排正反检查证明合法状态可用，错误HEAD/分支/基线/审核/文件变化/报告越界等会拒绝。模拟PASS仅用于检查函数，不能授权full。
- 新无上下文独立审查验证14类入口错误在0写入、0子进程前置拒绝，合法审核才能到完整门；真实R2_REVIEW逐字绑定8文件。
- 完整工程函数除根报告输出路径外与修改前一致。没有通过删门、跳过类型错误或改答案让工程变绿。

本轮把R1已修好但未完成工程验收的组件推进到可提交的工程交付状态；仍未把新语义接入真实App或正式保存。

## 分层实测数字

| 层 | 结果 | 说明 |
|---|---|---|
| 编排内存正反 | 22/22 | self-check-01，仅当前编排验证。 |
| 原定向/变形 | 72/72 | 原49项+R1新增23项，7测试/源码不变；不与全量重复相加。 |
| 旧V2内存确认 | 42/42 | 原真实capture/confirmV2/repository读回断言passed；JSON reporter未保存独立console字段清单，不伪造。 |
| 独立编排审查 | PASS | 14类错误入口探针0写/0子进程拒绝，8SHA匹配。 |
| 历史快照完整性 | 20/20 | 逐字SHA验证，换行差异不忽略。 |
| 当前依赖兼容 | 13/13 | 原357个非根条目与仅两类型增量边界；没有安装或升级依赖。 |
| 历史原封库测试 | 4/4 | 在独立历史快照内，不冒充当前旧环境结果。 |
| 前端/组件全量 | 1058通过，1跳过，0失败 | 101测试文件通过，1文件跳过。 |
| 其他当前功能 | 62/62 | server8、worker25、time-parity1、multimodal-library23、functions5。 |
| lint、两类type、Schema/时间契约 | 全部通过 | 没有改只读源码来隐藏错误。 |
| build、稳定bundle隔离 | 通过；18文件、0实验标记 | 构建仅在新临时目录，不改稳定入口或部署。 |
| 安全扫描、公开依赖审计 | 通过，0漏洞 | npm审计缓存/空配置在本轮新目录，不重装、不读密钥。 |
| 完整工程编排 | full-01一次，18层检查通过 | 日志SHA全部匹配；各层不同口径不混为一个准确率。 |
| 保护 | 870/870不变 | 原844项+R1静态21项及7源码去重，日志旧前缀不变。 |

唯一跳过项是既有src/lib/ocrLiveComponent.test.ts的真实本地Tesseract组件测试，原门一直以RUN_LIVE_OCR_COMPONENT=0关闭。本轮没有改测试或添加新skip；不据此宣称OCR质量已验收。模型识别准确率：本轮未测量。

## 证据和历史分离

- 原I1失败、R1首轮/二轮/最终结果、旧40/42、旧17、B8 FAIL全部原样保留。
- 旧R2当前环境3通过/1失败保持R2_FAIL_PRESERVED_NOT_RERUN；历史快照4/4和当前兼容13/13独立报告，不反写旧FAIL。
- 新完整结果为R2_ENGINEERING_CHECKS.json，详细日志由R2_TEMP_DIRECTORY.json绑定到本轮系统临时目录，文件SHA已核验。没有覆盖旧ENGINEERING_CHECKS或旧审核。
- R2_ATTEMPTS保留定向尝试与只读console提取缺失说明；该日志提取报错不是业务或工程测试失败，未以此扩张修补。
- 8实现最终原始字节见R2_IMPLEMENTATION_SNAPSHOT，与独立审核一致；不把工作树SHA冒充Git规范化blob摘要。

## 未解决且不在本轮范围

- 新语义实际App、正式确认/保存/刷新/导出：NOT_RUN。
- 条件三值、修订、状态、事件及时间/材料关系的产品侧展示/编辑/持久化尚需精确接入设计和另行授权。
- 真正模型语义识别、图片/文件效果、真人修改时间和商业收入均未测量；本轮工程通过不能替代。
- 正式任务编辑/执行、真实提醒、ICS、稳定接入、部署、RCO-6均未开展。

## Git与唯一下一建议

成功条件已满足后才按R2_STAGING_MANIFEST列明8实现/编排、本轮报告、短交接和追加日志。提交推送及远端精确号以实际Git回执为准，报告生成时尚未执行，不预写已推送；失败不强推或变基。

唯一建议：另批RCO-5-MAINLINE-05-SCOPE，只读确定这套新语义进入真实App确认与隔离保存的最小闭环白名单。理由是结构能承接不等于用户已经能使用，下一瓶颈是产品交接，不是继续换模型或再加词语规则。详细待授权提示词见R2_NEXT_PROMPT.md。

0外部识别/verifier/Repair/retry/模型网络/费用/密钥/剪贴板/真实库/真人/真实材料/新数据集/盲测/B10/稳定入口/部署/RCO-6。完成交付后停止，不自动进入下一包。
