# RCO-5-010-E1 本机修补关闭报告

## 结果与用途

本轮隔离组件的定向测试、独立机制复核与工程检查通过。移除词内“请”有限黑名单，改为动作位置和正向语法证明；命令证据不足时保持未知。分句与换行保留原文位置，问句不影响独立兄弟任务。名词化结果与命令解释冲突时不默认勾选。

入口在 await 前取得快照，并从原文独立重建 scope 与 candidate catalog，比较完整内容及指纹；保留旧指纹篡改对象、动作、条件或来源均被拒绝。此检查证明派生内容与本次原文一致，不代表来源身份认证；同时替换合法来源及全部派生记录属于新输入，须由正式接入层控制。

三值行动性独立于 selected；外发等义务可为 true 而不默认勾选。JSON 对象字段顺序不影响计数比较，数组顺序、缺值和稀疏数组仍严格验证。

## 验证数字

- 定向 6 文件 122/122；包含 U+4E00–U+9FFF 的 20,992 个单字 bridge 输入遍历（含4个明确允许字符的独立正例），不代表同等数量的独立语义材料。
- 独立无上下文只读审查：初次拒绝，修补后机制复核 PASS；审查者独立重跑相关三文件 108/108。
- 全量 npm test：Vitest 833 passed / 1 live OCR skipped；其他适配器/契约测试 66 passed；合计 899 passed / 1 skipped / 0 failed。
- 已见 B9 补充安全断言：3/3；当前12案例相对冻结实现期望的额外默认勾选0、内部策略违规默认勾选0。
- lint、类型检查、build、安全扫描 PASS；npm audit 0 vulnerabilities。
- 旧 B9 数据/runner/result 与 009/009A 组件保护检查19/19；旧 B9 runner未运行。
- 构建保留既有 >500 kB chunk warning；live OCR跳过不计作OCR验收。

## 历史与局限

原B9仍为FAIL。已见B9 requiresAction与冻结实现期望符合11/12；B9-07保留false/null分歧，B9-12不能用于语义准确率。旧诊断目录result/report是在E1修补前生成的产物，未覆盖；当前代码结果由只读Vitest回归验证。诊断runner仅做异步接口适配，未再次运行，也未执行B9原始一次性runner。

所有实现测试均是本机受控夹具或已见Development。没有模型、OCR、真实图片/文件、真人修改时间、浏览器或商业上线的新证据。冒号外层语境、未支持称谓、名词化歧义等会保守待确认，仍需后续新授权证据衡量召回损失。跨模型或人工独立审查未执行。

## 交付边界

模型调用/联网模型请求/verifier/Repair/retry/Secret/费用：0/0/0/0/0/NONE/0 CNY。既有Expected/freeze/dataset/checkpoint/cache与B9历史产物未改，RC.4/稳定路径未接，RCO-6未启动，未部署。建立新组件哈希清单并通过检查后，按用户授权单独提交和推送；之后停止，不创建B10、不调用付费模型。
