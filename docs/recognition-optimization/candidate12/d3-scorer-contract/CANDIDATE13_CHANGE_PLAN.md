# Candidate13 零调用变更计划

状态：`PLANNED_NOT_IMPLEMENTED_NOT_EVALUATED`。本文件不创建候选、不修改默认候选、不授权模型调用。

所有方向均来自已见D2 Development：

- false条件不得生成当前actionable任务；
- 材料、格式、地点、联系方式和说明不得冒充任务；
- 多端点取消和替代按动作、对象逐一保留，禁止跨对象合并；
- 保留依赖、完成标准、条件值与历史端点；
- 降低Forbidden，同时保持PD09类多端点召回改善；
- 不通过提高unknown或漏掉任务换取较低误报。

实施Candidate13时，先用本包24类契约夹具和额外匿名工程反例做零调用回归，再冻结候选bundle、Prompt SHA和版本。冻结必须发生在任何全新Development或Holdout Expected对实现者可见之前。

D2同一批12份来源和24个输出不得证明Candidate13提升，不得重标后冒充新验证。下一次效果评测必须使用全新匿名Development，或使用两位真实人员完成的独立Holdout；后者仍处于`WAITING_FOR_INDEPENDENT_HUMAN_LABELS`。
