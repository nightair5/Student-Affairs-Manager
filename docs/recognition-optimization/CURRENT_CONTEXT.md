# RCO Current Context

## 当前目标
- MAINLINE-REAL-INPUT-01同一完整包NOT_COMPLETE；先最小文字通路A01，全格式/完整工程/Edge随后同包完成。
- 真实App→本机读取校对→来源约束建议→主动核对确认→隔离保存找回。
- A01已发起1次模型请求尝试，0完整响应；上游处理/实际扣费未能观测。
- 约10.769秒后本地网关502，账本HALT TRANSPORT_OR_CRASH_UNKNOWN；禁止重发/继续A02。
- 保留3.30元未知预留（不是已确认消费）；模型识别准确率本轮未能测量。
- 自动选择NOT_ENABLED，不用不勾选冒充候选质量通过。

## Git与恢复
- 唯一仓库C:\Users\Winner\student-affairs-multimodal-exp。
- 分支codex/e2-multimodal-recognition-exp；禁止默认比赛目录。
- 本run起点24c80c02cf4e7b7d9e652caf935638cd2ad1f0ba；本次仅失败审计交付，最终HEAD查Git。
- 主方案56d0545fd8ffdd7b71f9feeed1ad9ac22a705dbe只读。
- 当前run mainline-real-input-01/runs/usage-resume-20260907a/。
- BASELINE含35源码/7缺失、旧证据/945保护与日志前缀；不重复实现。
- 业务源码未提交；不回切、重装、变基或套补丁。

## 当前授权
- 原41路径+Dashboard显式增量=42，职责按原计划和追加批准。
- 本次明确修P1_DUPLICATE_USAGE_RELEASE；预算模块/Node测试优先。
- 普通范围内问题连续修复；重大安全/预算/保护/重叠修改/越界停止。
- 用户另明确批准从剪贴板创建本机.env，旧禁止由此次明确覆盖。
- .env已创建，Git忽略/未跟踪；ACL仅当前用户和SYSTEM。
- Node服务端加载检查通过，未显示密钥或写入前端/日志/Git。
- 不再索要API Key；后续仅服务端--env-file=.env加载。
- 配置形状有效不等于服务商认证或模型连接成功。

## 本次已做
- 原始JSON扫描解码后属性名，重复/冲突在结算前拒绝。
- 不依赖JSON.parse覆盖值；字符串伪JSON不误当键。
- 失败保留3300000微元、halt、无settle，重开后拒绝新请求。
- 正常响应结算390微元且允许下一单元；不是全部拒绝。
- 修前53PASS/1FAIL，修后54/54；旧52断言不弱化。
- 无上下文独审PASS，预算/测试SHA见REVIEW.md。
- 审查仅覆盖登记修复，不能替代新runner/输入绑定发送复核。
- renderer用旧8通知生成PNG/JPEG/WebP/TXT/MD及4种PDF。
- 工程载体：C:\Users\Winner\AppData\Local\Temp\real-input-carriers-e1da8dbf1e8a4f5a9732c02f6469c24f。
- 实际Edge已完成B01–08本机读取；B01/02 OCR错字，B03/04空，B08文本层缺新要求。
- 原始结果保留；实际App逐键校对并保存上述5项，B05/06/07原读取保留。
- 这不是OCR正确率100%或真人编辑时间；已见材料不称盲测。
- 原origin 127.0.0.1:6631；原run real-input-d030c507-3f7c-4a2c-a511-8cd5bd534862。
- 原库rco-mainline-01-02-i1-real-input-d030c507-3f7c-4a2c-a511-8cd5bd534862；未清/换库。
- 当前隐藏回放服务PID11344，无env或外部模型；CUA实际用户标签763114724。
- 启动写入被无条件拒绝已修为仅空记录事务初始化，拒绝覆盖；不是Edge拦截导致的产品错误。
- 独立新仓储读回16来源/0任务/0jobs；实际注入Source保存失败前后全对象相等。
- runtime23/23、Node入口6/6、评分22/22分别记录；入口独审PASS_SCOPE_ONLY。

## 剩余工作
- 42路径已存在，不等于整包完成；完整工程/最终浏览器/模型仍待完成。
- 实际App已mount；runner目前只绑定，不读Key/不派发，checker仅保护编排。
- 三个真实下载已校验；STATE固定16单元，8正常对照、评分26/26及首调复核通过。
- 下载文件real-input-local-evidence*.json；基准必须来自显式独立读库，不自算两次冒充比较。
- 输入/候选/参考/评分依赖/请求body首调前绑定，不能事后换输入。
- 新首调独审PASS_SCOPE_ONLY，42源码清单SHA f9b386d19f6e703576238c04e8d16085bbbb8786bd20243c51523d515b7302c1。
- A01无有效raw/usage，其他15单元NOT_RUN；不要再次执行--paid或重建账本解锁。
- 下一优先是零调用补安全错误分类/本机传输配置诊断，不猜Key无效、不做额外试连接。
- 新调用需要先处理明确原因并获准恢复规则，原A01预留和失败必须保留。
- 公开config禁止Key；不得用额外认证smoke消耗未计数请求。

## 预算与质量
- deepseek-v4-flash-vision-exp，Responses固定接口。
- temperature0、reasoning.effort none、streamfalse、输出8192。
- A8+B8、可选新候选C8；单并发，总≤24请求/≤10元。
- 派发前结算上界+未知完整预留+新3300000微元≤10000000。
- 完整合法绑定usage按高峰人民币价结算；未知/超时/崩溃不释放。
- verifier/Repair/retry均0；价格/模型/协议/usage异常立即停发。
- 首调前新鲜核验公开价格参数，官方文档不代表账号认证。
- 已见工程只作诊断回归，不称新盲测/真实材料准确率。
- 首次候选质量、修后保存、浏览器与费用分开报告。

## 保护和交付
- 原946仅Dashboard获准例外，其余945逐字保护。
- 945聚合df68e4eda47f664f9f0fbf218a74687117ac3cc9d2c14f5ee6e9965cd2c28394。
- 起始日志269512字节/SHA3913d2202d2f0910e1c16b78c4f51991606203bb6cd4470cd3b4d24f43d9c12a。
- 旧16+12+20等静态证据与各日志前缀只读，BASELINE列明。
- 全局Schema/repository/capture/validator/confirmationV2/domainCommit/AST/依赖只读。
- 旧Expected/freeze/dataset/checkpoint/cache/runner/result/40/42/17/FAIL不改。
- 不接用户库/稳定入口，不建数据集/盲测，不部署/RCO-6/采真人。
- 日志新文件；变化链路重验，未变证据按SHA复用。
- 成功精确业务提交推送核远端；失败只审计、代码留现场。
- 完成后停止，不自动扩模型/预算或进入下一包。
