# RCO Current Context

## 当前工作与边界

- 阶段：MAINLINE-05完整用户闭环工作包的范围准备，不再按补丁拆阶段。
- 本次只做只读审计与文件白名单；产品实现必须等用户确认。
- 唯一仓库：C:\Users\Winner\student-affairs-multimodal-exp。
- 唯一分支：codex/e2-multimodal-recognition-exp。
- 产品源码基线：07cbd4cf9d3f9797949ead0799965960f90b2727，本机/远端已实时核验。
- 本次开始工作区干净，MAINLINE04的870项保护与8实现SHA全部匹配。
- 当前计划：mainline-05/PLAN.md。
- 唯一精确文件清单：mainline-05/IMPLEMENTATION_WHITELIST.json。
- 实施白名单尚未获得用户确认，不能把文档交付当作代码实施授权。
- 模型识别准确率：本轮未测量。

## 已有成果，不重复执行

- MAINLINE04工程交付已在07cbd4c；审计mainline-04-i1/R2_AUDIT.md。
- 其实现快照R2_IMPLEMENTATION_SNAPSHOT.json，分层结果R2_ENGINEERING_CHECKS.json。
- 新语义条件三值/覆盖/修订/完整关系已有组件，定向72项通过。
- 旧等价V2真实内存确认42/42；新语义实际App/正式保存仍NOT_RUN。
- 旧40/42、旧17、B8 FAIL、历史环境3/1与失败attempt原样保留。
- 本包不换模型、不重造契约、不新建数据。

## MAINLINE-05唯一交付目标

- 真实App录入→来源先存→完整建议→逐键编辑/明确保存→确认→原子隔离存储。
- 刷新后从任务中心/首页/日历/详情找回正确任务、条件/修订/事件/材料/时间/依据。
- 复用原8类人工通知和已登记映射；B8仅已见只读诊断，不能补成正确答案。
- 用户最终确认；投影、默认selected和原始响应不能直接写正式任务。
- 真无日期可确认；条件未知不改false；有效条件真/新要求/独立兄弟必须有成功正例。
- 保真/安全数字是工程验收，不是模型准确率或商业效果。

## 一次性待批准范围

- 7个已有文件：App、DraftReviewPanel、TaskDetailPanel、EventDetailPanel、CalendarPage。
- 另含mainline02/runtime.ts和domain/v2/domainCommit.ts的显式增量，旧默认不变。
- 18个新增源码/测试/脚本全部逐路径列入JSON；未获准的实际文件不得新增。
- 特别需确认：domainCommit既有冻结公共文件新增语义计划入口，原函数行为不改。
- 特别需确认：v8容器中的mainline05-semantic-state-1版本扩展。
- 扩展必须联合全图校验，实体/关系/确认历史同一事务；不是任意JSON存档替代语义承接。
- 全局Schema/repository/validators/capture/confirmationV2/MAINLINE04/依赖只读。
- 新空库使用既有合法前缀加mainline05和随机ID；固定本run的origin/库，不碰旧库。
- 实施时保护例外仅批准7路径，旧快照/日志前缀和其他保护始终不变。

## 连续执行与验收

- 用户确认后同一包连续实现→定向修补→独立审查→完整工程→真实Edge→Git。
- 普通范围内返修不再另开阶段；超白名单/职责、保护变化或重大安全问题停止申请。
- PLAN的J01–J12是完成线；逐键操作、存储读回、真实下载不可用DOM提示替代。
- 原42字段不退化，新事实/关系保真100%，错默认选择/重复/覆盖/丢失等为0。
- 共享实体/部分确认、故障/过期/重复、纯信息/暂缓和无日期找回均在本包。
- 新checker预先纳入授权，绑定本包基线/实际审核SHA，历史与当前依赖检查分层。
- 正式任务编辑/执行、真实提醒/ICS、跨来源自动修订、稳定接入与部署不在本包。

## 交付和接续

- 本次文档复核与检查：mainline-05/REVIEW.md、CHECKS.json。
- 文档实际Git交付号以提交和远端回执为准；实施从包含本批准计划的最终文档提交恢复。
- 不回切到旧源码基线，不执行旧一次性runner，不套失败补丁。
- 长日志留新目录；短交接只记录当前障碍/下一动作，不重读RCO历史。
- 必要Git/公开依赖审计及获准本机回环网络外，模型网络/费用/密钥/剪贴板/真实库均0。
- 当前唯一下一步：用户确认PLAN第9节和精确白名单，随后执行同一个完整工作包。
- 完成本包后停止，付费模型、真人、真实材料和部署仍分别授权。
