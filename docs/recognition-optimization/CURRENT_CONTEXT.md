# MAINLINE-REAL-INPUT-01 当前交接

## 当前结论
- 已修复共享材料导致兄弟任务截止时间串联的问题。
- U11/V02假性MULTIPLE_DEADLINES消失。
- candidate09仍NOT_ADOPT，candidate03继续为可靠基线。
- 本轮0模型调用、0新增模型费用。
- 6632已更新；当前浏览器实际下载/读库仍NOT_RUN。

## 仓库
- 仓库：C:\Users\Winner\student-affairs-multimodal-exp。
- 分支：codex/e2-multimodal-recognition-exp。
- 本轮起点：944a2f3aa493432ce75a1b7a0d63c632146ed620。
- 提交和远端以本轮最终Git核验为准。
- 不回切、不强推、不自动变基、不动生产。

## 根因
- 任务A和B共同关联材料M。
- 时间TA只属于A，TB只属于B。
- 旧遍历会走A→M→TB和B→M→TA。
- 每个任务因此看见两个截止，触发MULTIPLE_DEADLINES。
- 模型在U11/V02的时间归属并未造成这次串联。

## 修复
- semanticComposer只传播明确属于当前任务的时间。
- 仅属于材料的时间仍可随材料保存。
- semanticState核对、编辑和正式投影使用同一规则。
- factCorrections不再重新引入兄弟任务时间。
- 真正同一任务多截止仍阻止确认。
- 共享材料的多任务关系完整保留。

## 零调用回放
- U11-09两个任务各自只见一个时间。
- U11独立登记任务可保存为2026-09-17T14:00。
- U11仍有模型材料数量/属性和条件状态错误。
- V02-09首项可保存为2026-10-10T12:00。
- V02第二项模糊时间与未知依赖继续待核对。
- U01/U10/V04正常对照不退化。
- 两份代表性来源可保存独立正确任务由0/2变为2/2。
- 该变化是本机确认转化提升，不是模型准确率提高。

## 产品接线
- 6632增加U11-09、V02-09历史回放。
- 页面标明candidate09不采用、模型关闭。
- 模型POST继续关闭，不改变默认候选。
- 完整库导出改成先准备、再显示下载链接。
- 独立仓储工程证据也显示明确下载链接。
- 不再依赖异步结束后的程序化link.click()。

## 工程证据
- 定向时间/确认/纠错90项通过。
- acceptance及回放79项通过，fetch未调用模型。
- 全Vitest 1381通过、1跳过、0失败。
- app/node类型检查通过。
- lint通过，保留4条既有warning。
- 识别契约、时间AST、server、Worker、Functions通过。
- Vite和6632预览临时构建通过。
- 根.env未读取。
- 旧RCO冻结SHA不匹配为历史非适用项，未修改。

## 本地运行
- 地址：http://127.0.0.1:6632/。
- `/api/status`当前HTTP 200。
- 首页、browser.js、U11/V02记录均HTTP 200。
- 状态含6份历史记录。
- modelCallsEnabled=false。
- candidateAdopted=false。
- 服务更新未清除浏览器IndexedDB。

## 真实浏览器边界
- 本轮官方控制仍无法连接，因此不重复诊断。
- 未重新读取当前浏览器中的4项历史任务。
- 未在修复后完成U11/V02实际点击确认。
- 未取得两份新的实际下载文件。
- 代码和HTTP验证不能替代当前IndexedDB/下载证据。
- 上轮4任务证据仅作历史证据，不冒充本轮复验。

## 下一动作
- 无需立即再调用模型。
- 若控制恢复，只需完成一次U11/V02页面确认和双下载比较。
- 若开启下一模型轮次，只针对U11材料数量/属性和条件状态。
- 不创建candidate10，不新增数据集或请求。
- 详见shared-material-time-20260913a/AUDIT.md和CHECKS.json。
