# MAINLINE-REAL-INPUT-01 当前交接

## 当前结论
- candidate03仍是可靠基线，candidate09保持NOT_ADOPT。
- 本轮0模型调用、0新增费用。
- 20份candidate03历史回答已一次零调用回放。
- 正确事实到工程保存的程序阻碍已收敛为0。
- 当前浏览器自动控制仍失败，不循环诊断。

## 仓库
- 仓库：C:\Users\Winner\student-affairs-multimodal-exp。
- 分支：codex/e2-multimodal-recognition-exp。
- 本轮起点：5519b7a540d1cf803fbae54b8b443266f454f5ab。
- 最终提交和远端以本轮Git核验为准；不回切、不强推、不自动变基。

## 20份零调用回放
- 来源：20份。
- 建议任务：31条。
- 技术上写入23条；事实正确且正确保存20条。
- 需改模型事实3条；合理待处理3条；正确不创建待办5条。
- 纯信息来源：2份。
- 至少保存一条正确任务16份；意外程序阻碍0条；模型调用0次。

## 核对负担
- 28份独立材料事实各保存一次核对，不按关联任务重复核对。
- 准备状态无证据时统一保留unverified。
- 2条模糊日期由显式“日期待定”接受后保存。
- 真正无日期任务正常保存。
- 原答、首次建议与最终操作分别保留；重复回放不增项。

## 仍需纠正的模型事实
- U11把未知前置条件误判为成立。
- U11多造“准备两个密封袋”任务。
- U11材料数量/属性归属仍需核对。
- U12给“保存最终版展签”增加错误依赖并错归A6材料。
- U08纯取消被描述为替代关系。
- 这些错误已在模型原答中，不是转换丢失。

## 合理保持待处理
- U06领取投影仪：候补条件未知。
- V02安装展架标签：依赖与具体日期待核对。
- V05领取配电板：条件未知。
- 条件未知不会被改成成立；取消或条件未成立任务不会创建待办。

## 本轮代码变化
- semanticView给条件、取消、时间、坏引用等阻碍自然语言解释。
- 技术码不再是用户唯一说明。
- 没有放宽确认、事务或来源校验；acceptance新增U06/U07正反例。
- 两类阻碍仍无法被选择和确认。
- 失败操作保持工作区逐对象不变。

## 真实存储证据
- 6632先前真实页面保存“填写送样登记单”。
- 材料“送样登记单”为required=true/status=unverified。
- 时间为2026-09-17T14:00。
- 刷新后曾在任务、日历和详情找回。
- 独立仓储JSON与完整库JSON为两个实际下载文件，workspace逐对象完全相等。
- workspace SHA为7a3b21a3fcbf02c19c5ae56cf3558cf4ea8aedacc214e6779b4100ecfc2b8c7f。
- 该下载不含第二条替代旅程，不能扩大结论。

## 当前浏览器边界
- 本轮官方连接只尝试一次。
- 约25秒后返回nodeRepl.fetch request failed。
- 当前自动页面操作为NOT_RUN。
- 不重装插件、不重启、不循环刷新；Q01/Q07证据只作历史复用。
- HTTP、内存回放不冒充本轮浏览器点击。

## 工程证据
- app/node类型通过。
- lint通过。
- Vitest 1382通过、0失败、1跳过。
- 识别契约、时间AST、server、Worker、Functions通过。
- Vite及本地preview构建通过。
- 6632状态HTTP 200、模型关闭。
- 根.env未读取，模型调用0。
- 旧RCO历史锁文件SHA差异未弱化。

## 下一动作
- 不立即创建candidate10。
- 唯一模型问题：复合通知中的事实归属。
- 重点是动作、材料、条件和取消对象归属。
- 证据集中在U11/U12，错误已在原答。
- 当前结构能承接正确事实，无需再改仓储。
- 下一次应固定candidate03与wire，只改变模型能力；未来需单独授权。
- 详见baseline-conversion-20260914a/AUDIT.md。
