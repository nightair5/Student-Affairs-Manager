# RCO-5-007-B4 首次盲测对抗审查

## 裁定

`B4_ORACLE_QUALITY_PASS / ENGINEERING_GATE_FAIL / PAID_MODEL_BLOCKED / NO_PROMOTION / DO_NOT_LAUNCH`

B4 在 commit `fc2aeb78f8d01f06a4c14be6c31bfdb91073d5be` 先冻结并推送，再只运行一次。模型、网络、Repair、retry 和 Secret 均为 0/0/0/0/NONE。B4 现在已见，不能再用于修改 P2 后声称未见泛化。

## 通过内容

- 16/16 合同可评分，23 个参考任务全部按动作+对象匹配，Task P/R/F1 均为 100%。
- `requiresAction` 16/16，任务边界 16/16，安全默认 Recall 100%，Forbidden=0。
- Complete Task Case 15/16=93.75%，Major Correction 1/16=6.25%，超过预登记的 80%/低于 10% 主线门槛。
- “未接通/未熄灭”完整条件命题、动作 surface 保真、对象中的人员词、显式群体主体和一种新修订结构没有破坏安全门。

## 唯一未完整案例

B4-07 使用“此前通知……停止执行”。P2 的结构虽已分离修订状态，但确定性入口仍未把“此前通知”识别为历史来源，因此保留了一个未选中的旧外发任务，语义字段错 4 项。它没有造成危险默认操作，也没有影响当前新任务或 `requiresAction`，但用户仍需删除/忽略旧任务，属于真实修改成本。

这说明“旧/新状态分层”已经具备，但开放语言中的修订触发识别仍不是完全解决。后续付费候选若模型只提供 scope/action/object、而本机完全拒绝修订语义，这个缺口仍不会被模型修复；下一付费协议应单独统计 revision 语义和陈旧任务残留，不能只看总 F1。

## 结论边界

- 这是匿名合成、Codex 单作者 Development challenge，不是独立人工 GT、真实材料或图片/文件正确率。
- Expected-derived 完美锚点排除了上游模型错误，因此只证明 P2 本机层在这批新结构上跨过最低门。
- 首次质量门本来只允许另行申请固定模型、调用次数和人民币上限的付费验证；但随后全量 `npm run build` 发现冻结的 B4 数据测试存在 TypeScript 空元组类型声明错误。因为该测试已进入盲测前 freeze，预登记规则要求任一冻结哈希变化即 STOP，不能事后修复再把同一 B4 宣称为有效未见通过。
- 因此最终总体裁定降级为工程门失败、付费模型继续阻塞。下一步只能另行授权“类型夹具修复 + 已见 B4 回归 + 全新 B5 首次门”；不得重跑 B4 冒充未见。
