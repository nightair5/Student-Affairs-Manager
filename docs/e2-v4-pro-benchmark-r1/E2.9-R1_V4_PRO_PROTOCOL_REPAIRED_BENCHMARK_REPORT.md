# E2.9-R1 V4 PRO PROTOCOL-REPAIRED BENCHMARK REPORT

最终状态：**V4_PRO_SCREENING_V2_FAIL**
模型质量结论：**NOT AVAILABLE**。失败来自冻结 Screening Harness 对纯信息样例的协议校验缺陷，不是已证明的 Pro 质量失败；按预注册规则不得修补后重跑。

## 1. 上一轮阻断原因

上一轮存在 Flash 401 导致配对不完整、Smoke 缺失冻结 Ambiguity 标签、Selection 缺失 `information_only` 与 `prompt_injection` 覆盖，因此 S3、S4、Blind 均未运行。

## 2. 协议修订

协议升级到 `e2-9-v4-pro-reduced-protocol-2.0.0`：增加 6 次非评分认证探针、S0 原始证据、可复算 bundle hash、5-case Smoke v2、8-case Screening v2、24-case Selection v2、生成/评分防火墙、单次上游调用、路径匿名评审与机器 Gate。冻结提交为 `578aad5`；唯一识别变量为 `modelName`。

## 3. Auth Readiness Gate

PASS。Flash 3/3、Pro 3/3 均为 HTTP 200；requested/returned model、fingerprint、usage、JSON 对象均有效。Readiness checkpoint SHA-256：`019f3f1feba3605e2e250da144665ea946af78b7ee79bcd5347486803e6b9550`。

## 4. Secret activation chronology

- Preview code version：`c892c6a9-57b1-48a4-9a0a-00aad885446a`，创建于 `2026-08-12T17:44:59.789576Z`。
- Secret Change version：`8e685729-fdf0-4c91-bc44-f1d90fc90c44`，activeAt `2026-08-12T17:45:39.449Z`。
- Readiness：`2026-08-12T17:46:27.112Z` 至 `17:46:36.342Z`，晚于 Secret activeAt。
- 正式观察中 HTTP 401 = 0。

## 5. S0 raw evidence hashes

原始内容仅保存在 Git ignored cache。S0 checkpoint 为 `91224f…554d`；`/models` 为 `4639cc…b68dc`；Flash minimal completion 为 `de7368…4f23f`；Pro minimal completion 为 `558239…33bce`。两模型 identity、fingerprint、usage 与响应头均有记录。

## 6. Bundle hash algorithm

使用 SHA-256、UTF-8、slash-normalized path、JavaScript UTF-16 code-unit 路径升序、CRLF/CR→LF、递归排序 JSON object keys、数组保序、字节长度 framing 与固定分隔符。算法、输入清单、单文件 hash、bundle hash 及复算测试均已提交。主要 bundle：Schema `4118ad…aa7b`，Scoring `2ac2cf…8798`，Pipeline `d61b0c…13a`，Datasets `76c03f…ffb3`，Protocol implementation `10b777…a05`。

## 7. smoke-v2

PASS，10/10 完整。覆盖多 Task/Material、多 TimePoint/Event、relative/vague time + Ambiguity、pure-information、Prompt Injection；fallback=0、invalid JSON/schema=0、auth failure=0、安全失败=0。Smoke checkpoint：`fd7593…2813`。Smoke 不参与质量结论。

## 8. screening-v2 coverage

8 条 EXPOSED SCREENING 覆盖 `complex_multi_stage`、`multi_task`、`multi_timepoint`、`multi_material`、`event_task`、`ambiguity`、`pure_information`、`prompt_injection`；选择仅依据冻结结构标签。source-only canonical SHA-256 为 `0cf63c…60fb`。

## 9. 8-case results

16/16 上游请求均 HTTP 200、执行身份正确、fallback=0、transport failure=0；Harness 仅接受 14/16。失败对为 `e2-gen-14-2`：Flash 与 Pro 的最终规范化 result 都是 0 个业务实体、1 条 evidence、`requiresAction=false`，符合纯信息语义，却因 Screening source record 不携带 Smoke 专用 `pure_information` 角色而同时触发 `BASIC_CONTENT_EMPTY`。两臂原始模型 JSON 各含 2 个 TimePoint 与 1 个 Event，因此“0 实体”仅指规范化 result。此外 checkpoint 顶层错误地保留 `gateStatus=COMPLETE`；下游 scorer 的完整性检查仍拒绝评分。原始 checkpoint SHA-256：`52b315…d3d7d`。

## 10. 24-case selection

NOT RUN。Screening 未形成 8 个可评分完整对，按 Gate 立即停止；未调用剩余 16×2。

## 11. strict metrics

NOT RUN。不得以 7 个残缺对子集推断 Task、Evidence、Severe Error 或其他质量指标。

## 12. semantic metrics

NOT RUN。未创建路径匿名 packet，未读取 expected 做正式评分。

## 13. user-impact results

NOT RUN。

## 14. pair wins/losses

NOT RUN。无 labels、无 reveal key、无配对胜负。

## 15. latency/tokens

以下仅为真实调用成本/性能观测，不是质量评分。

| 阶段 | 模型 | HTTP 200 | 平均延迟 | P50 | P95 | Input | Output | Total |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Smoke | Flash | 5 | 9,025 ms | 6,338 | 14,473 | 11,874 | 8,489 | 20,363 |
| Smoke | Pro | 5 | 15,618 ms | 9,932 | 28,640 | 11,874 | 8,466 | 20,340 |
| Screening | Flash | 8 | 10,636 ms | 10,666 | 15,713 | 19,008 | 15,583 | 34,591 |
| Screening | Pro | 8 | 15,690 ms | 11,070 | 23,186 | 19,008 | 14,631 | 33,639 |

正式 Smoke+Screening 中 Pro 平均延迟约比 Flash 高 56.36%；总 Token 低 1.77%。总 completion 调用 34 次（Readiness 6、S0 2、Smoke 10、Screening 16），另有一次 `/models`；Selection 与 Blind 为 0。

## 16. selection decision

`V4_PRO_SCREENING_V2_FAIL`。这是协议完整性早停，不支持 `V4_PRO_NOT_SUPPORTED`，也不支持候选晋级。

## 17. candidate manifest

NOT CREATED。Selection Gate 未运行，禁止 Candidate Freeze。

## 18. blind protocol

预注册了 Candidate Freeze 后才创建 24 条全新 Blind 的边界；本轮未进入该阶段，也未查看或创建未来 Blind expected。

## 19. blind results

NOT RUN，0 次调用。

## 20. engineering tests

- `npm ci` PASS，0 vulnerabilities；lint、typecheck PASS。
- UTC 与 Asia/Shanghai 两轮 `npm run test` 均 PASS：50 test files / 213 Vitest，以及 Node 15、Server 8、Worker 34、Functions 5。
- 单独 Server suite 首次因随机端口落入 Fetch 禁止端口出现 `bad port`（7/8）；保留该现场，立即单独重跑 8/8 PASS。两轮完整 suite 中也均为 8/8 PASS。
- 独立 Worker 34/34、Functions 5/5、build PASS。
- `security:scan` PASS（421 files），`npm audit --audit-level=high` 为 0 vulnerabilities，`cloudflare:check` PASS。

## 21. security cleanup

Preview flag 已恢复 `false`；带原 bearer 与无 bearer探测均返回 404；短期 `E2_V4_PRO_BENCHMARK_TOKEN` 已删除，Preview secret list 只剩既有 `DEEPSEEK_API_KEY`。禁用代码 deployment 为 `1b095ec8-1878-4c13-95a6-e6bc358a98af`；删除 secret 后版本为 `fa5b793b-57eb-4923-a7c5-16b8f901bfb9`。Production 当前仍为 `3b6d6ba2-e21f-495c-80e4-c4bac62366be`（2026-08-08），未部署 `student-affairs.site`。

## 22. independent audit

`FAIL（协议完整性）`，且明确为 fresh same-family provisional，不是完全独立审阅。审计复算 canonical manifests、4 个 checkpoint、104 项 observation provenance、延迟/Token 与清理状态均匹配；确认下游正确 fail-closed，故最终状态和 `qualityConclusion=NOT_AVAILABLE` 不变。

审计新增的高严重度 lineage finding：13/13 个 Pro observation 的 `execution.requestedModel/returnedModel` 与 fingerprint 都证明实际调用 Pro，然而 raw output 与规范化 `result.modelName` 全部错标为 `deepseek-v4-flash`。这不是 upstream fallback，但任何依赖 `result.modelName` 的归因均不可信。审计还指出全局 no-rerun 缺少服务端 append-only ledger、阶段前置条件与 Gate hash 关联未被 runner 完整强制、protocol bundle 未覆盖完整部署链。

## 23. files changed

新增 R1 实验计划、manifests、hash contract、source-only generation harness、S0/readiness endpoint、评分/盲评/合并/Gate 脚本、Worker 测试、匿名 run summary 与本报告；实验期 Preview flag 的 enable/disable 分别单独提交。未改 Golden/Holdout/Development expected、Workspace v8、Repository、Migration、DomainCommitPlan 或正式 Capture/Commit 链路。

## 24. remaining risks

必须在新协议版本中先修复并测试：纯信息 role 传播、checkpoint 状态机、Pro `result.modelName` lineage、阶段前置条件、服务端 append-only no-rerun ledger、Gate 输入 hash 关联和完整部署链 hash 覆盖；不得复用本轮 label。修复后仍需从新 label 重新预注册；本轮输出不得选择性并入未来筛选分数。Pro 的质量收益、Selection、Blind、Browser A–J、成本并发与灰度均未知。

## 25. E2/E3/Production status

- E2：**READY WITH BLOCKERS**（工程与隔离执行能力具备；模型选择证据仍被协议缺陷阻断）。
- E3：**NOT READY**。
- Production：**NOT READY**。
- Production 默认模型与正式路径：未改变。

STOP：不进入 E3/E4，不部署 Production，不部署 `student-affairs.site`。
