# Trace limitations

This directory preserves the final audit verdict, reviewed freeze and reproduced counterexample classes. It does not contain, reconstruct or claim private internal reasoning or a complete chain-of-thought trace.

The reviewer was a fresh task from the same model family, so the conclusion is provisional. It did not access protected inputs, `.evaluation-cache`, Secrets, real material, model endpoints or deployment. Protected-input hashes are checked separately by the primary task.
