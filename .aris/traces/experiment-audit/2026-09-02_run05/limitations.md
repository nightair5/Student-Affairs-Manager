# Trace limitations

This directory preserves the available final audit verdict and counterexample classes. It does not contain, reconstruct or claim to contain private internal reasoning or a complete chain-of-thought trace.

The reviewer was a fresh task but from the same model family, so the review is provisional. It did not access protected inputs, `.evaluation-cache`, Secrets, real material, model endpoints or deployment. Protected-input hashes are checked separately by the primary task.
