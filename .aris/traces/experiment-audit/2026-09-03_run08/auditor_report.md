# RCO-5-003 facts-1.6 fresh audit

**Verdict:** `FAIL / REJECT_CANDIDATE / NO_PROMOTION`

All four frozen hashes matched and exact character slicing worked, but a minimal endpoint envelope still allowed the ledger to omit proposition-level qualifiers. Reproduced selected errors included `请勿`, withdrawn notices, question-form required material, tentative locations, later deadline/format/event-time corrections, an event end mislabeled as a start, an independent action laundered into a task constraint, bounded quantities compressed into exact values, duplicated entities, completed third-party actions, non-concrete locations, contradictory source classification and incompatible task time roles.

The 38 registered tests passed but were classified only as `contract_fixture / simulation_only`. Shared-schema composition, evidence offsets, required user confirmation and stable-path isolation passed structurally. The candidate was rejected and superseded; `RCO-G5 QUALITY NOT_RUN`, `RCO-6 BLOCKED`, `NO_PROMOTION`, and `DO_NOT_LAUNCH` remained unchanged.
