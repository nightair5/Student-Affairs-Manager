# 学生事务管家 PRD

> 版本：v0.4
> 状态：P2 服务接入基础实施中
> 最后更新：2026-08-01

## 1. 产品概述

### 1.1 产品定位

学生事务管家是一款面向大学生的桌面网页端个人事务工作台。它将散落在文件、图片或扫描件、老师消息、消息截图、网页链接中的通知统一转化为可确认、可修改、可追溯的任务，并进一步组织为日历、材料清单与提醒。

产品不替用户做最终判断。自动识别、分类和优先级均为建议，用户拥有最终控制权；任何修改都应保留历史记录与来源依据。

### 1.2 核心问题

- 信息入口分散，重要通知容易被聊天记录和文件淹没。
- 截止日期、材料要求与下一步动作常隐藏在长文本或图片中。
- “知道要做”不等于“知道现在先做什么”。
- 自动工具可能识别错误，用户需要低成本确认、修正和追溯。

### 1.3 产品目标

第一版建立完整但克制的核心闭环：

1. 用户从任意常见入口提交信息。
2. 系统给出结构化识别建议及原文依据。
3. 用户确认或修改。
4. 任务进入统一任务中心和日历。
5. 首页聚焦最多三项最该做的任务。
6. 用户可查看材料、风险、提醒设置与修改历史。

### 1.4 非目标

MVP 不承诺：

- 微信正式消息推送或绕过平台授权。
- 完整 OCR、邮件投递、网页定时抓取等生产级后端集成。
- 全自动替用户提交报名、作业或审批。
- 覆盖所有学校教务系统。
- 复杂多人协作与组织权限。

上述能力可先以清晰的产品入口、状态和演示数据表达，不伪装为已接通。

## 2. 用户与场景

### 2.1 目标用户

- 同时处理课程、比赛、保研、社团和老师任务的本科生或研究生。
- 经常从微信群、邮件、网页、PDF 和截图接收通知的学生。
- 希望系统帮忙整理，但不愿把决定权交给黑盒算法的用户。

### 2.2 核心场景

#### 场景 A：老师消息转任务

用户粘贴一段老师消息。系统建议分类为“老师任务”，提取截止时间、交付物、下一步动作和预计耗时，并高亮对应原文。用户修正日期后确认进入任务中心。

#### 场景 B：比赛通知转材料计划

用户上传通知文件或截图。系统识别报名截止、作品提交截止、所需材料和注意事项，生成任务与材料清单，并提示尚缺材料。

#### 场景 C：首页决策

用户打开产品，不浏览全部待办，也能看到最多三项关键任务，理解“为什么是它”“下一步做什么”“大约需要多久”。

#### 场景 D：回看与纠错

用户发现自动分类不准确，修改分类、优先级或日期。系统保存修改，并允许回看原始依据和历史版本。

## 3. 产品原则

1. **建议而非裁决**：所有自动结果必须可编辑、可拒绝、可追溯。
2. **先行动，再管理**：首页首先回答“现在做什么”，而不是展示系统拥有多少功能。
3. **来源可解释**：日期、材料和任务都应能回到原文依据。
4. **风险显性**：紧急、逾期、缺材料、信息待确认必须直观可见。
5. **统一入口**：输入来源可以不同，确认后统一进入任务中心。
6. **复杂能力渐进呈现**：第一版先跑通核心闭环，再接真实 OCR、邮件和监测服务。

## 4. MVP 信息架构

### 4.1 全局导航

- 今日：最多三项关键任务、风险摘要、快速录入。
- 任务中心：全部任务、筛选、状态与批量浏览。
- 日历：截止时间与计划开工时间。
- 文件库：输入来源与原文依据。
- 项目档案：比赛、保研、课程等项目化成果沉淀。

### 4.2 首页

首页必须在首屏完成以下表达：

- 今日日期与简短状态提示。
- 最多三项关键任务。
- 每张任务卡包含：
  - 任务名
  - 分类
  - 截止时间
  - 预计耗时
  - 下一步动作
  - 紧急、逾期、缺材料或待确认等风险
  - 优先级理由
- 快速录入入口。
- 一周节奏或近期截止概览。

### 4.3 统一录入与确认

支持的入口表达：

- 粘贴老师或群聊消息。
- 上传文件、图片或扫描件。
- 粘贴网页链接。

识别结果需进入确认面板，字段包括：

- 标题
- 建议分类：比赛、保研、课程、老师任务、其他
- 截止日期与时间
- 下一步动作
- 任务描述
- 所需材料
- 注意事项
- 预计耗时
- 优先级
- 提醒时间与渠道
- 来源摘要与对应依据

当一份输入中出现多个分别对应不同事项的时间点时，系统必须生成多条独立的待确认任务，不得折叠为一条笼统日程：

- 确认页显示识别出的任务总数。
- 每条任务分别保留标题、截止日期与时间、说明、预计耗时和对应原文依据。
- 支持多日期，以及同一日期下的多个时间点。
- 用户可以逐条编辑、删除，或一次确认剩余全部任务。
- 多条任务可以关联同一个来源，但任务字段与修改历史彼此独立。

MVP 可以使用前端演示识别逻辑，但界面必须明确标注“识别建议”，避免暗示真实 AI 服务已接通。

### 4.4 任务中心

- 按状态、分类、风险筛选。
- 支持查看任务详情。
- 支持编辑分类、日期、内容、耗时、优先级与提醒。
- 支持标记完成。
- 展示材料完成度和来源入口。
- 保存并展示修改历史。

### 4.5 日历、文件库与项目档案

MVP 提供可导航、可理解的桌面页面骨架：

- 日历按日期展示截止事项和开工建议。
- 文件库展示来源类型、识别状态和关联任务。
- 项目档案按项目归集任务、材料与成果。

### 4.6 本地持久化

- 已确认任务、手动编辑、材料状态、修改历史、来源和提醒设置必须在刷新或关闭后重新打开时恢复。
- MVP 使用带版本号的浏览器本地工作区快照，并兼容迁移旧版分散存储键。
- 读取失败或旧数据缺字段时执行安全迁移，不用演示数据覆盖可恢复的用户数据。
- 当前数据范围限定在同一设备、同一浏览器和同一站点地址；清除浏览器站点数据会移除本地工作区。
- 跨设备同步属于后续账号与后端能力，不得暗示当前已经支持。

### 4.7 P0 可信确认闭环（已实现）

- 每次录入先创建 `Source` 与 `ExtractionDraft`；关闭录入或审核面板不丢失待确认草稿。
- 不同日期、时间、事项和材料必须保留为独立草稿项。用户可逐项编辑、拒绝、确认，或确认全部剩余项；只有确认项可创建任务。
- 任务关联来源、项目与材料；任务字段修改和材料勾选保留可追溯历史。风险与优先级理由由纯业务规则给出，仅作为建议。
- P0 使用 IndexedDB 保存结构化实体数据，并安全迁移旧 localStorage 工作区。支持 JSON 导入导出，清空本机数据必须应用内二次确认。
- 文件本体、二进制图片和大附件不写入浏览器工作区；链接不代表已抓取网页内容。OCR、网页抓取、自动邮件、微信授权和跨设备同步均不属于 P0。

### 4.8 P1 本地增强（已实现）

- TXT、Markdown 与带文本层 PDF 可在浏览器本机读取；文件本体不保存、不上传。图片与无文本层扫描 PDF 会明确提示 OCR 未接通，并要求用户人工补充原文。
- 桌面端支持拖拽文件；窄屏设备提供拍摄或选择截图入口。所有来源仍先进入 `Source → ExtractionDraft` 待确认链路。
- 浏览器通知支持权限检查、显式授权与真实本地提醒，但只保证页面保持打开期间调度。邮件仅保存计划、不会发送；微信仍未授权接入。
- 项目包含可追溯里程碑；确认任务自动形成节点，任务标题、截止时间与完成状态会同步到对应节点，用户也可手动添加和勾选节点。
- 重复来源检测基于本地相似度规则，只显示人工核对建议，不自动合并、覆盖或删除来源。
- 用户可录入每周固定课程；本地规则在 08:00–22:00 内建议避开课程的连续开工时段。该建议不会读取系统日历，也不知道用户的其他安排。
- IndexedDB 工作区升级为 schema v4，并安全迁移 P0 schema v3；课程、里程碑和重复核对状态随工作区及 JSON 备份保存。

### 4.9 P2 安全同步基础（已实现）

- 提供可单独启动的最小 Node.js 本地服务，默认仅监听 `127.0.0.1`。同步令牌缺失或长度不足时，同步接口返回“未配置”，不开放匿名读写。
- 服务端令牌仅从 `.env`/进程环境读取；`.env.example` 只提供占位符。前端令牌只保存在当前 React 页面内存中，不写入 IndexedDB、JSON 备份、URL 或 Git。
- 工作区同步支持认证拉取、认证推送、原子文件保存、内容修订号与冲突响应。冲突时必须由用户预览远端、拉取远端或二次确认覆盖远端，禁止静默最后写入覆盖。
- “服务接入”页面展示服务可达、未配置、认证失败、远端为空、冲突和请求失败等真实状态。同步为用户主动操作；尚无账号登录、互联网托管、后台自动同步或端到端加密。
- IndexedDB 工作区升级为 schema v5，安全迁移 schema v3/v4，并持久化非敏感的服务地址、最后远端修订和同步时间。

## 5. 优先级与推荐逻辑

首页关键任务最多三项。推荐分数可基于：

- 距离截止时间。
- 是否逾期。
- 是否缺材料。
- 预计耗时与剩余可用时间。
- 是否存在未完成依赖。
- 用户手动优先级。

系统必须展示简短理由，例如“明晚截止且仍缺 2 项材料”。用户可手动调整优先级，调整后优先尊重用户选择。

## 6. 数据模型

### 6.1 Task

- `id`
- `title`
- `category`
- `status`
- `deadline`
- `estimatedMinutes`
- `nextAction`
- `description`
- `priority`
- `riskFlags`
- `materials`
- `dependencies`
- `reminders`
- `sourceIds`
- `createdAt`
- `updatedAt`

### 6.2 Source

- `id`
- `type`: text / file / image / link
- `title`
- `contentPreview`
- `createdAt`
- `extractionStatus`

### 6.3 HistoryEntry

- `id`
- `taskId`
- `field`
- `before`
- `after`
- `changedAt`
- `actor`: user / system

### 6.4 Reminder

- `id`
- `taskId`
- `channel`: browser / email / wechat-placeholder
- `scheduledAt`
- `enabled`

## 7. 功能分期

### P0：首个可运行骨架

- 桌面端应用壳与侧边栏导航。
- 首页最多三项关键任务。
- 任务卡完整信息与风险表达。
- 快速录入弹层或面板。
- 文本识别建议演示与确认。
- 任务中心、日历、文件库、项目档案基础页面。
- 本地演示数据与基本交互。
- 响应式降级到常见笔记本宽度。

### P1：核心闭环深化

- 已实现：真实任务编辑、本地持久化、修改历史、材料清单与任务依赖。
- 已实现：浏览器存活期间的本地通知、课程表最小录入与避让建议、项目里程碑、基础重复来源提示。
- 已实现：TXT、Markdown 与 PDF 文本层的本地解析入口，以及图片/扫描 PDF 的诚实失败状态。
- 未接通：真实邮件发送、生产 OCR、网页抓取与监测、微信授权、系统日历、跨设备同步。

### P2：集成与智能化

- 已实现：默认关闭的本地服务、服务端令牌认证、手动工作区同步、修订冲突检测与明确覆盖策略。
- 进行中：邮件提醒服务队列、网页通知变更监测接口、微信授权接入状态。
- 未提供：生产 OCR、互联网云托管、账号系统、后台自动同步与自动外发。

## 8. 视觉与交互规范

### 8.1 设计方向

采用“安静的校园指挥台”：沉稳、可信、清晰，不做花哨数据大屏。界面以深海军蓝与墨绿建立专业感，以纸张白和浅灰绿承载内容；暖橙和砖红只用于紧急与风险。

### 8.2 视觉约束

- 首屏视觉焦点必须是关键任务，不是欢迎语或统计数字。
- 重要任务可以使用深色大卡或暖色风险条；普通卡片保持低饱和。
- 不使用紫色渐变、霓虹、大面积玻璃拟态或模板化 SaaS 风格。
- 使用明确的字号、间距和边框层级。
- 动效短促且服务于状态变化，不干扰阅读。
- 中文字体优先选择有编辑感的标题字体与高可读正文搭配，并设置可靠回退。

### 8.3 可访问性

- 正文与背景对比度满足 WCAG AA。
- 所有按钮与表单有可见焦点态和文本标签。
- 颜色不是风险状态的唯一表达方式。
- 支持键盘关闭弹层和完成主要表单操作。

## 9. 技术方案

首版使用：

- React + TypeScript + Vite
- 组件级 CSS，不引入重型 UI 组件库
- Lucide 图标
- Vitest 用于关键逻辑测试
- ESLint 与 TypeScript 严格检查

选择理由：启动快、类型清晰、便于后续接入真实解析与提醒 API，同时可保持视觉实现的自由度。

## 10. 验收标准

### 10.1 产品验收

- 应用可以通过文档中的命令安装并启动。
- 首页默认只突出 1–3 项关键任务。
- 每项关键任务完整展示截止时间、耗时、下一步动作和风险。
- 用户可打开录入面板，粘贴消息并获得可编辑的识别建议。
- 用户确认后能看到任务进入任务中心。
- 包含多个时间点的文本会生成对应数量的独立待确认任务，并可逐条编辑或删除。
- 刷新和关闭后重新打开同一浏览器站点时，任务、编辑、材料、历史与提醒仍能恢复。
- 用户可在主要导航页面间切换。
- 自动结果明确标为建议，微信明确标为后续能力。
- TXT/Markdown/PDF 文本层只在本机读取；图片与扫描 PDF 不会伪装为已完成 OCR。
- 浏览器提醒在授权后可真实触发，并明确标注仅页面存活期间调度；邮件与微信明确标为未接通。
- P0 数据升级后仍可恢复，新增课程、里程碑和重复核对状态也能在刷新后保留。
- 未配置 `.env` 时同步接口必须拒绝读写；错误状态不得显示为已连接或已同步。
- 冲突不会静默覆盖；前端令牌不进入持久化数据、导出文件或 URL。

### 10.2 工程验收

- `npm run lint` 通过。
- `npm run test` 通过。
- `npm run build` 通过。
- 无密钥、账号、个人隐私或构建产物进入 Git。
- 每个可交付阶段使用 Conventional Commits 提交。
- 若已配置远程，每次提交后立即推送；若未配置，保留本地提交并明确报告阻碍。

## 11. 成功指标

MVP 验证阶段关注：

- 用户从录入到确认任务的完成率。
- 用户完成一次录入所需时间。
- 自动字段被用户修改的比例。
- 首页前三项中被执行或完成的比例。
- 因缺材料或日期不明导致的风险提示被解决的比例。

## 12. 风险与边界

- OCR 或自然语言识别存在误差：通过“建议 + 依据 + 确认”降低风险。
- 截止时间可能含歧义：对低置信度字段显式提示待确认。
- 提醒可能涉及敏感联系方式：后续接入时使用授权与最小化存储。
- 网页监测可能受登录、反爬与页面结构影响：必须显示监测状态，不保证绝对完整。
- 客户端演示数据不得被描述为真实用户数据或真实服务结果。
